""" Code is generated by ucloud-model, DO NOT EDIT IT. """

import typing


from ucloud.core.client import Client
from ucloud.services.uhadoop.schemas import apis


class UHadoopClient(Client):
    def __init__(
        self, config: dict, transport=None, middleware=None, logger=None
    ):
        super(UHadoopClient, self).__init__(
            config, transport, middleware, logger
        )

    def add_uhadoop_instance_node(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """AddUHadoopInstanceNode - 给已有集群添加一定数量的节点

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **InstanceId** (str) - (Required) 实例ID
        - **NodeRole** (str) - (Required) 节点的角色，值为task|core|client之一
        - **NodeType** (str) - (Required) 机型，如：o.hadoop2m.medium，可从GetUHadoopNodeType接口获取
        - **BootDiskSize** (str) - 系统盘容量,非裸金属机型必填
        - **BootDiskType** (str) - 系统盘类型，非裸金属机型必填，例如：CLOUD_RSSD
        - **DataDiskNum** (str) - 数据盘数量，非裸金属机型时必填
        - **DataDiskSize** (str) - 数据盘容量，非裸金属机型必填
        - **DataDiskType** (str) - 数据盘类型，非裸金属机型必填，例如：CLOUD_RSSD
        - **NodeCount** (int) - 节点数量,默认为1
        - **Password** (str) - 密码,NodeRole为client时必填
        - **Zone** (str) - 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.AddUHadoopInstanceNodeRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("AddUHadoopInstanceNode", d, **kwargs)
        return apis.AddUHadoopInstanceNodeResponseSchema().loads(resp)

    def create_uhadoop_instance(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CreateUHadoopInstance - 新建一个uhadoop集群

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **AppConfig** (list) - (Required) 集群需要安装的组件，格式：组件#版本通过ListUHadoopFrameworkApp接口获取，例如：Spark#3.3.0
        - **Framework** (str) - (Required) 框架，值为'Hadoop'|'HDFS'|'MR'|'StarRocks'之一,框架，例如Hadoop|MR|HDFS|StarRocks Hadoop框架包含存储与计算服务 MR集群包含计算服务 HDFS只包含存储服务,StarRocks为StarRocks集群
        - **FrameworkVersion** (str) - (Required) 版本,例如：3.2.1-udh3.0,通过ListUHadoopFrameworkApp接口获取
        - **InstanceGroupConfigs** (list) - (Required) 节点配置，里面包括NodeType（机型），NodeRole（节点类别，值为其中之一：master|core|task），Count（数量），DataDiskType（数据盘类别），DataDiskSize（数据盘大小），DataDiskNum（数据盘数量），BootDiskType（系统盘类型），BootDiskSize（系统盘大小），通过GetUHadoopNodeType接口获取，示例为：InstanceGroupConfigs.0.NodeType=o.hadoop2m.xlargeInstanceGroupConfigs.0.NodeRole=masterInstanceGroupConfigs.0.Count=2InstanceGroupConfigs.0.DataDiskType=CLOUD_RSSDInstanceGroupConfigs.0.DataDiskSize=100InstanceGroupConfigs.0.DataDiskNum=1InstanceGroupConfigs.0.BootDiskType=CLOUD_RSSDInstanceGroupConfigs.0.BootDiskSize=50InstanceGroupConfigs.1.NodeType=o.hadoop2m.xlargeInstanceGroupConfigs.1.NodeRole=coreInstanceGroupConfigs.1.Count=3InstanceGroupConfigs.1.DataDiskType=CLOUD_RSSDInstanceGroupConfigs.1.DataDiskSize=200InstanceGroupConfigs.1.DataDiskNum=1InstanceGroupConfigs.1.BootDiskType=CLOUD_RSSDInstanceGroupConfigs.1.BootDiskSize=50
        - **Password** (str) - (Required) 集群机器的登录密码
        - **SubnetId** (str) - (Required) 子网ID
        - **VPCId** (str) - (Required) VPCId
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **BusinessId** (str) - 工作组ID，默认Default
        - **ChargeType** (str) - 支付类别，默认：Month
        - **InstanceName** (str) - 实例名称，默认为实例ID
        - **IsSecurityEnabled** (str) - 是否开启安全组
        - **Quantity** (int) - 数量，默认1
        - **SecGroupIds** (str) - 安全组ID，字符串数组，IsSecurityEnabled为true时生效
        - **StandAloneMetaStore** (str) - 目前只支持传‘udb’
        - **StorgeClusterId** (str) - Framework为‘MR’时，存储集群ID
        - **US3AccessKey** (str) - US3 配置公钥，仅支持框架为StarRocks存算分离时传入
        - **US3Bucket** (str) - US3 bucket名称，仅支持框架为StarRocks存算分离时传入
        - **US3SecretKey** (str) - US3 配置私钥，仅支持框架为StarRocks存算分离时传入
        - **US3TokenName** (str) - US3 Token名称，仅支持框架为StarRocks存算分离时传入

        **Response**

        - **InstanceId** (str) - 实例ID
        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.CreateUHadoopInstanceRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateUHadoopInstance", d, **kwargs)
        return apis.CreateUHadoopInstanceResponseSchema().loads(resp)

    def delete_uhadoop_instance(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DeleteUHadoopInstance - 删除掉一个uhadoop集群

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **InstanceId** (str) - (Required) 实例ID
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ReleaseEIP** (bool) - 是否释放绑定的EIP。true: 解绑EIP后，并释放；其他值或不填：解绑EIP。

        **Response**

        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DeleteUHadoopInstanceRequestSchema().dumps(d)

        resp = self.invoke("DeleteUHadoopInstance", d, **kwargs)
        return apis.DeleteUHadoopInstanceResponseSchema().loads(resp)

    def describe_uhadoop_instance(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DescribeUHadoopInstance - 描述集群

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **InstanceId** (str) - (Required) 实例ID
        - **SubnetId** (str) - 子网ID
        - **VPCId** (str) - VPCID
        - **Zone** (str) - 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **ClusterSet** (list) - 见 **ClusterInfo** 模型定义
        - **Message** (str) - 错误信息

        **Response Model**

        **ClusterInfo**
        - **AppConfigCount** (str) - 组件数量
        - **AppConfigSet** (str) - 组件集合
        - **BusinessId** (str) - 业务组ID
        - **ChargeType** (str) - 付费类型
        - **CoreCount** (str) - core节点数量
        - **CreateTime** (str) - 创建时间
        - **ExpireTime** (str) - 到期时间
        - **Framework** (str) - 框架，值为'Hadoop'|'HDFS'|'MR'|'StarRocks'之一,框架，例如Hadoop|MR|HDFS|StarRocks Hadoop框架包含存储与计算服务 MR集群包含计算服务 HDFS只包含存储服务,StarRocks为StarRocks集群
        - **HadoopVersion** (str) - Hadoop版本，值为 hadoop3.2.1-udh3.0|hadoop3.3.4-udh3.1|hadoop2.8.5-udh2.2
        - **InstanceId** (str) - 实例ID
        - **InstanceName** (str) - 实例名称
        - **IsOpenSecGroup** (str) - 是否开启安全组
        - **MasterCount** (str) - Master节点数量
        - **NodeCount** (str) - 节点数量
        - **NodeSet** (str) - 节点集合
        - **ReleaseVersion** (str) - Uhadoop版本，值为 uhadoop 3.0|uhadoop 2.2|uhadoop 3.1
        - **RunningTime** (str) - 运行时间
        - **State** (str) - 状态，值为以下其中之一，Running(运行中)|Creating(创建中)|CreateFailed(创建失败)|Deploying(部署中)|Updating(变更中)
        - **SubnetId** (str) - 子网ID
        - **Tag** (str) - 集群Tag
        - **TaskCount** (str) - Task节点数量
        - **VPCId** (str) - VPC ID
        - **Zone** (str) - 可用区


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DescribeUHadoopInstanceRequestSchema().dumps(d)

        resp = self.invoke("DescribeUHadoopInstance", d, **kwargs)
        return apis.DescribeUHadoopInstanceResponseSchema().loads(resp)

    def get_uhadoop_node_type(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetUHadoopNodeType - 获取节点类型信息

        **Request**

        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Framework** (str) - 框架，例如Hadoop|MR|HDFS|StarRocks，Hadoop框架包含存储与计算服务，MR集群包含计算服务，HDFS只包含存储服务，StarRocks为StarRocks集群
        - **FrameworkVersion** (str) - 框架版本，例如3.2.1-udh3.0，通过ListUHadoopFrameworkApp接口获取
        - **NodeRole** (str) - 角色，master|core|task
        - **NodeType** (str) - 机型名称

        **Response**

        - **HostTypeSupportNodeType** (str) - 机型可支持的节点类型
        - **InstanceTypeSet** (list) - 见 **InstanceType** 模型定义
        - **Message** (str) - 错误信息
        - **MetaDataUDBUsable** (bool) - 当前可用区是否支持元数据库使用UDB

        **Response Model**

        **DiskSet**
        - **DiskMaxNum** (str) - 最大磁盘数量
        - **DiskMaxSize** (str) - 最大单块盘容量，单位GB
        - **DiskMinNum** (str) - 最小磁盘数量
        - **DiskMinSize** (str) - 最小单块盘容量，单位GB
        - **DiskNum** (int) - 磁盘数量
        - **DiskType** (list) - 可支持的磁盘类型
        - **SingleDiskSize** (int) - 单块盘大小
        - **Size** (int) - 总磁盘大小
        - **Type** (str) - 磁盘角色，分Data数据盘和Boot系统盘


        **InstanceType**
        - **CPU** (str) - cpu大小
        - **CPUToMemoryRatio** (str) - cpu内存比
        - **DiskSet** (list) - 见 **DiskSet** 模型定义
        - **GpuCount** (int) - GPU数量
        - **GpuType** (str) - GPU型号
        - **HostType** (str) - 机型种类，可选Outstanding(快杰机型)|General(普通机型)|BareMetal(裸金属机型)|GPU(GPU机型)四种类型
        - **IsOpenSecgroup** (str) - 是否支持开启安全组
        - **IsUsable** (str) - 是否可用,该机型是否支持创建
        - **Memory** (str) - 内存大小
        - **NodeType** (str) - 机型名称
        - **SuitableRole** (list) - 可用的节点类型，值为core|task|master之一


        """
        # build request
        d = {
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GetUHadoopNodeTypeRequestSchema().dumps(d)

        resp = self.invoke("GetUHadoopNodeType", d, **kwargs)
        return apis.GetUHadoopNodeTypeResponseSchema().loads(resp)

    def list_uhadoop_framework_app(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListUHadoopFrameworkApp - 列举可选app

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Framework** (str) - (Required) 框架，值为'Hadoop'|'HDFS'|'MR'|'StarRocks'之一,框架，例如Hadoop|MR|HDFS|StarRocks Hadoop框架包含存储与计算服务 MR集群包含计算服务 HDFS只包含存储服务,StarRocks为StarRocks集群
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **InstanceId** (str) - 实例ID,可选。传的话，过滤出适合此集群的app信息。

        **Response**

        - **AppConfigSet** (list) - 见 **AppConfig** 模型定义
        - **Message** (str) - 错误信息

        **Response Model**

        **AppConfig**
        - **AppName** (str) - 需要安装的应用如：Hive,HBase, Spark,Hue,Pig等其他组件
        - **AppStatus** (str) - 应用的状态(运行中)'Running'｜(已停止)'Stopped'｜(启动中)'Starting'｜(停止中)'Stopping'|(启动失败)'StartFailed'|(停止失败)'StopFailed'|(安装中)'Installing'|(安装失败)'InstallFailed'|(未安装)'NotInstalled',
        - **AppVersion** (str) - 应用的版本号(0.13.1,0.98.6 等等)


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListUHadoopFrameworkAppRequestSchema().dumps(d)

        resp = self.invoke("ListUHadoopFrameworkApp", d, **kwargs)
        return apis.ListUHadoopFrameworkAppResponseSchema().loads(resp)

    def list_uhadoop_framework_app_by_use_case(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListUHadoopFrameworkAppByUseCase - 按使用场景列出uhadoop的框架和框架中的应用

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **AppConfigSet** (list) - 见 **AppConfigVersion** 模型定义
        - **Message** (str) - 错误信息

        **Response Model**

        **UseCases**
        - **AppVersion** (str) - 组件版本
        - **Case** (str) - 集群种类
        - **MustHas** (str) - 依赖的组件


        **AppConfigVersion**
        - **Framework** (str) - 框架，值为'Hadoop'|'HDFS'|'MR'|'StarRocks'之一,框架，例如Hadoop|MR|HDFS|StarRocks Hadoop框架包含存储与计算服务 MR集群包含计算服务 HDFS只包含存储服务,StarRocks为StarRocks集群
        - **FrameworkVersion** (str) - 框架版本
        - **HadoopVersion** (str) - Hadoop版本，值为 hadoop3.2.1-udh3.0|hadoop3.3.4-udh3.1 |hadoop2.8.5-udh2.2
        - **ReleaseVersion** (str) - UHadoop版本，值为 uhadoop 3.0|uhadoop 2.2|uhadoop 3.1
        - **UseCases** (list) - 见 **UseCases** 模型定义


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListUHadoopFrameworkAppByUseCaseRequestSchema().dumps(d)

        resp = self.invoke("ListUHadoopFrameworkAppByUseCase", d, **kwargs)
        return apis.ListUHadoopFrameworkAppByUseCaseResponseSchema().loads(resp)

    def list_uhadoop_instance(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListUHadoopInstance - 列出用户所有的uhadoop集群

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Limit** (int) - 获取列表的长度限制，默认值为60
        - **Offset** (int) - 获取列表的偏移，默认值为0
        - **Zone** (str) - 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **ClusterSet** (list) - 见 **ListClusterInfo** 模型定义
        - **Message** (str) - 错误信息
        - **TotalCount** (int) - 总数

        **Response Model**

        **ListClusterInfo**
        - **BusinessId** (str) - 业务组ID
        - **CreateTime** (str) - 创建时间
        - **Framework** (str) - 框架，值为'Hadoop'|'HDFS'|'MR'|'StarRocks'之一，Hadoop框架包含存储与计算服务，MR集群包含计算服务，HDFS只包含存储服务，StarRocks为StarRocks集群
        - **HadoopVersion** (str) - Hadoop版本，值为 hadoop3.2.1-udh3.0|hadoop3.3.4-udh3.1 |hadoop2.8.5-udh2.2
        - **InstanceId** (str) - 实例ID
        - **InstanceName** (str) - 实例名称
        - **ReleaseVersion** (str) - Uhadoop版本，值为 uhadoop 3.0|uhadoop 2.2|uhadoop 3.1
        - **State** (str) - 状态，值为以下其中之一，Running(运行中)|Creating(创建中)|CreateFailed(创建失败)|Deploying(部署中)|Updating(变更中)
        - **SubnetId** (str) - 子网ID
        - **VPCId** (str) - VPC ID
        - **Zone** (str) - 可用区


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListUHadoopInstanceRequestSchema().dumps(d)

        resp = self.invoke("ListUHadoopInstance", d, **kwargs)
        return apis.ListUHadoopInstanceResponseSchema().loads(resp)

    def restart_uhadoop_service(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """RestartUHadoopService - 重启集群服务（包含start|stop|restart）

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **InstanceId** (str) - (Required) 实例ID
        - **ServiceName** (str) - (Required) 要操作的服务名。可通过ListUHadoopFrameworkApp接口获取
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ApplicationVersion** (str) - 应用版本，ApplicationVersion传参时，表示这次操作是整个应用所有服务。
        - **NodeId** (list) - 要操作的NodeId数组。如果传入，则用于过滤操作哪些Node。
        - **NodeRole** (list) - 要操作的NodeRole数组。如果传入，则用于过滤操作哪些Node。值为以下之一master|core|task
        - **OnlyStart** (bool) - 只启动。值为false|true之一，默认false，当OnlyStart和OnlyStop同时置为true，则重启。
        - **OnlyStop** (bool) - 只停止。值为false|true之一，默认false。当OnlyStart和OnlyStop同时置为true，则重启。

        **Response**

        - **Message** (str) - 错误信息
        - **State** (str) - 当前执行状态，running(操作运行中)|success(操作成功)|failed(操作失败)|killed(操作被终止)。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.RestartUHadoopServiceRequestSchema().dumps(d)

        resp = self.invoke("RestartUHadoopService", d, **kwargs)
        return apis.RestartUHadoopServiceResponseSchema().loads(resp)

    def upgrade_uhadoop_node(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """UpgradeUHadoopNode - 升级uhadoop节点

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **InstanceId** (str) - (Required) 实例ID
        - **NodeRole** (str) - (Required) 节点角色，支持"master"|"core"|"task"
        - **NodeType** (str) - (Required) 节点新机型，通过GetUHadoopNodeType接口获取
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **NodeNames** (list) - 节点名称，当NodeRole不为master时必填

        **Response**

        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.UpgradeUHadoopNodeRequestSchema().dumps(d)

        resp = self.invoke("UpgradeUHadoopNode", d, **kwargs)
        return apis.UpgradeUHadoopNodeResponseSchema().loads(resp)

    def upgrade_uhadoop_node_disk(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """UpgradeUHadoopNodeDisk - 扩容集群节点磁盘

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **DataDiskSize** (int) - (Required) 新的数据盘磁盘大小
        - **InstanceId** (str) - (Required) 实例ID
        - **NodeRole** (str) - (Required) 节点角色，值为master|core|task之一
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **BootDiskSize** (int) - 系统盘磁盘大小，仅支持云盘裸金属机型（系统盘和数据盘数值同时增加、只会处理系统盘参数）
        - **NodeNames** (list) - 节点名称集合,当NodeRole不为master时必填

        **Response**

        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.UpgradeUHadoopNodeDiskRequestSchema().dumps(d)

        resp = self.invoke("UpgradeUHadoopNodeDisk", d, **kwargs)
        return apis.UpgradeUHadoopNodeDiskResponseSchema().loads(resp)
