""" Code is generated by ucloud-model, DO NOT EDIT IT. """

import typing


from ucloud.core.client import Client
from ucloud.services.ukafka.schemas import apis


class UKafkaClient(Client):
    def __init__(
        self, config: dict, transport=None, middleware=None, logger=None
    ):
        super(UKafkaClient, self).__init__(
            config, transport, middleware, logger
        )

    def add_ukafka_instance_node(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """AddUKafkaInstanceNode - 给实例添加节点

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **InstanceId** (str) - (Required) 实例ID
        - **NodeCount** (int) - (Required) 新添加节点数量
        - **NodeType** (str) - (Required) 机型，支持的机型可通过GetUKafkaNodeType 接口返回的InstanceTypeSet[].InstanceTypeName
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.AddUKafkaInstanceNodeRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("AddUKafkaInstanceNode", d, **kwargs)
        return apis.AddUKafkaInstanceNodeResponseSchema().loads(resp)

    def create_ukafka_instance(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CreateUKafkaInstance - 创建一个ukafka实例

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ChargeType** (str) - (Required) 付费方式
        - **DiskSize** (int) - (Required) 数据盘大小。支持范围根据GetUKafkaNodeType 接口返回的InstanceTypeSet[].MaxDiskSize 和MinDiskSize获取
        - **FrameworkVersion** (str) - (Required) kafka版本，支持的版本可通过ListUKafkaFrameworkVersion 接口返回字段的FrameworkVersions获取
        - **InstanceName** (str) - (Required) 实例名，可自定义
        - **NodeType** (str) - (Required) 机型，支持的机型可通过GetUKafkaNodeType 接口返回的InstanceTypeSet[].InstanceTypeName
        - **SubnetId** (str) - (Required) 子网 ID
        - **VPCId** (str) - (Required) VPCID
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **BusinessId** (str) - 业务组，默认Default
        - **DiskControllerType** (str) - 磁盘管理方式,支持值：NONE、CLEAN。默认值：NONE
        - **DiskThreshold** (str) - 磁盘清理阈值，支持范围[70,90]。DiskControllerType 为CLEAN 时必填。默认值 90
        - **IsSecurityEnabled** (str) - 是否开启安全组，支持"true","false"，默认 false
        - **LogRetentionHours** (str) - kafka 日志保存时间，支持范围[1,240]。默认 72 小时
        - **NodeCount** (int) - 集群节点数量。默认 3 节点
        - **Quantity** (str) - 实例数量，默认 1

        **Response**

        - **InstanceId** (str) - 实例资源 ID
        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.CreateUKafkaInstanceRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateUKafkaInstance", d, **kwargs)
        return apis.CreateUKafkaInstanceResponseSchema().loads(resp)

    def delete_ukafka_instance(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DeleteUKafkaInstance - 删除实例

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **InstanceId** (str) - (Required) 实例ID
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DeleteUKafkaInstanceRequestSchema().dumps(d)

        resp = self.invoke("DeleteUKafkaInstance", d, **kwargs)
        return apis.DeleteUKafkaInstanceResponseSchema().loads(resp)

    def describe_ukafka_consumer(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DescribeUKafkaConsumer - 获取 Kafka 消费组信息

        **Request**

        - **ProjectId** (str) - (Config) 项目 ID。不填写为默认项目，子帐号必须填写。 请参考  `GetProjectList 接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterInstanceId** (str) - (Required) Kafka 集群 ID
        - **ConsumerGroup** (str) - (Required) 消费组组名
        - **Type** (str) - (Required) 消费者组类型（同消费者组列表返回的类型值）
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **GroupName** (str) - 消费者组组名
        - **Topics** (str) - 消费者组所订阅 topic 信息
        - **Type** (str) - 消费者组类型

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DescribeUKafkaConsumerRequestSchema().dumps(d)

        resp = self.invoke("DescribeUKafkaConsumer", d, **kwargs)
        return apis.DescribeUKafkaConsumerResponseSchema().loads(resp)

    def describe_ukafka_instance(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DescribeUKafkaInstance - 获取整个集群的信息

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterInstanceId** (str) - (Required) 实例ID，可以通过ListUKafkaInstance 接口的ClusterSet. ClusterInstanceId 获取
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **ClusterSet** (list) - 见 **ClusterInfo** 模型定义

        **Response Model**

        **UHostConfig**
        - **BootDiskSize** (int) - 节点系统盘大小（单位G)【删除】
        - **CPU** (int) - 节点 CPU 核心数
        - **DataDiskSize** (int) - 节点数据盘大小（单位G)
        - **DiskType** (str) - 节点数据盘类型
        - **Memory** (int) - 节点内存(单位MB)
        - **OS** (str) - 节点内部系统名称【删除】


        **BrokerOfTopicInfo**
        - **PartitionOnBroker** (str) - topic 在 broker 上分布的 partition 数量
        - **Partitions** (str) - topic 在 broker上 partition 列表
        - **Replication** (int) - Topic 副本数
        - **TopicName** (str) - topic 名称
        - **TotalPartitions** (int) - partition 数量


        **Url**
        - **Addr** (str) - 访问地址
        - **Protocal** (str) - 访问协议


        **Endpoints**
        - **PlainText** (dict) - 见 **Url** 模型定义
        - **SaslPlainText** (dict) - 见 **Url** 模型定义


        **IP**
        - **IP** (str) - IP地址
        - **Type** (str) - IP类型


        **Broker**
        - **BrokerId** (str) - broker id
        - **BrokerInfo** (list) - 见 **BrokerOfTopicInfo** 模型定义
        - **CreateTime** (str) - broker 创建信息
        - **DomainName** (str) - broker 域名
        - **Endpoints** (dict) - 见 **Endpoints** 模型定义
        - **ExpireTime** (int) - broker 过期时间
        - **IPSet** (list) - 见 **IP** 模型定义
        - **InstanceGroupType** (str) - broker 机型信息
        - **KafkaPort** (int) - kafka 服务端口
        - **Remark** (str) - broker 备注信息
        - **ResourceId** (str) - broker 注册资源信息
        - **SecurityGroupId** (str) - 安全组 id
        - **State** (str) - broker 当前状态
        - **UHostConfig** (dict) - 见 **UHostConfig** 模型定义
        - **UHostId** (str) - 节点 id
        - **UHostName** (str) - 节点名称
        - **UHostRole** (str) - 节点类型
        - **ZooKeeper** (str) - 节点是否部署 zookeeper
        - **ZooKeeperPort** (int) - zookeeper 服务端口


        **ClusterInfo**
        - **AutoRenew** (str) - 是否开启自动续费
        - **BusinessId** (str) - 业务组 ID
        - **ChargeType** (str) - 付费类型
        - **ClusterInstanceId** (str) - 实例id
        - **ClusterInstanceName** (str) - 实例名称
        - **CreateTime** (int) - 实例创建时间
        - **ExpireTime** (int) - 实例过期时间
        - **Framework** (str) - 实例框架
        - **FrameworkVersion** (str) - 实例框架版本
        - **IsOpenSecgroup** (str) - 是否开启安全组
        - **NetworkId** (str) - 网络 id（已废弃）
        - **Remark** (str) - 实例备注
        - **RunningTime** (int) - 实例运行时间
        - **State** (str) - 实例当前状态,实例状态："Running"| "Abnormal"| "Creating"| "Deleting"| "CreateFailed"| "DeleteFailed"| "Unavailable"| "Deleted"| "Updating"| "Deploying"| "Migrating"| "ExpandFailed"
        - **SubnetId** (str) - 所属子网 id
        - **Tag** (str) - 实例标记
        - **UHostCount** (int) - 实例节点个数
        - **UHostSet** (list) - 见 **Broker** 模型定义
        - **VPCId** (str) - 所属 VPC id
        - **ValidBrokerNum** (int) - 可用节点个数
        - **Zone** (str) - 所属可用区


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DescribeUKafkaInstanceRequestSchema().dumps(d)

        resp = self.invoke("DescribeUKafkaInstance", d, **kwargs)
        return apis.DescribeUKafkaInstanceResponseSchema().loads(resp)

    def get_ukafka_node_type(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetUKafkaNodeType - 获取节点机型配置

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **NodeType** (str) - 传参时返回指定机型信息，参数为空时返回所有机型信息

        **Response**

        - **Message** (str) - 错误信息
        - **NodeTypeSet** (list) - 见 **InstanceType** 模型定义

        **Response Model**

        **DiskSet**
        - **Size** (str) - 磁盘大小
        - **Type** (str) - 磁盘类型。Boot 表示系统盘，Data 表示数据盘


        **InstanceType**
        - **CPU** (int) - CPU核心数
        - **DiskSet** (dict) - 见 **DiskSet** 模型定义
        - **DiskType** (str) - 磁盘类型。RSSD 表示固态云盘，SSD 表示本地固态盘，COMMON 表示本地 SATA 盘
        - **Memory** (str) - 内存大小（单位 MB）
        - **NodeTypeName** (str) - 机型名称


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GetUKafkaNodeTypeRequestSchema().dumps(d)

        resp = self.invoke("GetUKafkaNodeType", d, **kwargs)
        return apis.GetUKafkaNodeTypeResponseSchema().loads(resp)

    def get_ukafka_topic_info(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetUKafkaTopicInfo - 获取指定 topic 的详细信息，包括Topic属性、消息动态、分区情况

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list.html>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist.html>`_
        - **ClusterInstanceId** (str) - (Required) Ukafka 集群 ID
        - **Topic** (str) - (Required) 指定的 Topic
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist.html>`_

        **Response**

        - **MessageState** (dict) - 见 **MessageState** 模型定义
        - **Partitions** (dict) - topic 分区信息
        - **TopicProperty** (dict) - 见 **TopicProperty** 模型定义

        **Response Model**

        **TopicProperty**
        - **BrokerSkewedPer** (float) - topic 的 broker 倾斜率
        - **BrokerSpreadPer** (float) - topic 的 broker 覆盖率
        - **NumOfBroker** (int) - broker 数量
        - **NumOfOccupyBroker** (int) - 占用 broker 数量
        - **NumOfPartition** (int) - 分区数量
        - **NumOfReplica** (int) - 副本数量
        - **PreferredReplicasPer** (float) - 优先副本占有率
        - **SumOfPartitionOffset** (int) - topic 的 offset 之和
        - **SumTopicUsage** (int) - topic 的磁盘使用之和
        - **UnderReplicasPer** (float) - 落后副本占比


        **MessageState**
        - **BytesInPerSec** (float) - topic 每分钟流入消息速率
        - **BytesOutPerSec** (float) - topic 每分钟流出消息速率
        - **LastUpdateTime** (int) - topic上次更新时间（时间戳）
        - **MessagesInPerSec** (float) - topic 每分钟流入消息条数


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GetUKafkaTopicInfoRequestSchema().dumps(d)

        resp = self.invoke("GetUKafkaTopicInfo", d, **kwargs)
        return apis.GetUKafkaTopicInfoResponseSchema().loads(resp)

    def is_ukafka_topic_name_exist(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """IsUKafkaTopicNameExist - 检查一个topic名称是否已经在集群中了

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list.html>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist.html>`_
        - **ClusterInstanceId** (str) - (Required) 集群ID
        - **TopicName** (str) - (Required) 待检查的topic名称
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist.html>`_

        **Response**

        - **IsExist** (str) - 是否已经存在

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.IsUKafkaTopicNameExistRequestSchema().dumps(d)

        resp = self.invoke("IsUKafkaTopicNameExist", d, **kwargs)
        return apis.IsUKafkaTopicNameExistResponseSchema().loads(resp)

    def list_ukafka_consumers(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListUKafkaConsumers - 列出 Kafka 消费组

        **Request**

        - **ProjectId** (str) - (Config) 项目 ID。不填写为默认项目，子帐号必须填写。 请参考  `GetProjectList 接口 <https://docs.ucloud.cn/api/summary/get_project_list.html>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist.html>`_
        - **ClusterInstanceId** (str) - (Required) Kafka 集群 ID
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist.html>`_

        **Response**

        - **Groups** (list) - 见 **Group** 模型定义

        **Response Model**

        **Group**
        - **GroupName** (str) - 消费者组组名
        - **NumOfTopics** (int) - 订阅 Topic 数量
        - **Type** (str) - 消费者组类型


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListUKafkaConsumersRequestSchema().dumps(d)

        resp = self.invoke("ListUKafkaConsumers", d, **kwargs)
        return apis.ListUKafkaConsumersResponseSchema().loads(resp)

    def list_ukafka_framework_version(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListUKafkaFrameworkVersion - 列举ukafka框架版本

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **FrameworkVersions** (list) - 见 **Version** 模型定义
        - **Message** (str) - 错误信息

        **Response Model**

        **Version**
        - **Label** (str) - kafka 版本标签。recommend 表示推荐版本，beta 表示公测版本
        - **Version** (str) - kafka版本


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListUKafkaFrameworkVersionRequestSchema().dumps(d)

        resp = self.invoke("ListUKafkaFrameworkVersion", d, **kwargs)
        return apis.ListUKafkaFrameworkVersionResponseSchema().loads(resp)

    def list_ukafka_instance(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListUKafkaInstance - 列举集群信息

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **BusinessId** (str) - 业务组 ID
        - **ClusterInstanceId** (str) - 实例ID
        - **Filter** (str) - 是否过滤删除了的节点，默认为‘true’
        - **Limit** (str) - 默认为60
        - **Offset** (str) - 默认为0
        - **SubnetId** (str) - SubnetId
        - **VPCId** (str) - VPCId
        - **Zone** (str) - 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **ClusterSet** (str) - 信息
        - **Message** (str) - 错误信息
        - **TotalCount** (str) - 总数

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListUKafkaInstanceRequestSchema().dumps(d)

        resp = self.invoke("ListUKafkaInstance", d, **kwargs)
        return apis.ListUKafkaInstanceResponseSchema().loads(resp)

    def list_ukafka_topics(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListUKafkaTopics - 展示kafka集群上所有topic

        **Request**

        - **ProjectId** (str) - (Config) 项目 ID。不填写为默认项目，子帐号必须填写。 请参考  `GetProjectList 接口 <https://docs.ucloud.cn/api/summary/get_project_list.html>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist.html>`_
        - **ClusterInstanceId** (str) - (Required) 集群资源id
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist.html>`_

        **Response**

        - **Length** (int) - 列表长度
        - **TopicList** (list) - 见 **TopicInfo** 模型定义

        **Response Model**

        **TopicInfo**
        - **NumOfOccupyBroker** (int) - 占用 broker 数量
        - **NumOfPartition** (int) - 分区数量
        - **NumOfReplica** (int) - 副本数量
        - **Status** (str) - topic 状态
        - **Topic** (str) - topic 名称
        - **UnderReplicasPer** (float) - 落后副本占比


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListUKafkaTopicsRequestSchema().dumps(d)

        resp = self.invoke("ListUKafkaTopics", d, **kwargs)
        return apis.ListUKafkaTopicsResponseSchema().loads(resp)

    def modify_ukafka_instance_type(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ModifyUKafkaInstanceType - 规格升降级

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **InstanceId** (str) - (Required) 实例ID
        - **NodeType** (str) - (Required) 目标机型，支持的机型可通过GetUKafkaNodeType 接口返回的InstanceTypeSet[].InstanceTypeName
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ModifyUKafkaInstanceTypeRequestSchema().dumps(d)

        resp = self.invoke("ModifyUKafkaInstanceType", d, **kwargs)
        return apis.ModifyUKafkaInstanceTypeResponseSchema().loads(resp)

    def resize_ukafka_disk(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ResizeUKafkaDisk - 磁盘扩容

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **DiskSize** (int) - (Required) 数据盘大小。支持范围根据GetUKafkaNodeType 接口返回的InstanceTypeSet[].MaxDiskSize 和MinDiskSize获取
        - **InstanceId** (str) - (Required) 实例ID
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ResizeUKafkaDiskRequestSchema().dumps(d)

        resp = self.invoke("ResizeUKafkaDisk", d, **kwargs)
        return apis.ResizeUKafkaDiskResponseSchema().loads(resp)
