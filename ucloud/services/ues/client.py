""" Code is generated by ucloud-model, DO NOT EDIT IT. """

import typing


from ucloud.core.client import Client
from ucloud.services.ues.schemas import apis


class UESClient(Client):
    def __init__(
        self, config: dict, transport=None, middleware=None, logger=None
    ):
        super(UESClient, self).__init__(config, transport, middleware, logger)

    def create_ues_instance(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CreateUESInstance - 创建实例

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **AppVersion** (str) - (Required) 应用服务版本号，支持的类型通过GetUESAppVersion AppVersionList[].AppVersion
        - **InstanceName** (str) - (Required) 实例名称
        - **KibanaNodeConf** (str) - (Required) Kibana节点配置, 支持的机型可通过GetUESNodeConf NodeConfList[].NodeConf
        - **KibanaNodeDiskConf** (str) - (Required) Kibana节点磁盘类型
        - **NodeConf** (str) - (Required) 节点配置标识, 支持的机型可通过GetUESNodeConf NodeConfList[].NodeConf
        - **NodeDiskConf** (str) - (Required) 磁盘类型
        - **SubnetId** (str) - (Required) 子网ID标识
        - **VPCId** (str) - (Required) VPCID标识
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **AppName** (str) - 应用名称，支持的类型通过GetUESAppVersion AppVersionList[].AppName, 默认为elasticsearch
        - **BusinessId** (str) - 业务组ID标识
        - **ChargeType** (str) - 计费类型，默认为Month
        - **CoordinatingNodeConf** (str) - Coordinating节点机型配置，, 支持的机型可通过GetUESNodeConf NodeConfList[].NodeConf,  默认为空
        - **CoordinatingNodeDiskConf** (str) - Coordinating节点磁盘类型
        - **CoordinatingNodeSize** (int) - Coordinating节点数量
        - **IsMultiZone** (bool) - 是否为多可用区，默认为false
        - **IsSecGroup** (bool) - 是否开启安全组，默认为false
        - **MasterConf** (str) - 主节点类型标示，支持的机型可通过GetUESNodeConf NodeConfList[].NodeConf, 默认为空
        - **MultiZones** (list) - 多可用区名称，默认空数组 []
        - **NodeDiskSize** (int) - 节点磁盘大小，默认为100G
        - **NodeSize** (int) - 节点个数，默认数目为3
        - **Quantity** (int) - 计费长度，默认为1
        - **Remark** (str) - 备注，默认为空
        - **SecGroupIds** (list) - 安全组ID，开启安全组必填，至多可以同时绑定5个安全组
        - **ServicePasswd** (str) - 服务用户密码，默认为changeme
        - **ServiceUserName** (str) - elasticsearch 服务用户名称，默认为elastic；OpenSearch 服务用户名称，固定为admin

        **Response**

        - **InstanceId** (str) - 实例ID
        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.CreateUESInstanceRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateUESInstance", d, **kwargs)
        return apis.CreateUESInstanceResponseSchema().loads(resp)

    def delete_ues_instance(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DeleteUESInstance - 删除实例

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **InstanceId** (str) - (Required) 实例ID
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DeleteUESInstanceRequestSchema().dumps(d)

        resp = self.invoke("DeleteUESInstance", d, **kwargs)
        return apis.DeleteUESInstanceResponseSchema().loads(resp)

    def describe_ues_instance_v2(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DescribeUESInstanceV2 - 查询指定实例详细信息

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **InstanceId** (str) - (Required) 集群实例ID
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Result** (dict) - 见 **ClusterNodeV2Info** 模型定义

        **Response Model**

        **ClusterV2Info**
        - **BusinessId** (str) - 项目组ID标识
        - **Region** (str) - 地域
        - **ServiceVersion** (str) - 服务版本号
        - **State** (str) - 服务集群状态
        - **SubnetId** (str) - 子网ID标识
        - **UESInstanceId** (str) - 服务集群ID标识
        - **UESInstanceName** (str) - 服务集群名称
        - **VPCId** (str) - VPCID标识
        - **Vip** (str) - Vip
        - **Zone** (str) - 可用区


        **NodeV2Info**
        - **CPU** (int) - 节点cpu数量
        - **DiskSize** (int) - 节点磁盘大小
        - **DiskType** (str) - 节点磁盘类型
        - **Memory** (int) - 节点内存大小
        - **NodeConf** (str) - 节点配置标识
        - **NodeIP** (str) - 节点IP
        - **NodeId** (str) - 节点ID
        - **NodeName** (str) - 节点名称
        - **NodeRole** (str) - 节点类型
        - **NodeState** (str) - 节点状态


        **ClusterNodeV2Info**
        - **ClusterInfo** (dict) - 见 **ClusterV2Info** 模型定义
        - **NodeInfoList** (list) - 见 **NodeV2Info** 模型定义
        - **RequestId** (str) -


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DescribeUESInstanceV2RequestSchema().dumps(d)

        resp = self.invoke("DescribeUESInstanceV2", d, **kwargs)
        return apis.DescribeUESInstanceV2ResponseSchema().loads(resp)

    def expand_ues_instance(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ExpandUESInstance - 扩容实例节点

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **InstanceId** (str) - (Required) 实例ID
        - **NodeCount** (int) - (Required) 扩容后对应类型节点的数目
        - **NodeRole** (str) - (Required) 节点类型（compute、coordinating）
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ExpandUESInstanceRequestSchema().dumps(d)

        resp = self.invoke("ExpandUESInstance", d, **kwargs)
        return apis.ExpandUESInstanceResponseSchema().loads(resp)

    def get_ues_app_version(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetUESAppVersion - 获取服务应用版本列表

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **AppVersionList** (list) - 见 **AppVersion** 模型定义
        - **Message** (str) - 错误信息
        - **TotalCount** (int) - 服务应用版本个数

        **Response Model**

        **AppVersion**
        - **AppName** (str) - 应用名称，默认值为elasticsearch
        - **AppVersion** (str) - 应用版本号
        - **IsMultiZone** (bool) - 是否支持多区部署，默认为false


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GetUESAppVersionRequestSchema().dumps(d)

        resp = self.invoke("GetUESAppVersion", d, **kwargs)
        return apis.GetUESAppVersionResponseSchema().loads(resp)

    def get_ues_disk_size_limitation(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetUESDiskSizeLimitation - 获取磁盘容量限制

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **DiskSizeLimitationSet** (list) - 见 **DiskSizeLimitation** 模型定义
        - **Message** (str) - 错误信息

        **Response Model**

        **DiskSizeLimitation**
        - **DiskType** (str) - 数据盘类别，包含普通云盘|CLOUD_NORMAL、SSD云盘|CLOUD_SSD和RSSD云盘|CLOUD_RSSD。普通本地盘只包含普通本地盘|LOCAL_NORMAL一种。SSD本地盘只包含SSD本地盘|LOCAL_SSD一种。
        - **MaxSize** (int) - 最大值，单位GB
        - **MinSize** (int) - 最小值，单位GB


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GetUESDiskSizeLimitationRequestSchema().dumps(d)

        resp = self.invoke("GetUESDiskSizeLimitation", d, **kwargs)
        return apis.GetUESDiskSizeLimitationResponseSchema().loads(resp)

    def get_ues_node_conf(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetUESNodeConf - 获取节点配置列表

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Message** (str) - 错误信息
        - **NodeConfList** (list) - 见 **NodeConf** 模型定义
        - **TotalCount** (int) - 所有节点配置信息的个数

        **Response Model**

        **NodeConf**
        - **CPU** (int) - CPU数量
        - **DiskSize** (int) - 磁盘大小，单位为GB
        - **DiskType** (str) - 磁盘类型[RSSD|SSD]
        - **IsSecGroup** (bool) - 是否支持安全组[true|false]
        - **Memory** (int) - 内存，单位为GB
        - **NodeConf** (str) - 节点配置标识


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GetUESNodeConfRequestSchema().dumps(d)

        resp = self.invoke("GetUESNodeConf", d, **kwargs)
        return apis.GetUESNodeConfResponseSchema().loads(resp)

    def list_ues_instance(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListUESInstance - 获取实例列表

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Limit** (int) - 返回数据长度, 默认为30
        - **Offset** (int) - 数据偏移量, 默认为0
        - **Zone** (str) - 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **ClusterSet** (list) - 见 **ClusterInfo** 模型定义
        - **Message** (str) - 错误信息
        - **TotalCount** (int) - 实例个数

        **Response Model**

        **ClusterInfo**
        - **AppName** (str) - 应用名称
        - **AppVersion** (str) - 应用服务版本号
        - **BusinessId** (str) - 项目组ID标识
        - **ChargeType** (str) - 计费类型，默认为Month
        - **CreateTime** (int) - 创建时间
        - **ExpireTime** (int) - 失效时间
        - **InstanceId** (str) - 实例资源ID
        - **InstanceName** (str) - 实例名称
        - **IsSecGroup** (bool) - 是否开启安全组
        - **MultiZones** (list) - 多可用区
        - **NodeCount** (int) - 节点个数，默认为集群大小
        - **Resizable** (bool) - 是否支持改配
        - **RunTime** (int) - 实例运行时长
        - **ServiceVersion** (str) - 服务版本号（弃用）
        - **State** (str) - 实例状态
        - **SubnetId** (str) - 子网ID标识
        - **Tag** (str) - 业务组
        - **UESInstanceId** (str) - 服务集群ID标识（弃用）
        - **UESInstanceName** (str) - 服务集群名称（弃用）
        - **VPCId** (str) - VPCID标识
        - **Vip** (str) - VIP地址信息
        - **Zone** (str) - 可用区


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListUESInstanceRequestSchema().dumps(d)

        resp = self.invoke("ListUESInstance", d, **kwargs)
        return apis.ListUESInstanceResponseSchema().loads(resp)

    def resize_ues_instance(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ResizeUESInstance - 改配实例节点配置

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **InstanceId** (str) - (Required) 实例ID
        - **NodeRole** (str) - (Required) 节点类型（compute、master、coordinating、kibana、dashboard）
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ForceResizing** (bool) - 进行改配操作是否强制检查集群健康状态，默认为false
        - **NodeConf** (str) - 改配节点类型，NodeDiskSize为0，基于NodeRole 进行改配
        - **NodeDiskSize** (int) - 改配节点磁盘大小，NodeConf 为空字符串，基于NodeRole 进行改配

        **Response**

        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ResizeUESInstanceRequestSchema().dumps(d)

        resp = self.invoke("ResizeUESInstance", d, **kwargs)
        return apis.ResizeUESInstanceResponseSchema().loads(resp)

    def restart_ues_instance(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """RestartUESInstance - 重启实例

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **InstanceId** (str) - (Required) 实例ID
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Message** (str) - 错误信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.RestartUESInstanceRequestSchema().dumps(d)

        resp = self.invoke("RestartUESInstance", d, **kwargs)
        return apis.RestartUESInstanceResponseSchema().loads(resp)
