""" Code is generated by ucloud-model, DO NOT EDIT IT. """

import typing


from ucloud.core.client import Client
from ucloud.services.ukms.schemas import apis


class UKMSClient(Client):
    def __init__(
        self, config: dict, transport=None, middleware=None, logger=None
    ):
        super(UKMSClient, self).__init__(config, transport, middleware, logger)

    def cancel_key_deletion(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CancelKeyDeletion - 取消计划删除中的密钥。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥  ID。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.CancelKeyDeletionRequestSchema().dumps(d)

        resp = self.invoke("CancelKeyDeletion", d, **kwargs)
        return apis.CancelKeyDeletionResponseSchema().loads(resp)

    def cancel_schedule_key_deletion(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CancelScheduleKeyDeletion - 取消计划删除密钥

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **KeyId** (str) - (Required) 需要查看的主密钥对应的 KeyId
        - **ResourceId** (str) - 资源id

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.CancelScheduleKeyDeletionRequestSchema().dumps(d)

        resp = self.invoke("CancelScheduleKeyDeletion", d, **kwargs)
        return apis.CancelScheduleKeyDeletionResponseSchema().loads(resp)

    def create_alias(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """CreateAlias - 为密钥创建别名。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **AliasName** (str) - (Required) 密钥别名，格式为 alias/name。
        - **KeyId** (str) - (Required) 密钥 ID、ARN 或别名。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.CreateAliasRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateAlias", d, **kwargs)
        return apis.CreateAliasResponseSchema().loads(resp)

    def create_key(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """CreateKey - 创建密钥。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **ResourceId** (str) - (Required) UKMS 实例资源 ID。
        - **Alias** (str) - 可选密钥别名，格式为 alias/name。
        - **DeletionProtection** (str) - 是否开启删除保护。可选值：true、false；默认 false。
        - **Description** (str) - 密钥描述，最多 8192 字符。
        - **KeySpec** (str) - 密钥规格，默认 SYMMETRIC_DEFAULT（AES_256）。可选值：SYMMETRIC_DEFAULT(AES_256)、RSA_2048、RSA_3072、RSA_4096、ECC_NIST_P256、ECC_NIST_P384、ECC_NIST_P521、HMAC_256、HMAC_384、HMAC_512。
        - **KeyUsage** (str) - 密钥用途
        - **Origin** (str) - 密钥材料来源，默认 UCLOUD_KMS。当前仅支持 UCLOUD_KMS；EXTERNAL 为 BYOK 规划值，当前传入会返回 100660。

        **Response**

        - **KeyId** (str) - 密钥 ID。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.CreateKeyRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateKey", d, **kwargs)
        return apis.CreateKeyResponseSchema().loads(resp)

    def decrypt(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """Decrypt - 您可以使用此操作解密使用对称加密 KMS 密钥或非对称加密 KMS 密钥加密的密文。当 KMS 密钥为非对称密钥时，您必须指定用于加密密文的 KMS 密钥和加密算法。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **CiphertextBlob** (str) - (Required) 待解密密文。
        - **EncryptionAlgorithm** (str) - 解密算法。可选值：SYMMETRIC_DEFAULT、RSAES_OAEP_SHA_1、RSAES_OAEP_SHA_256；非对称密钥解密时必填或使用默认 RSAES_OAEP_SHA_256。
        - **EncryptionContext** (str) - 加密上下文，JSON Object。
        - **KeyId** (str) - 主密钥 KeyId；对称密钥可空，从 CiphertextBlob 自动识别；非对称必填。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**

        - **EncryptionAlgorithm** (str) - 实际使用的解密算法。取值：SYMMETRIC_DEFAULT、RSAES_OAEP_SHA_1、RSAES_OAEP_SHA_256。
        - **KeyId** (str) - 密钥资源长 ID。
        - **Plaintext** (str) - 解密后的明文，Base64 编码。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DecryptRequestSchema().dumps(d)

        resp = self.invoke("Decrypt", d, **kwargs)
        return apis.DecryptResponseSchema().loads(resp)

    def delete_alias(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """DeleteAlias - 删除密钥别名。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **AliasName** (str) - (Required) 密钥别名，格式为 alias/name。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DeleteAliasRequestSchema().dumps(d)

        resp = self.invoke("DeleteAlias", d, **kwargs)
        return apis.DeleteAliasResponseSchema().loads(resp)

    def describe_key(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """DescribeKey - 查看指定密钥的元数据。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥资源长 ID、ARN 或别名。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**

        - **KeyMetadata** (dict) - 见 **KeyMetadata** 模型定义

        **Response Model**

        **KeyMetadata**
        - **CreationDate** (int) - 创建时间，Unix 时间戳。
        - **DeletionDate** (int) - 计划删除时间，Unix 时间戳。
        - **DeletionProtection** (str) - 是否开启删除保护。取值：true、false。
        - **Description** (str) - 密钥描述。
        - **Enabled** (str) - 是否启用。取值：true、false。
        - **KeyId** (str) - 密钥资源长 ID。
        - **KeySpec** (str) - 密钥规格。取值：SYMMETRIC_DEFAULT、RSA_2048、RSA_3072、RSA_4096、ECC_NIST_P256、ECC_NIST_P384、ECC_NIST_P521、HMAC_256、HMAC_384、HMAC_512。
        - **KeyState** (str) - 密钥对外状态。取值：Enabled、Disabled、PendingDeletion、PendingImport、Unavailable。
        - **KeyUsage** (list) - 密钥用途。取值：ENCRYPT_DECRYPT、SIGN_VERIFY、GENERATE_VERIFY_MAC、KEY_AGREEMENT。
        - **KeyVersion** (int) - 当前密钥版本。
        - **Origin** (str) - 密钥材料来源。取值：UCLOUD_KMS、EXTERNAL；当前 CreateKey 仅支持 UCLOUD_KMS。
        - **ResourceId** (str) - 密钥所属的 UKMS 实例资源 ID。


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DescribeKeyRequestSchema().dumps(d)

        resp = self.invoke("DescribeKey", d, **kwargs)
        return apis.DescribeKeyResponseSchema().loads(resp)

    def disable_key(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """DisableKey - 禁用指定密钥。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥 DB 数字 ID。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DisableKeyRequestSchema().dumps(d)

        resp = self.invoke("DisableKey", d, **kwargs)
        return apis.DisableKeyResponseSchema().loads(resp)

    def disable_key_rotation(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DisableKeyRotation - 关闭密钥自动轮转。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥 ID、ARN 或别名。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DisableKeyRotationRequestSchema().dumps(d)

        resp = self.invoke("DisableKeyRotation", d, **kwargs)
        return apis.DisableKeyRotationResponseSchema().loads(resp)

    def enable_key(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """EnableKey - 启用指定密钥。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥 DB 数字 ID。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.EnableKeyRequestSchema().dumps(d)

        resp = self.invoke("EnableKey", d, **kwargs)
        return apis.EnableKeyResponseSchema().loads(resp)

    def enable_key_rotation(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """EnableKeyRotation - 开启对称密钥自动轮转。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥 ID、ARN 或别名。
        - **ResourceId** (str) - (Required) UKMS 实例资源 ID。
        - **RotationPeriodInDays** (int) - 轮转周期，单位天。取值范围 1~2560，默认 365。仅 SYMMETRIC_DEFAULT 且 Origin=UCLOUD_KMS 的密钥支持轮转。

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.EnableKeyRotationRequestSchema().dumps(d)

        resp = self.invoke("EnableKeyRotation", d, **kwargs)
        return apis.EnableKeyRotationResponseSchema().loads(resp)

    def encrypt(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """Encrypt - 使用指定密钥加密明文数据。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥资源长 ID、ARN 或别名。
        - **Plaintext** (str) - (Required) 待加密明文，Base64 编码。
        - **EncryptionAlgorithm** (str) - 加密算法。可选值：SYMMETRIC_DEFAULT、RSAES_OAEP_SHA_1、RSAES_OAEP_SHA_256；对称密钥默认 SYMMETRIC_DEFAULT，RSA 默认 RSAES_OAEP_SHA_256。
        - **EncryptionContext** (str) - 加密上下文，JSON Object。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**

        - **CiphertextBlob** (str) - 加密后的密文。
        - **EncryptionAlgorithm** (str) - 实际使用的加密算法。取值：SYMMETRIC_DEFAULT、RSAES_OAEP_SHA_1、RSAES_OAEP_SHA_256。
        - **KeyId** (str) - 密钥资源长 ID。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.EncryptRequestSchema().dumps(d)

        resp = self.invoke("Encrypt", d, **kwargs)
        return apis.EncryptResponseSchema().loads(resp)

    def generate_data_key(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GenerateDataKey - 生成数据密钥，返回明文和密文。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥资源长 ID、ARN 或别名。
        - **EncryptionContext** (str) - 加密上下文，JSON Object。
        - **KeySpec** (str) - 数据密钥规格。可选值：AES_256、AES_128；默认 AES_256。与 NumberOfBytes 二选一，同时填写时 NumberOfBytes 优先。
        - **NumberOfBytes** (int) - 生成数据密钥的字节长度，取值范围 1~1024。与 KeySpec 二选一，同时填写时本字段优先。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**

        - **CiphertextBlob** (str) - 加密后的数据密钥。
        - **KeyId** (str) - 加密该数据密钥的密钥资源长 ID。
        - **Plaintext** (str) - 数据密钥明文，Base64 编码。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GenerateDataKeyRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("GenerateDataKey", d, **kwargs)
        return apis.GenerateDataKeyResponseSchema().loads(resp)

    def generate_data_key_pair(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GenerateDataKeyPair - 创建数据密钥对

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **KeyId** (str) - (Required) 密钥ID
        - **KeyPairSpec** (str) - (Required) 密钥类型
        - **EncryptionContext** (str) - 加密上下文

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GenerateDataKeyPairRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("GenerateDataKeyPair", d, **kwargs)
        return apis.GenerateDataKeyPairResponseSchema().loads(resp)

    def generate_data_key_pair_without_plaintext(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GenerateDataKeyPairWithoutPlaintext - 创建数据密钥对（无明文返回）

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **KeyId** (str) - (Required) 密钥ID

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GenerateDataKeyPairWithoutPlaintextRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("GenerateDataKeyPairWithoutPlaintext", d, **kwargs)
        return apis.GenerateDataKeyPairWithoutPlaintextResponseSchema().loads(
            resp
        )

    def generate_data_key_without_plaintext(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GenerateDataKeyWithoutPlaintext - 创建数据密钥（无明文），仅返回加密后的数据密钥。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥资源长 ID、ARN 或别名。
        - **EncryptionContext** (str) - 加密上下文，JSON Object。
        - **KeySpec** (str) - 数据密钥规格。可选值：AES_256、AES_128；默认 AES_256。与 NumberOfBytes 二选一，同时填写时 NumberOfBytes 优先。
        - **NumberOfBytes** (int) - 生成数据密钥的字节长度，取值范围 1~1024。与 KeySpec 二选一，同时填写时本字段优先。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**

        - **CiphertextBlob** (str) - 加密后的数据密钥。
        - **KeyId** (str) - 加密该数据密钥的密钥资源长 ID。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GenerateDataKeyWithoutPlaintextRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("GenerateDataKeyWithoutPlaintext", d, **kwargs)
        return apis.GenerateDataKeyWithoutPlaintextResponseSchema().loads(resp)

    def generate_mac(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """GenerateMac - 使用HMAC密钥管理服务（KMS）密钥和该密钥支持的MAC算法，为消息生成基于哈希的消息认证码（HMAC）。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **KeyId** (str) - (Required) 密钥ID
        - **MacAlgorithm** (str) - (Required) 用于生成消息认证码的 MAC 算法。
        - **MacMessage** (str) - (Required) 待哈希的消息。

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GenerateMacRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("GenerateMac", d, **kwargs)
        return apis.GenerateMacResponseSchema().loads(resp)

    def generate_random(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GenerateRandom - 生成随机数

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **NumberOfBytes** (int) - (Required) 随机数长度，最大1024

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GenerateRandomRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("GenerateRandom", d, **kwargs)
        return apis.GenerateRandomResponseSchema().loads(resp)

    def get_key_rotation_status(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetKeyRotationStatus - 查询密钥自动轮转状态。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥资源长 ID、ARN 或别名。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**

        - **KeyId** (str) - 密钥资源长 ID。
        - **KeyRotationEnabled** (bool) - 是否开启自动轮转。取值：true、false。
        - **NextRotationDate** (int) - 下次轮转时间，Unix 时间戳。
        - **OnDemandRotationStartDate** (int) - 按需轮转开始时间，Unix 时间戳。
        - **RotationPeriodInDays** (int) - 轮转周期，单位天；未开启时返回 0。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GetKeyRotationStatusRequestSchema().dumps(d)

        resp = self.invoke("GetKeyRotationStatus", d, **kwargs)
        return apis.GetKeyRotationStatusResponseSchema().loads(resp)

    def get_public_key(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetPublicKey - 获取非对称密钥的公钥。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥 ID、ARN 或别名。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**

        - **EncryptionAlgorithms** (list) - 支持的加密算法列表。取值范围：RSAES_OAEP_SHA_1、RSAES_OAEP_SHA_256。
        - **KeyId** (str) - 密钥 ID。
        - **KeySpec** (str) - 密钥规格。取值：SYMMETRIC_DEFAULT、RSA_2048、RSA_3072、RSA_4096、ECC_NIST_P256、ECC_NIST_P384、ECC_NIST_P521、HMAC_256、HMAC_384、HMAC_512。
        - **KeyUsage** (list) - 密钥用途。取值：ENCRYPT_DECRYPT、SIGN_VERIFY、GENERATE_VERIFY_MAC、KEY_AGREEMENT。
        - **KmsPublicKey** (str) - PEM 或 DER 编码的公钥。
        - **SigningAlgorithms** (list) - 支持的签名算法列表。取值范围：RSASSA_PSS_SHA_256、RSASSA_PSS_SHA_384、RSASSA_PSS_SHA_512、RSASSA_PKCS1_V1_5_SHA_256、RSASSA_PKCS1_V1_5_SHA_384、RSASSA_PKCS1_V1_5_SHA_512、ECDSA_SHA_256、ECDSA_SHA_384、ECDSA_SHA_512。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GetPublicKeyRequestSchema().dumps(d)

        resp = self.invoke("GetPublicKey", d, **kwargs)
        return apis.GetPublicKeyResponseSchema().loads(resp)

    def list_aliases(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """ListAliases - 获取 UKMS 实例下的别名列表。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - 可选：筛选指定密钥资源长 ID 的别名。
        - **Limit** (int) - 返回数据长度。
        - **Offset** (int) - 列表起始位置偏移量。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**

        - **Aliases** (list) - 见 **AliasInfo** 模型定义
        - **TotalCount** (int) - 别名总数。

        **Response Model**

        **AliasInfo**
        - **AliasName** (str) - 别名，含 alias/ 前缀。
        - **CreationDate** (int) - 创建时间，Unix 时间戳。
        - **LastUpdatedDate** (int) - 最后更新时间，Unix 时间戳。
        - **TargetKeyId** (str) - 别名指向的密钥资源长 ID。


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListAliasesRequestSchema().dumps(d)

        resp = self.invoke("ListAliases", d, **kwargs)
        return apis.ListAliasesResponseSchema().loads(resp)

    def list_keys(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """ListKeys - 查询用户的主密钥信息列表。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **Alias** (str) - 按密钥 ID 或别名模糊过滤。
        - **Limit** (int) - 返回数据长度。最大1000
        - **Offset** (int) - 列表起始位置偏移量。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**

        - **Data** (list) - 见 **DEK** 模型定义
        - **TotalCount** (int) - 符合条件的总数，不同于 Limit。

        **Response Model**

        **DEK**
        - **CreatedTime** (int) - 创建时间，Unix 时间戳。
        - **Description** (str) - 密钥描述。
        - **KeyId** (str) - 对外主密钥 ID（ukms_key_info.key_id）。
        - **KeyRotationEnabled** (bool) - 是否已开启自动轮转；未配置或已关闭均为 false
        - **KeySpec** (str) - 密钥规格。取值：SYMMETRIC_DEFAULT、RSA_2048、RSA_3072、RSA_4096、ECC_NIST_P256、ECC_NIST_P384、ECC_NIST_P521、HMAC_256、HMAC_384、HMAC_512。
        - **KeyUsage** (list) - 按 KeySpec 派生的密钥用途。取值：ENCRYPT_DECRYPT、SIGN_VERIFY、GENERATE_VERIFY_MAC、KEY_AGREEMENT。
        - **NextRotationDate** (int) - 下次自动轮转时间（Unix 时间戳，秒）；仅在已开启自动轮转时返回。
        - **Origin** (str) - 密钥来源，由 Origin 派生。取值：ucloud、import。当前 CreateKey 仅支持 ucloud。
        - **PlanDeleteTime** (int) - 计划删除时间，Unix 时间戳。
        - **ResourceId** (str) - 密钥所属的 UKMS 实例资源 ID。
        - **RotationPeriodInDays** (int) - 自动轮转周期(天)；未开启时为 0
        - **Status** (str) - 数据库密钥状态。常见取值：Active、Deactivated、PendingDeletion。
        - **UpdateTime** (int) - 更新时间，Unix 时间戳。


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListKeysRequestSchema().dumps(d)

        resp = self.invoke("ListKeys", d, **kwargs)
        return apis.ListKeysResponseSchema().loads(resp)

    def list_schedule_deletion_keys(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListScheduleDeletionKeys - 获取计划删除密钥列表，调用ScheduleKeyDeletion命令后进入此列表， 默认30天后正式删除。正式删除前可调用CancelScheduleKeyDeletion恢复

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list.html>`_
        - **Limit** (int) - 输出列表数量，默认返回200个
        - **Offset** (int) - 输出列表起始位置，默认从0开始
        - **OrderBy** (str) - 列表排序方式, 可选项: "-created_time", "created_time","plan_delete_time","-plan_delete_time";默认按-plan_delete_time 计划删除时间升序返回

        **Response**

        - **Objects** (list) - 见 **CMK** 模型定义
        - **RequestUuid** (str) - 请求唯一标识符
        - **Status** (str) - 操作结果
        - **TotalCount** (int) - 符合条件的总数, 不同于Limit

        **Response Model**

        **CMK**
        - **Alias** (str) - 别名，与CMK一一对应
        - **CreatedTime** (int) - 创建时间 时间戳
        - **Description** (str) - 对密钥的描述说明
        - **Enabled** (bool) - 是否启用
        - **KeyId** (str) - CMK 的唯一标识符
        - **LastModifiedTime** (int) - 最后修改时间 时间戳
        - **PlanDeleteTime** (int) - 计划删除时间 时间戳
        - **Type** (str) - 密钥类型，仅支持UCloudManagedKeys、CustomerManagedKeys。默认值CustomerManagedKeys


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.ListScheduleDeletionKeysRequestSchema().dumps(d)

        resp = self.invoke("ListScheduleDeletionKeys", d, **kwargs)
        return apis.ListScheduleDeletionKeysResponseSchema().loads(resp)

    def rotate_key_on_demand(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """RotateKeyOnDemand - 立即触发一次密钥轮转。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥 ID、ARN 或别名。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**

        - **KeyId** (str) - 密钥 ID。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.RotateKeyOnDemandRequestSchema().dumps(d)

        resp = self.invoke("RotateKeyOnDemand", d, **kwargs)
        return apis.RotateKeyOnDemandResponseSchema().loads(resp)

    def schedule_key_deletion(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ScheduleKeyDeletion - 计划删除指定密钥。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥 DB 数字 ID。
        - **DeleteDay** (int) - 删除等待天数，取值范围为 7~30 天；未填写时默认为 30 天。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ScheduleKeyDeletionRequestSchema().dumps(d)

        resp = self.invoke("ScheduleKeyDeletion", d, **kwargs)
        return apis.ScheduleKeyDeletionResponseSchema().loads(resp)

    def sign(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """Sign - 使用非对称密钥对消息或消息摘要签名。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥资源长 ID、ARN 或别名。
        - **SigningAlgorithm** (str) - (Required) 签名算法。可选值：RSASSA_PSS_SHA_256、RSASSA_PSS_SHA_384、RSASSA_PSS_SHA_512、RSASSA_PKCS1_V1_5_SHA_256、RSASSA_PKCS1_V1_5_SHA_384、RSASSA_PKCS1_V1_5_SHA_512、ECDSA_SHA_256、ECDSA_SHA_384、ECDSA_SHA_512；须与密钥 KeySpec 匹配。
        - **SigningMessage** (str) - (Required) 待签名消息，Base64 编码，最大 4096 字节。
        - **MessageType** (str) - 消息类型。可选值：RAW、DIGEST；默认 RAW。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**

        - **KeyId** (str) - 密钥资源长 ID。
        - **SignatureResult** (str) - 签名结果，Base64 编码。
        - **SigningAlgorithm** (str) - 实际使用的签名算法。取值：RSASSA_PSS_SHA_256、RSASSA_PSS_SHA_384、RSASSA_PSS_SHA_512、RSASSA_PKCS1_V1_5_SHA_256、RSASSA_PKCS1_V1_5_SHA_384、RSASSA_PKCS1_V1_5_SHA_512、ECDSA_SHA_256、ECDSA_SHA_384、ECDSA_SHA_512。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.SignRequestSchema().dumps(d)

        resp = self.invoke("Sign", d, **kwargs)
        return apis.SignResponseSchema().loads(resp)

    def update_alias(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """UpdateAlias - 将别名更新到另一个密钥。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **AliasName** (str) - (Required) 密钥别名，格式为 alias/name。
        - **KeyId** (str) - (Required) 密钥 ID、ARN 或别名。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.UpdateAliasRequestSchema().dumps(d)

        resp = self.invoke("UpdateAlias", d, **kwargs)
        return apis.UpdateAliasResponseSchema().loads(resp)

    def update_key_description(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """UpdateKeyDescription - 更新指定密钥的描述信息。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **Description** (str) - (Required) 新的密钥描述，最多 8192 字符；空字符串表示清空描述。
        - **KeyId** (str) - (Required) 密钥资源长 ID、ARN 或别名。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**

        - **RequestUuid** (str) - 此次请求唯一标识符。
        - **Status** (str) - 返回状态。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.UpdateKeyDescriptionRequestSchema().dumps(d)

        resp = self.invoke("UpdateKeyDescription", d, **kwargs)
        return apis.UpdateKeyDescriptionResponseSchema().loads(resp)

    def verify(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """Verify - 使用非对称密钥验证签名。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子账号必须填写。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **KeyId** (str) - (Required) 密钥 ID、ARN 或别名。
        - **SignatureResult** (str) - (Required) 待验证的签名，Base64 编码。
        - **SigningAlgorithm** (str) - (Required) 签名时使用的算法。可选值：RSASSA_PSS_SHA_256、RSASSA_PSS_SHA_384、RSASSA_PSS_SHA_512、RSASSA_PKCS1_V1_5_SHA_256、RSASSA_PKCS1_V1_5_SHA_384、RSASSA_PKCS1_V1_5_SHA_512、ECDSA_SHA_256、ECDSA_SHA_384、ECDSA_SHA_512；须与密钥 KeySpec 匹配。
        - **SigningMessage** (str) - (Required) 待验签的消息或消息摘要，Base64 编码。
        - **MessageType** (str) - 消息类型。可选值：RAW、DIGEST；默认 RAW。
        - **ResourceId** (str) - UKMS 实例资源 ID。

        **Response**

        - **KeyId** (str) - 密钥 ID。
        - **SignatureValid** (bool) - 签名是否有效。
        - **SigningAlgorithm** (str) - 使用的签名算法。取值：RSASSA_PSS_SHA_256、RSASSA_PSS_SHA_384、RSASSA_PSS_SHA_512、RSASSA_PKCS1_V1_5_SHA_256、RSASSA_PKCS1_V1_5_SHA_384、RSASSA_PKCS1_V1_5_SHA_512、ECDSA_SHA_256、ECDSA_SHA_384、ECDSA_SHA_512。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.VerifyRequestSchema().dumps(d)

        resp = self.invoke("Verify", d, **kwargs)
        return apis.VerifyResponseSchema().loads(resp)

    def verify_mac(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """VerifyMac - 验证签名

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **KeyId** (str) - (Required) 密钥ID
        - **Mac** (str) - (Required) 要验证的 HMAC。请输入由 GenerateMac 操作生成的 HMAC，前提是您指定的消息、HMAC KMS 密钥和 MAC 算法与此请求中指定的值相同。
        - **MacAlgorithm** (str) - (Required) 验证过程中将使用的 MAC 算法。请输入与计算 HMAC 时相同的 MAC 算法。此算法必须受 KeyId 参数标识的 HMAC KMS 密钥支持。
        - **MacMessage** (str) - (Required) 用于验证的消息。请输入与生成 HMAC 时所用消息相同的消息。

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.VerifyMacRequestSchema().dumps(d)

        resp = self.invoke("VerifyMac", d, **kwargs)
        return apis.VerifyMacResponseSchema().loads(resp)
