""" Code is generated by ucloud-model, DO NOT EDIT IT. """

import typing


from ucloud.core.client import Client
from ucloud.services.ses.schemas import apis


class SESClient(Client):
    def __init__(
        self, config: dict, transport=None, middleware=None, logger=None
    ):
        super(SESClient, self).__init__(config, transport, middleware, logger)

    def send_ses_email(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """SendSESEmail - 直接发送邮件，无需创建发送任务和模版，支持 HTML/纯文本内容、主题、抄送/密送、附件和邮件头，单次最多 100 个收件人。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **From** (str) - (Required) 发件人邮箱，配置地址：https://console.ucloud.cn/ses/email-config?tab=sender-list
        - **Subject** (str) - (Required) 邮件主题（长度 1~200 字符）
        - **To** (list) - (Required) 收件人
        - **Attachments** (list) - 见 **SendSESEmailParamAttachments** 模型定义
        - **Bcc** (list) - 密送（抄送+密送总数量不能超过100）
        - **Cc** (list) - 抄送（抄送+密送总数量不能超过100）
        - **FromName** (str) - 发件人名称，不传或为空时，自动回落到该发件人在控制台配置的别名（Alias）
        - **Headers** (list) - 见 **SendSESEmailParamHeaders** 模型定义
        - **Html** (str) - HTML 内容 （与Text二选一，长度上限 10,485,760个字符）
        - **PreHeader** (str) - 邮件预览文本
        - **Text** (str) - 纯文本内容 （与Html二选一，长度上限 10,485,760个字符）
        - **Zone** (str) - 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **FailContent** (list) - 见 **FailedTargetEmail** 模型定义
        - **SessionNo** (str) - 本次发送任务的唯一标识
        - **SuccessCount** (int) - 发送成功数

        **Request Model**

        **SendSESEmailParamHeaders**
        - **Name** (str) - 邮件头名称，最多 20 个且仅允许字母、数字和 -（正则 ^[A-Za-z0-9-]+$）。禁止使用保留名称：From/To/Cc/Bcc/Subject/Reply-To/Content-Type/Mime-Version，以及 X-SES- 前缀（均不区分大小写）。
        - **Value** (str) - 邮件头值


        **SendSESEmailParamAttachments**
        - **ContentType** (str) - MIME 类型，如： application/pdf
        - **Data** (str) - 附件内容（Base64 编码），单次请求最多 10 个附件；总大小不超过 10MB
        - **Filename** (str) - 附件文件名


        **Response Model**

        **FailedTargetEmail**
        - **Bcc** (list) - 密送
        - **Cc** (list) - 抄送
        - **EmailAddress** (str) - 收件邮箱
        - **FailureReason** (str) - 失败原因
        - **TemplateVariableParams** (list) - 模版变量,variableName{##}variableValue 格式；仅使用模版发送（SendSESEmailTemplate）时返回，未使用模版发送（SendSESEmail）时不返回该字段
        - **To** (str) - 收件人


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.SendSESEmailRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("SendSESEmail", d, **kwargs)
        return apis.SendSESEmailResponseSchema().loads(resp)

    def send_ses_email_template(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """SendSESEmailTemplate - 通过模版发送邮件，无需创建发送任务。支持自定义主题、摘要、预览文本、抄送/密送、附件和邮件头，单次最多 100 个收件人。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **From** (str) - (Required) 发件人邮箱
        - **Subject** (str) - (Required) 邮件主题
        - **TemplateId** (str) - (Required) 模版 ID，获取：https://console.ucloud.cn/ses/email-config?tab=template
        - **Attachments** (list) - 见 **SendSESEmailTemplateParamAttachments** 模型定义
        - **EmailContent** (list) - 见 **SendSESEmailTemplateParamEmailContent** 模型定义
        - **FromName** (str) - 发件人名称，不传或为空时，自动回落到该发件人在控制台配置的别名（Alias）
        - **Headers** (list) - 见 **SendSESEmailTemplateParamHeaders** 模型定义
        - **PreHeader** (str) - 邮件预览文本
        - **Zone** (str) - 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **FailContent** (list) - 见 **FailedTargetEmail** 模型定义
        - **SessionNo** (str) - 本次发送任务的唯一标识
        - **SuccessCount** (int) - 发送成功数

        **Request Model**

        **SendSESEmailTemplateParamEmailContent**
        - **Bcc** (list) - 密送（抄送+密送总数量不能超过100）
        - **Cc** (list) - 抄送（抄送+密送总数量不能超过100）
        - **TemplateVariableParams** (list) - 模版变量，格式 variableName{##}variableValue （例：{"name{##}Tom", "code{##}1234"}）
        - **To** (str) - 收件人，最多100条


        **SendSESEmailTemplateParamAttachments**
        - **ContentType** (str) - MIME 类型，如： application/pdf
        - **Data** (str) - 附件内容（Base64 编码），单次请求最多 10 个附件；总大小不超过 10MB
        - **Filename** (str) - 附件文件名


        **SendSESEmailTemplateParamHeaders**
        - **Name** (str) - 邮件头名称，最多 20 个且仅允许字母、数字和 -（正则 ^[A-Za-z0-9-]+$）。禁止使用保留名称：From/To/Cc/Bcc/Subject/Reply-To/Content-Type/Mime-Version，以及 X-SES- 前缀（均不区分大小写）。
        - **Value** (str) - 邮件头值


        **Response Model**

        **FailedTargetEmail**
        - **Bcc** (list) - 密送
        - **Cc** (list) - 抄送
        - **EmailAddress** (str) - 收件邮箱
        - **FailureReason** (str) - 失败原因
        - **TemplateVariableParams** (list) - 模版变量
        - **To** (str) - 收件人


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.SendSESEmailTemplateRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("SendSESEmailTemplate", d, **kwargs)
        return apis.SendSESEmailTemplateResponseSchema().loads(resp)
