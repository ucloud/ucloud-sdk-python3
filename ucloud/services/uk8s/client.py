""" Code is generated by ucloud-model, DO NOT EDIT IT. """

import typing


from ucloud.core.client import Client
from ucloud.services.uk8s.schemas import apis


class UK8SClient(Client):
    def __init__(
        self, config: dict, transport=None, middleware=None, logger=None
    ):
        super(UK8SClient, self).__init__(config, transport, middleware, logger)

    def add_uk8s_existing_uhost(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """AddUK8SExistingUHost - 将预先创建好的云主机加入到UK8S集群，需要注意的是，该云主机依然会执行重装系统的操作。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list.html>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist.html>`_
        - **ClusterId** (str) - (Required) UK8S集群ID。 可从UK8S控制台获取。
        - **Password** (str) - (Required) Node节点密码。请遵照 `字段规范 <https://docs.ucloud.cn/api/uhost-api/specification>`_ 设定密码。密码需使用base64进行编码，如下：# echo -n Password1 | base64
        - **UHostId** (str) - (Required) 云主机Id，为了保证节点正常运行，该主机配置不得低于2C4G。
        - **DisableSchedule** (bool) - 用于标示添加完节点后是否将节点临时禁用. 传入 "true" 表示禁用,传入其它或不传表示不禁用
        - **ImageId** (str) - 镜像 Id，不填时后台程序会自动选用一个可用的镜像 Id，支持用户自定义镜像，自定义镜像必须基于基础镜像制作。
        - **InitScript** (str) - 用户自定义Shell脚本。与UserData的区别在于InitScript在节点初始化完毕后才执行，UserData则是云主机初始化时执行。
        - **Labels** (str) - Node节点标签。key=value形式,多组用”,“隔开，最多5组。 如env=pro,type=game
        - **MaxPods** (int) - 默认110，生产环境建议小于等于110。
        - **SubnetId** (str) - 该云主机所属子网Id。
        - **UserData** (str) - 用户自定义数据。当镜像支持Cloud-init Feature时可填写此字段。注意：1、总数据量大小不超过 16K；2、使用base64编码。
        - **Zone** (str) - 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist.html>`_

        **Response**

        - **Message** (str) - 返回错误消息，当 RetCode 非 0 时提供详细的描述信息。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.AddUK8SExistingUHostRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("AddUK8SExistingUHost", d, **kwargs)
        return apis.AddUK8SExistingUHostResponseSchema().loads(resp)

    def add_uk8s_node_group(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """AddUK8SNodeGroup - 添加UK8S节点池

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterId** (str) - (Required) 集群ID
        - **NodeGroupName** (str) - (Required) 节点池名字
        - **BootDiskSize** (int) - 系统盘大小，单位GB。默认40。范围：[40, 500]。注意SSD本地盘无法调整。
        - **BootDiskType** (str) - 磁盘类型
        - **CPU** (int) - GPU卡核心数。仅GPU机型支持此字段（可选范围与MachineType+GpuType相关）
        - **ChargeType** (str) - 计费模式
        - **DataDiskSize** (int) - 数据磁盘大小
        - **DataDiskType** (str) - 磁盘类型
        - **GPU** (int) - GPU卡核心数
        - **GpuType** (str) - GPU类型
        - **ImageId** (str) - 镜像ID
        - **MachineType** (str) - 云主机机型。枚举值["N", "C", "G", "O", "OS"]。参考 `云主机机型说明 <https://docs.ucloud.cn/api/uhost-api/uhost_type>`_ 。
        - **Mem** (int) - 内存大小。单位：MB
        - **MinimalCpuPlatform** (str) - 最低cpu平台，枚举值["Intel/Auto", "Intel/IvyBridge", "Intel/Haswell", "Intel/Broadwell", "Intel/Skylake", "Intel/Cascadelake"；"Intel/CascadelakeR"; “Amd/Epyc2”,"Amd/Auto"],默认值是"Intel/Auto"
        - **SubnetId** (str) - 子网 ID。默认为集群创建时填写的子网ID，也可以填写集群同VPC内的子网ID。
        - **Tag** (str) - 业务组
        - **Zone** (str) - 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Message** (str) - 返回错误消息，当 RetCode 非 0 时提供详细的描述信息。
        - **NodeGroupId** (str) - 节点池ID

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.AddUK8SNodeGroupRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("AddUK8SNodeGroup", d, **kwargs)
        return apis.AddUK8SNodeGroupResponseSchema().loads(resp)

    def add_uk8s_phost_node(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """AddUK8SPHostNode - 为UK8S集群添加一台或多台物理云主机类型的节点。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list.html>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist.html>`_
        - **ChargeType** (str) - (Required) 计费模式。枚举值为： \\ > Year，按年付费； \\ > Month，按月付费；\\ 默认为月付
        - **ClusterId** (str) - (Required) UK8S集群ID。 可从UK8S控制台获取。
        - **Count** (int) - (Required) 最大创建Node节点数量，取值范围是[1,10]。
        - **Password** (str) - (Required) Node节点密码。请遵照 `字段规范 <https://docs.ucloud.cn/api/uhost-api/specification>`_ 设定密码。密码需使用base64进行编码，如下：# echo -n Password1 | base64
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist.html>`_
        - **DisableSchedule** (bool) - 用于标示添加完节点后是否将节点临时禁用. 传入 "true" 表示禁用,传入其它或不传表示不禁用
        - **ImageId** (str) - 镜像 Id，不填时后台程序会自动选用一个可用的镜像 Id，支持用户自定义镜像，自定义镜像必须基于基础镜像制作。
        - **InitScript** (str) - 用户自定义Shell脚本。与UserData的区别在于InitScript在节点初始化完毕后才执行。
        - **Labels** (str) - Node节点标签。key=value形式,多组用”,“隔开，最多5组。 如env=pro,type=game
        - **MaxPods** (int) - 默认110，生产环境建议小于等于110。
        - **NIC** (str) - 网络环境，可选千兆：1G ，万兆：10G， 默认1G。
        - **Quantity** (int) - 购买时长。默认: 1。月付时，此参数传0，代表了购买至月末。
        - **Raid** (str) - Raid配置，默认Raid10 支持:Raid0、Raid1、Raid5、Raid10，NoRaid
        - **SubnetId** (str) - 子网 ID。默认为集群创建时填写的子网ID，也可以填写集群同VPC内的子网ID。
        - **Type** (str) - 物理机类型，默认为：db-2(基础型-SAS-V3)

        **Response**

        - **Message** (str) - 返回错误消息，当 RetCode 非 0 时提供详细的描述信息。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.AddUK8SPHostNodeRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("AddUK8SPHostNode", d, **kwargs)
        return apis.AddUK8SPHostNodeResponseSchema().loads(resp)

    def add_uk8s_uhost_node(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """AddUK8SUHostNode - 为UK8S集群添加一台Node节点，机型类型为云主机

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **CPU** (int) - (Required) 虚拟CPU核数。可选参数：2-64（具体机型与CPU的对应关系参照控制台）。默认值: 4。
        - **ChargeType** (str) - (Required) 计费模式。枚举值为： \\ > Year，按年付费； \\ > Month，按月付费；\\ > Dynamic，按小时预付费 \\ > Postpay，按小时后付费（支持关机不收费，目前仅部分可用区支持，请联系您的客户经理） \\ 默认为月付
        - **ClusterId** (str) - (Required) UK8S集群ID。 可从UK8S控制台获取。
        - **Count** (int) - (Required) 创建Node节点数量，取值范围是[1,50]。
        - **Mem** (int) - (Required) 内存大小。单位：MB。范围 ：[4096, 262144]，取值为1024的倍数（可选范围参考控制台）。默认值：8192
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **BootDiskSize** (int) - 系统盘大小，单位GB。默认40。范围：[40, 500]。注意SSD本地盘无法调整。
        - **BootDiskType** (str) - 磁盘类型。请参考 `磁盘类型 <https://docs.ucloud.cn/api/uhost-api/disk_type>`_ 。默认为SSD云盘
        - **DataDiskSize** (int) - 数据磁盘大小，单位GB。默认0。范围 ：[20, 1000]
        - **DataDiskType** (str) - 磁盘类型。请参考 `磁盘类型 <https://docs.ucloud.cn/api/uhost-api/disk_type>`_ 。默认为SSD云盘
        - **DisableSchedule** (bool) - 用于标示添加完节点后是否将节点临时禁用. 传入 "true" 表示禁用,传入其它或不传表示不禁用
        - **GPU** (int) - GPU卡核心数。仅GPU机型支持此字段（可选范围与MachineType+GpuType相关）
        - **GpuType** (str) - GPU类型，枚举值["K80", "P40", "V100",]，MachineType为G时必填
        - **ImageId** (str) - 镜像 Id，不填时后台程序会自动选用一个可用的镜像 Id，支持用户自定义镜像，自定义镜像必须基于基础镜像制作。
        - **InitScript** (str) - 用户自定义Shell脚本。与UserData的区别在于InitScript在节点初始化完毕后才执行，UserData则是云主机初始化时执行。
        - **IsolationGroup** (str) - 硬件隔离组id。可通过DescribeIsolationGroup获取。
        - **KubeletConfiguration** (dict) - 见 **AddUK8SUHostNodeParamKubeletConfiguration** 模型定义
        - **Labels** (str) - Node节点标签。key=value形式,多组用”,“隔开，最多5组。 如env=pro,type=game
        - **MachineType** (str) - 云主机机型。枚举值["N", "C", "G", "O", "OS"]。参考 `云主机机型说明 <https://docs.ucloud.cn/api/uhost-api/uhost_type>`_ 。
        - **MaxPods** (int) - 默认110，生产环境建议小于等于110。
        - **MinimalCpuPlatform** (str) - 最低cpu平台，枚举值["Intel/Auto", "Intel/IvyBridge", "Intel/Haswell", "Intel/Broadwell", "Intel/Skylake", "Intel/Cascadelake"；"Intel/CascadelakeR"; “Amd/Epyc2”,"Amd/Auto"],默认值是"Intel/Auto"
        - **NamePrefix** (str) - 自定义主机名前缀。完整的自定义主机名为{NamePrefix}-{NodeIP}。
        - **NetCapability** (str) - 网络增强特性。枚举值：Normal，不开启; Super，开启网络增强1.0； Ultra，开启网络增强2.0；Extreme，开启网络增强3.0; Infiniband, 开启网络增强4.0（详情参考主机官网文档）
        - **NetworkInterface** (list) - 见 **AddUK8SUHostNodeParamNetworkInterface** 模型定义
        - **NodeGroupId** (str) - 节点池id
        - **Password** (str) - Node节点密码。请遵照 `字段规范 <https://docs.ucloud.cn/api/uhost-api/specification>`_ 设定密码。密码需使用base64进行编码，如下：# echo -n Password1 | base64
        - **Quantity** (int) - 购买时长。默认: 1。按小时购买(Dynamic)时无需此参数。 月付时，此参数传0，代表了购买至月末。
        - **SecGroupId** (list) - 见 **AddUK8SUHostNodeParamSecGroupId** 模型定义
        - **SecurityGroupId** (str) - 防火墙ID，默认：Web推荐防火墙。如何查询SecurityGroupId请参见  `DescribeFirewall <https://docs.ucloud.cn/api/uk8s-api/api/unet-api/describe_firewall.html>`_ 。
        - **SecurityMode** (str) - 主机安全模式。Firewall：防火墙；SecGroup：安全组；默认值：Firewall。
        - **SubnetId** (str) - 子网 ID。默认为集群创建时填写的子网ID，也可以填写集群同VPC内的子网ID。
        - **Tag** (str) - 业务组
        - **Taints** (str) - Node节点污点，形式为key=value:effect，多组taints用”,“隔开,最多支持五组。
        - **UHostFamily** (str) - 主机规格族
        - **UNIFeature** (bool) - 弹性网卡特性。开启了弹性网卡权限位，此特性才生效，默认 false 未开启，true 开启。
        - **UserData** (str) - 用户自定义数据。当镜像支持Cloud-init Feature时可填写此字段。注意：1、总数据量大小不超过 16K；2、使用base64编码。
        - **UserLabels** (list) - 见 **AddUK8SUHostNodeParamUserLabels** 模型定义

        **Response**

        - **Message** (str) - 返回错误消息，当 RetCode 非 0 时提供详细的描述信息。
        - **NodeIds** (list) - Node实例Id集合

        **Request Model**

        **AddUK8SUHostNodeParamNetworkInterfaceEIP**
        - **Bandwidth** (int) - 【若绑定EIP，此参数必填】弹性IP的外网带宽, 单位为Mbps. 共享带宽模式下非必传, 非共享带宽模式必须指定非0Mbps带宽. 各地域非共享带宽的带宽范围如下： 流量计费[1-300]，带宽计费[1-800]
        - **CouponId** (str) - 当前EIP代金券id。请通过DescribeCoupon接口查询，或登录用户中心查看。
        - **OperatorName** (str) - 【若绑定EIP，此参数必填】弹性IP的线路。枚举值: 国际: International，BGP: Bgp。各地域允许的线路参数如下: cn-sh1: Bgp cn-sh2: Bgp cn-gd: Bgp cn-bj1: Bgp cn-bj2: Bgp hk: International us-ca: International th-bkk: International kr-seoul:International us-ws:International ge-fra:International sg:International tw-kh:International.其他海外线路均为 International
        - **PayMode** (str) - 弹性IP的计费模式. 枚举值: "Traffic", 流量计费; "Bandwidth", 带宽计费; "ShareBandwidth",共享带宽模式. "Free":免费带宽模式,默认为 "Bandwidth"
        - **ShareBandwidthId** (str) - 绑定的共享带宽Id，仅当PayMode为ShareBandwidth时有效


        **AddUK8SUHostNodeParamKubeletConfiguration**
        - **ContainerLogMaxFiles** (str) - 全量KubeletConfiguration.XXX定义参考AddUK8SNodeGroup接口: https://uxiao.ucloudadmin.com/#/api-manager/api/detail/UK8S/AddUK8SNodeGroup


        **AddUK8SUHostNodeParamNetworkInterface**
        - **EIP** (dict) - 见 **AddUK8SUHostNodeParamNetworkInterfaceEIP** 模型定义


        **AddUK8SUHostNodeParamUserLabels**
        - **Key** (str) - UK8S用户资源标签的键值
        - **Value** (str) - UK8S用户资源标签的值


        **AddUK8SUHostNodeParamSecGroupId**
        - **Id** (str) - 安全组 ID。至多可以同时绑定5个安全组。
        - **Name** (str) - 安全组名称。
        - **Priority** (str) - 安全组优先级。取值范围[1, 5]


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.AddUK8SUHostNodeRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("AddUK8SUHostNode", d, **kwargs)
        return apis.AddUK8SUHostNodeResponseSchema().loads(resp)

    def create_uk8s_cluster_v2(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CreateUK8SClusterV2 - 创建UK8S集群

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterName** (str) - (Required) 集群名称
        - **MasterCPU** (int) - (Required) Master节点的虚拟CPU核数。可选参数：2-64（具体机型与CPU的对应关系参照控制台）。
        - **MasterMachineType** (str) - (Required) Master节点的云主机机型（V2.0），如["N", "C", "O", "OS"]，具体请参照云主机机型。
        - **MasterMem** (int) - (Required) Master节点的内存大小。单位：MB。范围 ：[4096, 262144]，取值为1024的倍数（可选范围参考控制台）。
        - **Password** (str) - (Required) 集群节点密码，包括Master和Node。密码需包含最少一个大写字母，请使用base64进行编码，举例如下：# echo -n Password1 | base64
        - **ServiceCIDR** (str) - (Required) Service 网段，用于分配ClusterIP，如172.17.0.0/16。该网段不能与集群所属VPC网段重叠。
        - **SubnetId** (str) - (Required) 集群Node及Pod所属子网
        - **VPCId** (str) - (Required) 集群Node及Pod所属VPC
        - **ChargeType** (str) - 集群所有节点的付费模式。枚举值为： Year，按年付费； Month，按月付费； Dynamic，按小时付费（需开启权限），默认按月。
        - **ClusterDomain** (str) - 创建集群的时候定义clusterdomain
        - **ExternalApiServer** (str) - 是否允许外网访问apiserver，开启：Yes 不开启：No。默认为No。
        - **ForwardSrcIPMethod** (str) - LbClass为nlb的时候支持的源ip转发模式，目前只支持Toa,为空则不开源ip功能 枚举："",Toa
        - **ImageId** (str) - Master节点和Node节点的镜像 ID，不填则随机选择可用的基础镜像。支持用户自定义镜像。
        - **InitScript** (str) - 用户自定义脚本，与UserData不同，自定义脚本将在集群安装完毕后执行。注意：1、总数据量大小不超多16K；2、使用base64编码。
        - **K8sVersion** (str) - k8s集群的版本，版本信息请参考UK8S集群创建页，不指定的话默认为当前支持的最高版本。
        - **KubeProxy** (dict) - 见 **CreateUK8SClusterV2ParamKubeProxy** 模型定义
        - **LbClass** (str) - master lb 类型默认ulb，可选ulb nlb
        - **Master** (list) - 见 **CreateUK8SClusterV2ParamMaster** 模型定义
        - **MasterBootDiskSize** (int) - Master节点系统盘大小，单位GB，默认为40。范围：[40, 500]。注意SSD本地盘无法调整。
        - **MasterBootDiskType** (str) - Master节点系统盘类型。请参考 `磁盘类型 <https://docs.ucloud.cn/api/uhost-api/disk_type>`_ 。默认为SSD云盘
        - **MasterDataDiskSize** (int) - Master节点的数据盘大小，单位GB，默认为0。范围 ：[20, 1000]
        - **MasterDataDiskType** (str) - Master节点数据盘类型。请参考 `磁盘类型 <https://docs.ucloud.cn/api/uhost-api/disk_type>`_ 。默认为SSD云盘
        - **MasterImageId** (str) - Master节点的镜像 ID，不填则使用ImageId参数。支持用户自定义镜像。
        - **MasterIsolationGroup** (str) - 【无效，已删除】当前将自动为Master节点创建隔离组，确保Master节点归属于不同物理机。
        - **MasterMinimalCpuPlatform** (str) - Master节点的最低cpu平台，不选则随机。枚举值["Intel/Auto", "Intel/IvyBridge", "Intel/Haswell", "Intel/Broadwell", "Intel/Skylake", "Intel/Cascadelake"。
        - **Nodes** (list) - 见 **CreateUK8SClusterV2ParamNodes** 模型定义
        - **Quantity** (int) - 购买时长。默认为1。按小时购买(Dynamic)时无需此参数。 月付时，此参数传0，代表了购买至月末。
        - **Tag** (str) - 业务组
        - **UserData** (str) - 用户自定义数据。注意：1、总数据量大小不超多16K；2、使用base64编码。
        - **UserLabels** (str) - UK8S用户标签，key=value形式,多组用”,“隔开，最多5组。 如env=pro,type=game

        **Response**

        - **ClusterId** (str) - 集群ID

        **Request Model**

        **CreateUK8SClusterV2ParamKubeProxy**
        - **Mode** (str) - 集群kube-proxy模式。支持iptables和ipvs，默认为iptables。


        **CreateUK8SClusterV2ParamNodesNetworkInterfaceEIP**
        - **Bandwidth** (int) - 【若绑定EIP，此参数必填】弹性IP的外网带宽, 单位为Mbps. 共享带宽模式下非必传, 非共享带宽模式必须指定非0Mbps带宽. 各地域非共享带宽的带宽范围如下： 流量计费[1-300]，带宽计费[1-800]
        - **CouponId** (str) - 当前EIP代金券id。请通过DescribeCoupon接口查询，或登录用户中心查看。
        - **OperatorName** (str) - 【若绑定EIP，此参数必填】弹性IP的线路。枚举值: 国际: International BGP: Bgp 各地域允许的线路参数如下: cn-sh1: Bgp cn-sh2: Bgp cn-gd: Bgp cn-bj1: Bgp cn-bj2: Bgp hk: International us-ca: International th-bkk: International kr-seoul:International us-ws:International ge-fra:International sg:International tw-kh:International.其他海外线路均为 International
        - **PayMode** (str) - 弹性IP的计费模式. 枚举值: "Traffic", 流量计费; "Bandwidth", 带宽计费; "ShareBandwidth",共享带宽模式. "Free":免费带宽模式,默认为 "Bandwidth"
        - **ShareBandwidthId** (str) - 绑定的共享带宽Id，仅当PayMode为ShareBandwidth时有效


        **CreateUK8SClusterV2ParamNodesNetworkInterface**
        - **EIP** (dict) - 见 **CreateUK8SClusterV2ParamNodesNetworkInterfaceEIP** 模型定义


        **CreateUK8SClusterV2ParamNodesSecGroupId**
        - **Id** (str) - 安全组 ID。至多可以同时绑定5个安全组。
        - **Name** (str) - 安全组名称。
        - **Priority** (str) - 安全组优先级。取值范围[1, 5]


        **CreateUK8SClusterV2ParamNodes**
        - **BootDiskSIze** (int) - Node节点的系统盘大小，单位GB，默认为40。范围：[40, 500]。注意SSD本地盘无法调整。
        - **BootDiskType** (str) - 一组Node节点的系统盘类型，请参考 `磁盘类型 <https://docs.ucloud.cn/api/uhost-api/disk_type>`_ 。默认为SSD云盘
        - **CPU** (int) - 一组Node节点的虚拟CPU核数。单位：核，范围：[2, 64]，可选范围参考控制台。
        - **Count** (int) - 一组Node节点的数量，范围：[1,10]。
        - **DataDiskSize** (int) - 数据磁盘大小，单位GB。默认0。范围 ：[20, 1000]
        - **DataDiskType** (str) - 一组Node节点的数据盘类型，请参考 `磁盘类型 <https://docs.ucloud.cn/api/uhost-api/disk_type>`_ 。默认为SSD云盘
        - **GPU** (int) - 一组Node节点的GPU卡核心数，仅GPU机型支持此字段。
        - **GpuType** (str) - 一组Node节点的GPU类型，枚举值["K80", "P40", "V100"]，最新值参考Console。
        - **ImageId** (str) - Node节点的镜像 ID，不填则使用ImageId参数。支持用户自定义镜像。
        - **IsolationGroup** (str) - 一组Node节点的隔离组Id，归属于同一隔离组的虚拟机节点将落在不同的物理机上，单个隔离组最多只能容纳8个节点。参见DescribeIsolationGroup。
        - **Labels** (str) - Node节点标签，形式为key=value，多组Labels用”,“隔开,最多支持五组。
        - **MachineType** (str) - 一组Nodes节点云主机机型，如["N", "C", "O", "OS"]，具体请参照云主机机型。
        - **MaxPods** (int) - Node节点上可运行最大节点数，默认为110。
        - **Mem** (int) - 一组Node节点的内存大小。单位：MB,范围 ：[4096, 262144]，取值为1024的倍数，可选范围参考控制台。
        - **MinimalCpuPlatform** (str) - Node节点的最低cpu平台，不选则随机。枚举值["Intel/Auto", "Intel/IvyBridge", "Intel/Haswell", "Intel/Broadwell", "Intel/Skylake", "Intel/Cascadelake"。
        - **NamePrefix** (str) - 一组Node的自定义主机名前缀。 完整的自定义主机名为{NamePrefix}-{NodeIP}。
        - **NetworkInterface** (list) - 见 **CreateUK8SClusterV2ParamNodesNetworkInterface** 模型定义
        - **SecGroupId** (list) - 见 **CreateUK8SClusterV2ParamNodesSecGroupId** 模型定义
        - **SecurityGroupId** (str) - 防火墙ID，默认：Web推荐防火墙。如何查询SecurityGroupId请参见  `DescribeFirewall <https://docs.ucloud.cn/api/uk8s-api/api/unet-api/describe_firewall.html>`_ 。
        - **SecurityMode** (str) - 主机安全模式。Firewall：防火墙；SecGroup：安全组；默认值：Firewall。
        - **Taints** (str) - Node节点污点，形式为key=value:effect，多组taints用”,“隔开,最多支持五组。
        - **UNIFeature** (str) - 弹性网卡特性。开启了弹性网卡权限位，此特性才生效，默认 false 未开启，true 开启。
        - **Zone** (str) - 一组Nodes节点所属可用区，可创建多组Nodes节点，如一组是CPU Nodes节点，另一组是GPU Nodes节点。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_


        **CreateUK8SClusterV2ParamMasterSecGroupId**
        - **Id** (str) - 安全组 ID。至多可以同时绑定5个安全组。
        - **Name** (str) - 安全组名称。
        - **Priority** (str) - 安全组优先级。取值范围[1, 5]


        **CreateUK8SClusterV2ParamMaster**
        - **SecGroupId** (list) - 见 **CreateUK8SClusterV2ParamMasterSecGroupId** 模型定义
        - **Zone** (str) - Master节点所属可用区，需要设置 Master.0.Zone、 Master.1.Zone、Master.2.Zone 三个 Master 节点的可用区。 三个节点可部署在不同可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.CreateUK8SClusterV2RequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateUK8SClusterV2", d, **kwargs)
        return apis.CreateUK8SClusterV2ResponseSchema().loads(resp)

    def create_uk8s_uls_config(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CreateUK8SULSConfig - 创建 LogConfig 自定义资源，用于声明式地定义日志采集规则

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterId** (str) - (Required) UK8S 集群ID。
        - **Name** (str) - (Required) 要创建的日志的采集规则的名称，不能重复。总长度不能超过 253个字符。字符类型：只能包含小写字母（a-z）、数字（0-9）、破折号（-）和点（.）。开头和结尾字符：必须以小写字母或数字开头，并且也必须以小写字母或数字结尾。不允许以 - 或 . 开头或结尾。连续特殊字符：不能连续出现点（.）或破折号（-）。
        - **TopicID** (str) - (Required) 日志服务中用于接收日志的目标 Topic ID。
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ExtractRule** (dict) - 见 **CreateUK8SULSConfigParamExtractRule** 模型定义
        - **InputDetail** (dict) - 见 **CreateUK8SULSConfigParamInputDetail** 模型定义
        - **MatchRule** (dict) - 见 **CreateUK8SULSConfigParamMatchRule** 模型定义

        **Response**


        **Request Model**

        **CreateUK8SULSConfigParamMatchRuleWorkloads**
        - **Name** (str) - 按工作负载匹配时，工作负载的名称。
        - **Namespace** (str) - 按工作负载匹配时，工作负载所在的命名空间。
        - **Type** (str) - 按工作负载匹配时，工作负载的类型，例如 deployment, statefulset, daemonset,job, cronjob。


        **CreateUK8SULSConfigParamMatchRulePodLabelsLabels**
        - **Key** (str) - 按 Pod 标签匹配时，要匹配的标签的 Key。
        - **Value** (str) - 按 Pod 标签匹配时，要匹配的标签的值。
        - **ValueOperator** (str) - 按 Pod 标签匹配时，标签值的匹配操作符。可选值: in, notin。


        **CreateUK8SULSConfigParamExtractRuleExtractRule**
        - **LogRegexBase64** (str) - Base64 编码的日志提取正则表达式。


        **CreateUK8SULSConfigParamExtractRule**
        - **BeginningRegex** (str) - 行首正则表达式。当 logType 为多行模式 (如 multi_line 或 multi_line_full_regex) 时，用于标识一条新日志的开始。
        - **BeginningRegexBase64** (str) - Base64 编码的行首正则，优先级高于 BeginningRegex
        - **CollectPolicy** (str) - 采集策略。可选值: full (全量采集存量日志), increment (从当前时间点增量采集)。默认为 full。
        - **Delimiter** (str) - 当 LogType 为delimiter 时可选，接收 "space"、"tab"、"|"、";"、","。
        - **DelimiterBase64** (str) - Base64 编码的分隔符，优先级高于 Delimiter
        - **Encode** (str) - 日志原文的编码格式。可选值: utf-8, gbk。默认为 utf-8。
        - **ExtractRule** (dict) - 见 **CreateUK8SULSConfigParamExtractRuleExtractRule** 模型定义
        - **Keys** (list) - 当LogType 为分隔符、正则、多行正则时可用
        - **LogRegex** (str) - 日志提取正则表达式。当 logType 为正则模式 (如 full_regex,multi_line_full_regex) 时，用于从日志中提取字段。
        - **LogType** (str) - 日志解析类型，决定了如何结构化日志。可选值: multi_line_delimiter：多行分隔符，delimiter:分隔符，full_regex:完全正则，multi_line_full_regex:多行完全正则，minimal_list:单行全文日志,multi_line:多行全文日志
        - **TimeFormat** (str) - timeKey 对应的时间格式。
        - **TimeKey** (str) - 当日志为 json 或正则提取时，指定包含日志时间的字段名 (Key)。
        - **UnMatchKey** (str) - 没有设置默认值；UnMatchUpload="true" 时强制要求填写
        - **UnMatchUpload** (str) - 是否上传解析失败的日志。true 表示上传，false 表示丢弃。默认为 false。


        **CreateUK8SULSConfigParamMatchRulePodLabels**
        - **Labels** (list) - 见 **CreateUK8SULSConfigParamMatchRulePodLabelsLabels** 模型定义
        - **Namespace** (str) - 命名空间名称
        - **NamespaceOperator** (str) - 指定/排除命名空间, 可选值: in/notin


        **CreateUK8SULSConfigParamMatchRule**
        - **Container** (str) - 要匹配的容器名称，*表示所有容器，用逗号分隔
        - **ContainerOperator** (str) - 容器名称匹配操作符。支持：in(包含)，notin(不包含)
        - **PodLabels** (dict) - 见 **CreateUK8SULSConfigParamMatchRulePodLabels** 模型定义
        - **Workloads** (list) - 见 **CreateUK8SULSConfigParamMatchRuleWorkloads** 模型定义


        **CreateUK8SULSConfigParamInputDetailFilePaths**
        - **File** (str) - 定义采集路径的文件名
        - **Path** (str) - 定义采集路径


        **CreateUK8SULSConfigParamInputDetailMetadata**
        - **Container** (str) - 指定具体要采集元数据的容器名。如果留空，则不采集容器的元数据,可选字段：container_name,namespace,pod_name,pod_ip,pod_uid,container_id,image_name。Pod Label 元数据通过指定 InputDetail.Metadata.Labels字段。
        - **Labels** (str) - 定义要采集哪些 Pod 的标签 (Labels)。可选值: * (采集所有标签), "app,version" (仅采集 app 和 version), "" (不采集任何标签)。


        **CreateUK8SULSConfigParamInputDetail**
        - **FilePaths** (list) - 见 **CreateUK8SULSConfigParamInputDetailFilePaths** 模型定义
        - **Metadata** (dict) - 见 **CreateUK8SULSConfigParamInputDetailMetadata** 模型定义
        - **Stream** (str) - all、stdout、stderr，默认 all (用于 InputDetail.Type = container_stdout)
        - **Type** (str) - 日志输入类型。支持 container_file 和 container_stdout


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.CreateUK8SULSConfigRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateUK8SULSConfig", d, **kwargs)
        return apis.CreateUK8SULSConfigResponseSchema().loads(resp)

    def del_uk8s_cluster(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DelUK8SCluster - 删除UK8S集群

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list.html>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist.html>`_
        - **ClusterId** (str) - (Required) 集群id
        - **ReleaseUDisk** (bool) - 是否删除节点挂载的数据盘。枚举值[true:删除，false: 不删除]，默认不删除

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DelUK8SClusterRequestSchema().dumps(d)

        resp = self.invoke("DelUK8SCluster", d, **kwargs)
        return apis.DelUK8SClusterResponseSchema().loads(resp)

    def del_uk8s_cluster_node_v2(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DelUK8SClusterNodeV2 - 删除集群中的Node节点，删除前务必先将其中的Pod驱逐。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list.html>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist.html>`_
        - **ClusterId** (str) - (Required) UK8S集群ID。 可从UK8S控制台获取。
        - **NodeId** (str) - (Required) Node在UK8S处的唯一标示，如uk8s-reewqe5-sdasadsda。**非云主机或物理云主机资源Id**
        - **ReleaseDataUDisk** (bool) - 删除节点时是否释放数据盘。 枚举值[true:释放，false: 不释放]，默认为true。

        **Response**

        - **Message** (str) - 返回错误消息，当 RetCode 非 0 时提供详细的描述信息。

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DelUK8SClusterNodeV2RequestSchema().dumps(d)

        resp = self.invoke("DelUK8SClusterNodeV2", d, **kwargs)
        return apis.DelUK8SClusterNodeV2ResponseSchema().loads(resp)

    def delete_uk8s_uls_config(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DeleteUK8SULSConfig - 删除指定UK8S集群的日志采集规则。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。请参考GetProjectList接口。
        - **Region** (str) - (Config) 地域。参见地域和可用区列表。
        - **ClusterId** (str) - (Required) 要操作的 UK8S 集群的 ID。
        - **Name** (str) - (Required) 要删除的日志的采集规则的名称。
        - **Zone** (str) - (Required) 可用区。参见可用区列表。

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DeleteUK8SULSConfigRequestSchema().dumps(d)

        resp = self.invoke("DeleteUK8SULSConfig", d, **kwargs)
        return apis.DeleteUK8SULSConfigResponseSchema().loads(resp)

    def describe_uk8s_cluster(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DescribeUK8SCluster - 获取集群信息

        **Request**

        - **ProjectId** (str) - (Config) 项目id
        - **Region** (str) - (Config) 所属区域
        - **ClusterId** (str) - (Required) k8s集群ID

        **Response**

        - **ApiServer** (str) - 集群apiserver地址
        - **Autoscaler** (dict) - 见 **Autoscaler** 模型定义
        - **CACert** (str) - 集群CA根证书
        - **CNIMode** (str) - CNI模式，可选值VPC/Calico
        - **ClusterDomain** (str) - 自定义或者默认的clusterdomain
        - **ClusterId** (str) - 集群ID
        - **ClusterName** (str) - 资源名字
        - **ClusterType** (str) - 集群版本
        - **CreateTime** (int) - 创建时间
        - **DedicatedPodSubnet** (bool) - Pod是否使用独立子网
        - **DeleteProtection** (int) - 删除保护开关。0表示不开启，1表示开启。默认不开启
        - **EnableUserAuth** (bool) - 是否开启了授权管理功能
        - **EtcdCert** (str) - 集群etcd服务证书
        - **EtcdKey** (str) - 集群etcd服务密钥
        - **ExternalApiServer** (str) - 集群外部apiserver地址
        - **ExternalUlb** (str) - 外部 API Server 负载均衡实例 ID
        - **InternalUlb** (str) - 内部 API Server 负载均衡实例 ID
        - **KubeProxy** (dict) - 见 **KubeProxy** 模型定义
        - **LbClass** (str) - 负载均衡类型
        - **LoopbackClientCert** (dict) - 见 **LoopbackClientCert** 模型定义
        - **MasterCount** (int) - Master 节点数量
        - **MasterList** (list) - 见 **UhostInfo** 模型定义
        - **MasterResourceStatus** (str) - Master配置预警：Normal正常；Warning 需要升级；Error    需要紧急升级；
        - **MonitorType** (str) - 集群的监控类型：no无监控；cloudwatch统一监控平台；prometheus内置监控
        - **NodeCIDR** (str) - 节点网段
        - **NodeCount** (int) - Node节点数量
        - **NodeList** (list) - 见 **UhostInfo** 模型定义
        - **PodCIDR** (str) - Pod网段
        - **PodSubnetIds** (list) - Pod使用的独立子网列表
        - **PodSubnetSecGroups** (list) - Pod独立子网内的ip使用的安全组
        - **RuntimeName** (str) - 容器运行时名称
        - **RuntimeVersion** (str) - 容器运行时版本
        - **ServiceCIDR** (str) - 服务网段
        - **Status** (str) - 状态
        - **SubnetId** (str) - 所属子网
        - **UpdateTime** (int) - 更新时间
        - **VPCId** (str) - 所属VPC
        - **Version** (str) - K8S版本

        **Response Model**

        **DiskSet**
        - **BackupType** (str) - 备份方案，枚举类型：BASIC_SNAPSHOT,普通快照；DATAARK,方舟。无快照则不返回该字段。
        - **DiskId** (str) - 磁盘长ID
        - **DiskType** (str) - LOCAL_NOMAL| CLOUD_NORMAL| LOCAL_SSD| CLOUD_SSD|EXCLUSIVE_LOCAL_DISK
        - **Drive** (str) - 磁盘盘符
        - **Encrypted** (str) - Yes: 加密 No: 非加密
        - **IOPS** (int) - 当前主机的IOPS值
        - **IsBoot** (str) - True| False
        - **Name** (str) - UDisk名字（仅当磁盘是UDisk时返回）
        - **Size** (int) - 磁盘大小，单位: GB
        - **Type** (str) - 磁盘类型。系统盘: Boot，数据盘: Data,网络盘：Udisk


        **IPSet**
        - **Bandwidth** (int) - IP对应的带宽, 单位: Mb (内网IP不显示带宽信息)
        - **Default** (str) - 是否默认的弹性网卡的信息。true: 是默认弹性网卡；其他值：不是。
        - **IP** (str) - IP地址
        - **IPId** (str) - IP资源ID (内网IP无对应的资源ID)
        - **IPMode** (str) - IP 地址分配模式
        - **Mac** (str) - 网卡的 MAC 地址
        - **NetworkInterfaceId** (str) - 虚拟网卡 Id
        - **SubnetId** (str) - IP 所在的 子网 Id
        - **Type** (str) - 国际: Internation，BGP: Bgp，内网: Private
        - **VPCId** (str) - IP 所属的 VPC Id


        **SecGroupId**
        - **Id** (str) - 安全组名称
        - **Name** (str) - 安全组id
        - **Priority** (str) - 安全组优先级


        **Autoscaler**
        - **Enabled** (int) - 打开/关闭
        - **ScaleDownDelayAfterAdd** (str) - 静默时间
        - **ScaleDownGpuUtilizationThreshold** (str) - GPU缩容阈值
        - **ScaleDownUnneededTime** (str) - 缩容触发延时
        - **ScaleDownUtilizationThreshold** (str) - CPU缩容阈值
        - **UpdateTime** (int) -
        - **Version** (str) - 伸缩器版本


        **UhostInfo**
        - **BasicImageName** (str) - 基础镜像名称
        - **CPU** (int) - Cpu数量
        - **CreateTime** (int) - 创建时间
        - **DiskSet** (list) - 见 **DiskSet** 模型定义
        - **ExpireTime** (int) - 到期时间
        - **GPU** (int) - GPU 数量
        - **GpuType** (str) - GPU 型号
        - **IPSet** (list) - 见 **IPSet** 模型定义
        - **MachineType** (str) - 主机机型类别
        - **Memory** (int) - 内存
        - **Name** (str) - 主机名称
        - **NodeId** (str) - 主机ID
        - **NodeType** (str) - 节点类型：uhost表示云主机;uphost表示物理云主机
        - **OsName** (str) - 镜像信息
        - **OsType** (str) - 操作系统类型
        - **SecGroupId** (list) - 见 **SecGroupId** 模型定义
        - **State** (str) - 主机状态
        - **TotalDiskSpace** (int) - 节点总磁盘空间
        - **Zone** (str) - 所在机房


        **LoopbackClientCert**
        - **ExpireTime** (int) - 证书到期时间
        - **Warn** (bool) - 证书是否进入过期告警状态


        **KubeProxy**
        - **Mode** (str) - KubeProxy模式，枚举值为[ipvs,iptables]


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DescribeUK8SClusterRequestSchema().dumps(d)

        resp = self.invoke("DescribeUK8SCluster", d, **kwargs)
        return apis.DescribeUK8SClusterResponseSchema().loads(resp)

    def describe_uk8s_image(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DescribeUK8SImage - 获取UK8S支持的Node节点操作系统，可基于该操作系统制定自定义镜像

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **GPUType** (str) - 适用GPU类型，如1080Ti、4090、V100、A800等，MachineType为G时必须提供
        - **K8sVersion** (str) - k8s集群版本，如1.28.15
        - **MachineType** (str) - 适用机型，如O、G、OPRO等，默认为O
        - **ProductType** (str) - 产品类型，可选值uhost、uphost，不填则返回所有
        - **Zone** (str) - 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **CustomImageSet** (list) - 见 **ImageInfo** 模型定义
        - **CustomPHostImageSet** (list) - 见 **ImageInfo** 模型定义
        - **ImageSet** (list) - 见 **ImageInfo** 模型定义
        - **Message** (str) - 返回错误消息，当 RetCode 非 0 时提供详细的描述信息。
        - **PHostImageSet** (list) - 见 **ImageInfo** 模型定义

        **Response Model**

        **ImageInfo**
        - **Features** (list) - 镜像支持的特性
        - **ImageId** (str) - 镜像 Id
        - **ImageName** (str) - 镜像名称
        - **ImageSize** (int) - 镜像大小
        - **IntegratedSoftware** (str) - 集成软件名称, 如NV驱动版本、cuda版本
        - **NotSupportGPU** (bool) - 该镜像是否支持GPU机型，枚举值[true:不支持，false:支持]。
        - **OsName** (str) - OS 名称
        - **OsType** (str) - OS 类型
        - **SupportedGPUTypes** (list) - 支持的GPU机型
        - **ZoneId** (int) - 可用区 Id


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DescribeUK8SImageRequestSchema().dumps(d)

        resp = self.invoke("DescribeUK8SImage", d, **kwargs)
        return apis.DescribeUK8SImageResponseSchema().loads(resp)

    def describe_uk8s_node(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DescribeUK8SNode - 用于获取 UK8S 节点详情

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterId** (str) - (Required) UK8S 集群 Id
        - **Name** (str) - (Required) K8S 节点IP或者节点ID

        **Response**

        - **Action** (str) - 操作名称
        - **AllocatedPodCount** (int) - 已分配到当前节点的 Pod 数量
        - **Annotations** (list) - 字符串数组，每一项是类似 "node.alpha.kubernetes.io/ttl=0" 的注解
        - **CPUCapacity** (str) - 节点 CPU 总量
        - **CPULimits** (str) - 节点上已分配 Pod 的 CPU 限制值
        - **CPULimitsFraction** (str) - 节点上已分配 Pod 的 CPU 限制值占 CPU 总量的比例
        - **CPURequests** (str) - 节点上已分配 Pod 的 CPU 请求量
        - **CPURequestsFraction** (str) - 节点上已分配 Pod 的 CPU 请求量占 CPU 总量的比例
        - **Conditions** (list) - 见 **K8SNodeCondition** 模型定义
        - **ContainerImages** (list) - 节点上镜像名称数组
        - **ContainerRuntimeVersion** (str) - 容器运行时版本，如："docker://18.9.9"
        - **CreationTimestamp** (int) - 时间戳，单位是 秒
        - **Hostname** (str) - 主机名
        - **InternalIP** (str) - 内部 IP 地址
        - **KernelVersion** (str) - 内核版本，如："4.19.0-6.el7.ucloud.x86_64"
        - **KubeProxyVersion** (str) - kubeproxy 版本
        - **KubeletVersion** (str) - kubelet 版本
        - **Labels** (list) - 字符串数组，每一项是类似 "kubernetes.io/arch=amd64" 的标签
        - **MemoryCapacity** (str) - 节点内存总量
        - **MemoryLimits** (str) - 节点上已分配 Pod 的内存限制量
        - **MemoryLimitsFraction** (str) - 节点上已分配 Pod 的内存限制量占内存总量的比例，如返回值为 "18"，则意味着限制量占总量的 18%
        - **MemoryRequests** (str) - 节点上已分配 Pod 的内存请求量
        - **MemoryRequestsFraction** (str) - 节点上已分配 Pod 的内存请求量占内存总量的比例，如返回值为 "4.5"，则意味着请求量占总量的 4.5%
        - **Message** (str) - 操作出错时的提示信息
        - **Name** (str) - 节点名称
        - **OSImage** (str) - 操作系统类型，如："CentOS Linux 7 (Core)"
        - **PodCapacity** (int) - 节点允许的可分配 Pod 最大数量
        - **ProviderID** (str) - 字符串，如："UCloud://cn-sh2-02//uk8s-vsc0vgob-n-mpzxc"
        - **RetCode** (int) - 返回码
        - **Taints** (list) - 字符串数组，每一项是类似 "node-role.kubernetes.io/master:NoSchedule" 的污点
        - **Unschedulable** (bool) - 是否禁止调度

        **Response Model**

        **K8SNodeCondition**
        - **LastProbeTime** (str) - 最后一次上报状态的时间
        - **LastTransitionTime** (str) - 最后一次状态转变时间
        - **Message** (str) - 状态变化的描述信息
        - **Reason** (str) - 状态变化的原因
        - **Status** (str) - 状态，False、True
        - **Type** (str) - Condition 类型，如 MemoryPressure、DiskPressure、PIDPressure、Ready


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DescribeUK8SNodeRequestSchema().dumps(d)

        resp = self.invoke("DescribeUK8SNode", d, **kwargs)
        return apis.DescribeUK8SNodeResponseSchema().loads(resp)

    def get_cluster_config(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetClusterConfig - 获取集群配置文件，管理集群的凭证

        **Request**

        - **ProjectId** (str) - (Config) 所在项目
        - **Region** (str) - (Config) 所在区域
        - **ClusterId** (str) - (Required) 集群ID

        **Response**

        - **ExternalKubeConfig** (str) - 开启公网apiserver的情况下，有数据返回。
        - **KubeConfig** (str) - 配置信息
        - **Updatable** (bool) - 用于标示 kubeconfig 是否可以进行替换更新

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GetClusterConfigRequestSchema().dumps(d)

        resp = self.invoke("GetClusterConfig", d, **kwargs)
        return apis.GetClusterConfigResponseSchema().loads(resp)

    def get_uk8s_versions(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetUK8SVersions - 获取支持创建的UK8S集群版本、Containerd版本

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Kind** (str) - (Required) 集群类型，可选值为[Dedicated]

        **Response**

        - **Data** (list) - 见 **UK8SVersionData** 模型定义

        **Response Model**

        **UK8SVersionData**
        - **ContainerdVersion** (str) - Containerd 版本
        - **K8sVersion** (str) - K8S 版本


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GetUK8SVersionsRequestSchema().dumps(d)

        resp = self.invoke("GetUK8SVersions", d, **kwargs)
        return apis.GetUK8SVersionsResponseSchema().loads(resp)

    def list_uk8s_cluster_node_v2(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListUK8SClusterNodeV2 - 获取UK8S集群节点信息

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterId** (str) - (Required) UK8S集群ID
        - **NodeIds** (str) - 可传一个或多个节点id  不传或为空则返回所有节点

        **Response**

        - **NodeSet** (list) - 见 **NodeInfoV2** 模型定义
        - **TotalCount** (int) - 满足条件的节点数量，包括Master。

        **Response Model**

        **KubeProxy**
        - **Mode** (str) - KubeProxy模式，枚举值为[ipvs,iptables]


        **UHostIPSet**
        - **Bandwidth** (int) - IP对应的带宽, 单位: Mb (内网IP不显示带宽信息)
        - **IP** (str) - IP地址
        - **IPId** (str) - IP资源ID (内网IP无对应的资源ID)
        - **IPMode** (str) - IP 协议类型
        - **Mac** (str) - Mac地址
        - **NetworkInterfaceId** (str) - 网络接口资源 ID
        - **SubnetId** (str) - IP地址对应的子网 ID
        - **Type** (str) - 国际: Internation，BGP: Bgp，内网: Private
        - **VPCId** (str) - IP地址对应的VPC ID


        **NodeInfoV2**
        - **AsgId** (str) - 节点所属伸缩组ID，非伸缩组创建出来的节点，伸缩组ID为Default。
        - **BootDiskSize** (int) - 系统盘大小
        - **CPU** (int) - Node节点CPU核数，单位: 核。
        - **CPUPlatform** (str) - CPU平台
        - **CreateTime** (int) - 节点创建时间
        - **DataDiskSize** (int) - 数据盘大小，如果有多块数据盘会汇总展示，不包括PVC
        - **ExpireTime** (int) - 节点计费到期时间
        - **GPU** (int) - 节点的GPU颗数。
        - **GPUType** (str) - 节点GPU型号(如果为GPU机型)
        - **IDCId** (str) - 边缘机房id
        - **IDCName** (str) - 边缘机房
        - **IPSet** (list) - 见 **UHostIPSet** 模型定义
        - **ImageAccelable** (bool) - 是否启用了容器镜像加速
        - **InstanceId** (str) - 资源ID，如uhost-xxxx，或uphost-xxxxx。
        - **InstanceName** (str) - 资源名称，初始值等于NodeId，用户可在UHost或UPHost处修改。
        - **InstanceType** (str) - Node节点的资源类型，枚举值为UHost或UPHost。
        - **KubeProxy** (dict) - 见 **KubeProxy** 模型定义
        - **KubeletVersion** (str) - Kubelet版本
        - **Labels** (list) - 节点标签
        - **MachineType** (str) - 机型类别，分别对应Uhost的MachineType或PHost的PHostType。
        - **MaxCPU** (int) - CPU最大可用
        - **MaxMemory** (int) - 内存最大可用
        - **MaxPod** (int) - pod最大可用
        - **Memory** (int) - 内存大小，单位: MB。
        - **NodeGroupId** (str) - 节点池id
        - **NodeGroupName** (str) - 节点所属节点池名称
        - **NodeId** (str) - NodeId，Node在UK8S处的唯一标示，如uk8s-reewqe5-sdasadsda
        - **NodeLogInfo** (str) - 加节点时判断是否没有资源，如果返回NORESOURCE则代表没有资源了
        - **NodeRole** (str) - node角色，枚举值为master、node
        - **NodeStatus** (str) - Node的状态：枚举值：初始化："Initializing"；启动中："Starting"；运行："Running"；停止中："Stopping"；停止："Stopped"；待删除："ToBeDeleted"；删除中："Deleting"；异常："Error"；安装失败："Install Fail"；
        - **OsName** (str) - Node节点的镜像名称。
        - **OsType** (str) - Node节点的操作系统类别，如Linux或Windows。
        - **PodCIDR** (str) - Pod CIDR
        - **Remark** (str) - 节点主机备注信息
        - **RequestCPU** (int) - 已申请的CPU
        - **RequestMemory** (int) - 已申请的Memory
        - **RequestPod** (int) - 已申请的pod
        - **RuntimeName** (str) - Runtime 名字
        - **RuntimeVersion** (str) - Runtime 版本
        - **Tag** (str) - 节点所属业务组
        - **UHostFamily** (str) - 主机规格族
        - **Unschedulable** (bool) - 是否允许Pod调度到该节点，枚举值为true或false。
        - **UsedCPU** (int) - 已使用的CPU
        - **UsedMemory** (int) - 已使用的Memory
        - **Zone** (str) - Node所在可用区


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListUK8SClusterNodeV2RequestSchema().dumps(d)

        resp = self.invoke("ListUK8SClusterNodeV2", d, **kwargs)
        return apis.ListUK8SClusterNodeV2ResponseSchema().loads(resp)

    def list_uk8s_cluster_v2(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListUK8SClusterV2 - 获取UK8S集群列表信息

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterId** (str) - UK8S集群ID
        - **Limit** (int) - 返回数据长度，默认为20。
        - **Offset** (int) - 列表起始位置偏移量，默认为0。

        **Response**

        - **ClusterCount** (int) - 满足条件的集群数量
        - **ClusterSet** (list) - 见 **ClusterSet** 模型定义
        - **TotalCount** (int) - 总数

        **Response Model**

        **LoopbackClientCert**
        - **ExpireTime** (int) - 证书到期时间
        - **Warn** (bool) - 证书是否进入过期告警状态


        **ClusterSet**
        - **ApiServer** (str) - 集群apiserver地址
        - **CNIMode** (str) - CNI网络模式
        - **ClusterId** (str) - 集群ID
        - **ClusterLogInfo** (str) - 创建集群时判断如果为NORESOURCE则为没资源，否则为空
        - **ClusterName** (str) - 资源名字
        - **ClusterType** (str) - 计费/管理形态，区分"专有版"和"托管版"两种售卖形态
        - **CreateTime** (int) - 创建时间
        - **DeleteProtection** (int) - 删除保护开关。0表示不开启，1表示开启。默认不开启
        - **ExternalApiServer** (str) - 集群外部apiserver地址
        - **K8sVersion** (str) - 集群版本
        - **LoopbackClientCert** (dict) - 见 **LoopbackClientCert** 模型定义
        - **MasterCount** (int) - Master 节点数量
        - **NodeCount** (int) - Node节点数量
        - **PodCIDR** (str) - Pod网段
        - **RuntimeName** (str) - 容器运行时名称
        - **RuntimeVersion** (str) - 容器运行时版本号，docker 或 containerd 版本
        - **ServiceCIDR** (str) - 服务网段
        - **Status** (str) - 集群状态，枚举值：初始化："INITIALIZING"；启动中："STARTING"；创建失败："CREATEFAILED"；正常运行："RUNNING"；添加节点："ADDNODE"；删除节点："DELNODE"；删除中："DELETING"；删除失败："DELETEFAILED"；错误："ERROR"；升级插件："UPDATE_PLUGIN"；更新插件信息："UPDATE_PLUGIN_INFO"；异常："ABNORMAL"；升级集群中："UPGRADING"；容器运行时切换："CONVERTING"
        - **SubnetId** (str) - 所属子网
        - **VPCId** (str) - 所属VPC


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListUK8SClusterV2RequestSchema().dumps(d)

        resp = self.invoke("ListUK8SClusterV2", d, **kwargs)
        return apis.ListUK8SClusterV2ResponseSchema().loads(resp)

    def list_uk8s_node_group(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListUK8SNodeGroup - 列出UK8S节点池

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterId** (str) - (Required) 集群ID

        **Response**

        - **NodeGroupList** (list) - 见 **NodeGroupSet** 模型定义

        **Response Model**

        **ReservedResource**
        - **CPU** (str) - CPU
        - **EphemeralStorage** (str) - 存储
        - **Memory** (str) - 内存
        - **Pid** (str) - Pid


        **EIP**
        - **Bandwidth** (int) - 【若绑定EIP，此参数必填】弹性IP的外网带宽, 单位为Mbps. 共享带宽模式下非必传, 非共享带宽模式必须指定非0Mbps带宽. 各地域非共享带宽的带宽范围如下： 流量计费[1-300]，带宽计费[1-800]
        - **CouponId** (str) - 当前EIP代金券id。请通过DescribeCoupon接口查询，或登录用户中心查看。
        - **OperatorName** (str) - 【若绑定EIP，此参数必填】弹性IP的线路。枚举值: 国际: International，BGP: Bgp。 各地域允许的线路参数如下: cn-sh1: Bgp cn-sh2: Bgp cn-gd: Bgp cn-bj1: Bgp cn-bj2: Bgp hk: International us-ca: International th-bkk: International kr-seoul:International us-ws:International ge-fra:International sg:International tw-kh:International.其他海外线路均为 International
        - **PayMode** (str) - 弹性IP的计费模式. 枚举值: "Traffic", 流量计费; "Bandwidth", 带宽计费; "ShareBandwidth",共享带宽模式. "Free":免费带宽模式,默认为 "Bandwidth"
        - **ShareBandwidthId** (str) - 绑定的共享带宽Id，仅当PayMode为ShareBandwidth时有效


        **SecGroupId**
        - **Id** (str) - 安全组名称
        - **Name** (str) - 安全组id
        - **Priority** (str) - 安全组优先级


        **EvictionCondition**
        - **ImagefsAvailable** (str) - 镜像文件系统存储相关驱逐条件或宽限时间。
        - **MemoryAvailable** (str) - 内存相关驱逐条件或宽限时间。
        - **NodefsAvailable** (str) - 节点存储余量相关驱逐条件或宽限时间。
        - **NodefsInodesFree** (str) - 节点剩余inodes驱逐条件或宽限时间。


        **KubeletConfiguration**
        - **ContainerLogMaxFiles** (int) - 最大日志文件数量
        - **ContainerLogMaxSize** (str) - 最大日志文件大小
        - **EvictionHard** (dict) - 见 **EvictionCondition** 模型定义
        - **EvictionSoft** (dict) - 见 **EvictionCondition** 模型定义
        - **EvictionSoftGracePeriod** (dict) - 见 **EvictionCondition** 模型定义
        - **ImageGCHighThresholdPercent** (int) - 镜像垃圾收集阈值
        - **ImageGCLowThresholdPercent** (int) - 停止镜像垃圾收集阈值
        - **KubeReserved** (dict) - 见 **ReservedResource** 模型定义
        - **MaxPods** (int) - 最大Pod数量
        - **SystemReserved** (dict) - 见 **ReservedResource** 模型定义


        **NetworkInterface**
        - **EIP** (dict) - 见 **EIP** 模型定义


        **DiskSet**
        - **BackupType** (str) - 备份方案，枚举类型：BASIC_SNAPSHOT,普通快照；DATAARK,方舟。无快照则不返回该字段。
        - **DiskId** (str) - 磁盘长ID
        - **DiskType** (str) - LOCAL_NOMAL| CLOUD_NORMAL| LOCAL_SSD| CLOUD_SSD|EXCLUSIVE_LOCAL_DISK
        - **Drive** (str) - 磁盘盘符
        - **Encrypted** (str) - Yes: 加密 No: 非加密
        - **IOPS** (int) - 当前主机的IOPS值
        - **IsBoot** (str) - True| False
        - **Name** (str) - UDisk名字（仅当磁盘是UDisk时返回）
        - **Size** (int) - 磁盘大小，单位: GB
        - **Type** (str) - 磁盘类型。系统盘: Boot，数据盘: Data,网络盘：Udisk


        **NodeGroupSet**
        - **BootDiskSize** (int) - 系统盘大小
        - **BootDiskType** (str) - 系统盘类型
        - **CPU** (int) - 虚拟CPU核数
        - **ChargeType** (str) - 付费方式
        - **CreateTime** (int) - 节点池创建时间
        - **DataDiskSize** (int) - 数据盘大小
        - **DataDiskType** (str) - 数据盘类型
        - **Disks** (list) - 见 **DiskSet** 模型定义
        - **GPU** (int) - GPU卡核心数
        - **GpuType** (str) - GPU类型
        - **ImageId** (str) - 镜像ID
        - **ImageName** (str) - 镜像名称
        - **ImageType** (str) - 镜像类型
        - **InitScript** (str) - 用户自定义Shell脚本。与UserData的区别在于InitScript在节点初始化完毕后才执行，UserData则是云主机初始化时执行。
        - **IsolationGroupId** (str) - 硬件隔离组id。可通过DescribeIsolationGroup获取。
        - **KubeletConfiguration** (dict) - 见 **KubeletConfiguration** 模型定义
        - **Labels** (str) - Node节点标签。key=value形式,多组用”,“隔开，最多5组。 如env=pro,type=game
        - **MachineType** (str) - 机型
        - **MaxPods** (int) - int默认110，生产环境建议小于等于110。
        - **Mem** (int) - 内存大小
        - **MinimalCpuPlatform** (str) - cpu平台
        - **NetCapability** (str) - 网络配置
        - **NetworkInterface** (list) - 见 **NetworkInterface** 模型定义
        - **NodeGroupId** (str) - 节点池ID
        - **NodeGroupName** (str) - 节点池名字
        - **NodeList** (list) - 节点id列表
        - **NodeNamePrefix** (str) - 自定义Uhost主机名前缀。完整的自定义Uhost主机名为{NodeNamePrefix}-{NodeIP}。
        - **OsName** (str) - 操作系统名称
        - **OsType** (str) - 操作系统类型
        - **RelatedAsg** (list) - 节点池关联的弹性伸缩组ID
        - **SecGroupId** (list) - 见 **SecGroupId** 模型定义
        - **SecurityGroupId** (str) - 防火墙ID，默认：Web推荐防火墙。如何查询SecurityGroupId请参见  `DescribeFirewall <https://docs.ucloud.cn/api/uk8s-api/api/unet-api/describe_firewall.html>`_ 。
        - **SecurityMode** (str) - 主机安全模式。Firewall：防火墙；SecGroup：安全组；默认值：Firewall。
        - **SubnetId** (str) - 子网 ID。默认为集群创建时填写的子网ID，也可以填写集群同VPC内的子网ID。
        - **Tag** (str) - 业务组
        - **Taints** (str) - Node节点污点，形式为key=value:effect，多组taints用”,“隔开,最多支持五组。
        - **UHostFamily** (str) - 主机规格族
        - **UNIFeature** (bool) - 是否启用 UNI 网络特性
        - **UpdateTime** (int) - 节点池更新时间
        - **UserData** (str) - 用户自定义数据。当镜像支持Cloud-init Feature时可填写此字段。注意：1、总数据量大小不超过 16K；2、使用base64编码。
        - **Zone** (str) - 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListUK8SNodeGroupRequestSchema().dumps(d)

        resp = self.invoke("ListUK8SNodeGroup", d, **kwargs)
        return apis.ListUK8SNodeGroupResponseSchema().loads(resp)

    def list_uk8s_uls_config(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListUK8SULSConfig - 查询 UK8S 的 ULSConfig

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **TopicID** (str) - (Required) 日志服务中用于接收日志的目标 TopicId。
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterId** (str) - 集群 Id，如果不填，返回该账号该地域所有集群的 ULSConfig

        **Response**

        - **LogConfig** (list) - 见 **ULSLogConfig** 模型定义

        **Response Model**

        **ULSWorkloadMatch**
        - **Name** (str) - 工作负载的名称。
        - **Namespace** (str) - 工作负载所在的命名空间。
        - **Type** (str) - 工作负载的类型，例如 deployment, statefulset, daemonset,cronjob,job。


        **ULSLabels**
        - **Key** (str) - 要匹配的标签的 Key。
        - **Value** (str) - 要匹配的标签的值。
        - **ValueOperator** (str) - 标签值的匹配操作符。可选值: in, notin。


        **ULSPodLabelsMatch**
        - **Labels** (list) - 见 **ULSLabels** 模型定义
        - **Namespace** (str) - 要匹配的命名空间。namespaceOperator 存在时必需。
        - **NamespaceOperator** (str) - 命名空间名称的匹配操作符。可选值: in, notin。


        **ULSMatchRule**
        - **Container** (str) - 要匹配的容器名称，*表示所有容器，用逗号分隔
        - **ContainerOperator** (str) - 容器名称匹配操作符。支持：in(包含)，notin(不包含)
        - **PodLabels** (dict) - 见 **ULSPodLabelsMatch** 模型定义
        - **Workloads** (list) - 见 **ULSWorkloadMatch** 模型定义


        **ULSExtractRule**
        - **BeginningRegex** (str) - 行首正则表达式。在 multi_line、multi_line_full_regex 或 multi_line_delimiter 模式下，BeginningRegex 和 BeginningRegexBase64 必须至少填写一个。
        - **BeginningRegexBase64** (str) - Base64 编码的行首正则表达式。填写时优先于 BeginningRegex。
        - **CollectPolicy** (str) - 采集策略。可选值：full（全量采集存量日志）、increment（从当前时间点增量采集）。默认为 full。
        - **Delimiter** (str) - 分隔符。适用于 delimiter 或 multi_line_delimiter，可选值：space、tab、|、;、,。
        - **DelimiterBase64** (str) - Base64 编码的分隔符。填写时优先于 Delimiter。
        - **Encode** (str) - 日志原文的编码格式。可选值：utf-8、gbk。默认为 utf-8。
        - **Keys** (list) - 提取后的字段名列表。仅适用于 delimiter、full_regex、multi_line_full_regex 和 multi_line_delimiter。
        - **LogRegex** (str) - 日志提取正则表达式。在 full_regex 或 multi_line_full_regex 模式下，LogRegex 和 LogRegexBase64 必须至少填写一个。
        - **LogRegexBase64** (str) - Base64 编码的日志提取正则表达式。填写时优先于 LogRegex。
        - **LogType** (str) - 日志解析类型。可选值：json、delimiter、full_regex、multi_line_full_regex、multi_line_delimiter、minimal_list、multi_line。
        - **TimeFormat** (str) - TimeKey 对应的时间格式。在 json、full_regex 或 multi_line_full_regex 模式下，填写 TimeKey 时必须同时填写 TimeFormat。
        - **TimeKey** (str) - 包含日志时间的字段名。
        - **UnMatchKey** (str) - 存放无法解析的日志原文的 Key。UnMatchUpload 为 true 时必须填写。
        - **UnMatchUpload** (str) - 是否上传解析失败的日志。字符串 true 表示上传，false 表示丢弃。默认为 false。


        **ULSFilePaths**
        - **File** (str) - 采集文件
        - **Path** (str) - 定义采集路径


        **ULSInputMetadata**
        - **Container** (str) - 指定具体要采集元数据的容器名。如果留空，则不采集容器的元数据，可选字段：container_name,namespace,pod_name,pod_ip,pod_uid,container_id,image_name。Pod Label 元数据通过指定 InputDetail.Metadata.Labels 字段。
        - **Labels** (str) - 定义要采集哪些 Pod 的标签 (Labels)。可选值：*：采集所有标签。app,version：仅采集 app 和 version 这两个标签。""（空字符串）：不采集任何标签。


        **ULSInputDetail**
        - **FilePaths** (list) - 见 **ULSFilePaths** 模型定义
        - **InputMetadata** (dict) - 见 **ULSInputMetadata** 模型定义
        - **Stream** (str) - 容器标准输出流类型。仅适用于 container_stdout，可选值：all、stdout、stderr，默认为 all。
        - **Type** (str) - 日志输入类型。可选值：container_file、container_stdout。


        **ULSLogConfig**
        - **ClusterId** (str) - uk8s集群id
        - **ExtractRule** (dict) - 见 **ULSExtractRule** 模型定义
        - **InputDetail** (dict) - 见 **ULSInputDetail** 模型定义
        - **MachineGroup** (str) - 机器组
        - **MatchRule** (dict) - 见 **ULSMatchRule** 模型定义
        - **Name** (str) - 采集配置规则名称
        - **TopicID** (str) - 日志服务中用于接收日志的目标 Topic ID。


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListUK8SULSConfigRequestSchema().dumps(d)

        resp = self.invoke("ListUK8SULSConfig", d, **kwargs)
        return apis.ListUK8SULSConfigResponseSchema().loads(resp)

    def remove_uk8s_node_group(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """RemoveUK8SNodeGroup - 删除UK8S节点池

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterId** (str) - (Required) 集群id
        - **NodeGroupId** (str) - (Required) 节点池Id

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.RemoveUK8SNodeGroupRequestSchema().dumps(d)

        resp = self.invoke("RemoveUK8SNodeGroup", d, **kwargs)
        return apis.RemoveUK8SNodeGroupResponseSchema().loads(resp)

    def update_uk8s_uls_config(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """UpdateUK8SULSConfig - 更新指定UK8S集群的日志采集规则。

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。请参考GetProjectList接口：https://docs.ucloud.cn/api/summary/get_project_list
        - **Region** (str) - (Config) 地域。参见地域和可用区列表：https://docs.ucloud.cn/api/summary/regionlist
        - **ClusterId** (str) - (Required) UK8S集群ID。
        - **Name** (str) - (Required) 要修改的日志采集规则名称。名称长度不能超过253个字符，只能包含小写字母、数字、破折号和点，并且必须以字母或数字开头和结尾。
        - **Zone** (str) - (Required) 可用区。参见地域和可用区列表：https://docs.ucloud.cn/api/summary/regionlist
        - **ExtractRule** (dict) - 见 **UpdateUK8SULSConfigParamExtractRule** 模型定义
        - **InputDetail** (dict) - 见 **UpdateUK8SULSConfigParamInputDetail** 模型定义
        - **MatchRule** (dict) - 见 **UpdateUK8SULSConfigParamMatchRule** 模型定义
        - **TopicID** (str) - 日志服务中用于接收日志的目标Topic ID。不填写时保持原Topic ID不变。

        **Response**

        - **Message** (str) - 返回信息。

        **Request Model**

        **UpdateUK8SULSConfigParamMatchRulePodLabelsLabels**
        - **Key** (str) - 按Pod标签匹配时，要匹配的标签Key。
        - **Value** (str) - 要匹配的标签值。
        - **ValueOperator** (str) - 标签值匹配操作符。可选值：in、notin。


        **UpdateUK8SULSConfigParamMatchRulePodLabels**
        - **Labels** (list) - 见 **UpdateUK8SULSConfigParamMatchRulePodLabelsLabels** 模型定义
        - **Namespace** (str) - 按Pod标签匹配时要匹配的命名空间。
        - **NamespaceOperator** (str) - 按Pod标签匹配时，命名空间名称的匹配操作符。可选值：in、notin。填写该参数时必须同时填写MatchRule.PodLabels.Namespace。PodLabels和Workloads不能同时设置。


        **UpdateUK8SULSConfigParamInputDetailFilePaths**
        - **File** (str) - 要采集的文件名。仅适用于container_file。
        - **Path** (str) - 日志采集路径。仅适用于container_file。


        **UpdateUK8SULSConfigParamInputDetailMetadata**
        - **Container** (str) - 要附加到日志中的容器元数据字段，多个字段使用逗号分隔。可选字段：container_name、namespace、pod_name、pod_ip、pod_uid、container_id、image_name。留空表示不采集容器元数据。
        - **Labels** (str) - 要采集的Pod标签。*表示采集所有标签，app,version表示仅采集指定标签，空字符串表示不采集标签。


        **UpdateUK8SULSConfigParamInputDetail**
        - **FilePaths** (list) - 见 **UpdateUK8SULSConfigParamInputDetailFilePaths** 模型定义
        - **Metadata** (dict) - 见 **UpdateUK8SULSConfigParamInputDetailMetadata** 模型定义
        - **Stream** (str) - 容器标准输出流类型。仅适用于container_stdout，可选值：all、stdout、stderr，默认为all。
        - **Type** (str) - 日志输入类型。可选值：container_file、container_stdout。


        **UpdateUK8SULSConfigParamMatchRuleWorkloads**
        - **Name** (str) - 工作负载名称。
        - **Namespace** (str) - 按工作负载匹配时，工作负载所在的命名空间。Workloads和PodLabels不能同时设置。
        - **Type** (str) - 工作负载类型。可选值：deployment、statefulset、daemonset、job、cronjob。


        **UpdateUK8SULSConfigParamMatchRule**
        - **Container** (str) - 要匹配的容器名称，*表示所有容器，多个名称使用逗号分隔。
        - **ContainerOperator** (str) - 容器名称匹配操作符。可选值：in、notin。填写该参数时必须同时填写MatchRule.Container。
        - **PodLabels** (dict) - 见 **UpdateUK8SULSConfigParamMatchRulePodLabels** 模型定义
        - **Workloads** (list) - 见 **UpdateUK8SULSConfigParamMatchRuleWorkloads** 模型定义


        **UpdateUK8SULSConfigParamExtractRule**
        - **BeginningRegex** (str) - 行首正则表达式。multi_line、multi_line_full_regex或multi_line_delimiter模式下，BeginningRegex和BeginningRegexBase64必须至少填写一个。
        - **BeginningRegexBase64** (str) - Base64编码的行首正则表达式。填写时优先于BeginningRegex。
        - **CollectPolicy** (str) - 采集策略。可选值：full（全量采集存量日志）、increment（从当前时间点增量采集）。默认为full。
        - **Delimiter** (str) - 分隔符。delimiter或multi_line_delimiter模式下可用。可选值：space、tab、|、;、,。
        - **DelimiterBase64** (str) - Base64编码的分隔符。填写时优先于Delimiter。
        - **Encode** (str) - 日志原文的编码格式。可选值：utf-8、gbk。默认为utf-8。
        - **Keys** (list) - 提取后的字段名。仅适用于delimiter、full_regex、multi_line_full_regex和multi_line_delimiter。
        - **LogRegex** (str) - 日志提取正则表达式。full_regex或multi_line_full_regex模式下，LogRegex和LogRegexBase64必须至少填写一个。
        - **LogRegexBase64** (str) - Base64编码的日志提取正则表达式。填写时优先于LogRegex。
        - **LogType** (str) - 日志解析类型。可选值：json、delimiter、full_regex、multi_line_full_regex、multi_line_delimiter、minimal_list、multi_line。
        - **TimeFormat** (str) - TimeKey对应的时间格式。json、full_regex或multi_line_full_regex模式下，填写TimeKey时必须同时填写TimeFormat。
        - **TimeKey** (str) - 包含日志时间的字段名。
        - **UnMatchKey** (str) - 存放无法解析的日志原文的Key。UnMatchUpload为true时必须填写。
        - **UnMatchUpload** (str) - 是否上传解析失败的日志。字符串true表示上传，false表示丢弃。默认为false。


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.UpdateUK8SULSConfigRequestSchema().dumps(d)

        resp = self.invoke("UpdateUK8SULSConfig", d, **kwargs)
        return apis.UpdateUK8SULSConfigResponseSchema().loads(resp)
