""" Code is generated by ucloud-model, DO NOT EDIT IT. """

import typing


from ucloud.core.client import Client
from ucloud.services.nlb.schemas import apis


class NLBClient(Client):
    def __init__(
        self, config: dict, transport=None, middleware=None, logger=None
    ):
        super(NLBClient, self).__init__(config, transport, middleware, logger)

    def add_nlb_targets(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """AddNLBTargets - 给监听器添加后端服务节点

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ListenerId** (str) - (Required) 监听器的ID。
        - **NLBId** (str) - (Required) 负载均衡实例的ID。
        - **Targets** (list) - 见 **AddNLBTargetsParamTargets** 模型定义

        **Response**

        - **Targets** (list) - 见 **Target** 模型定义

        **Request Model**

        **AddNLBTargetsParamTargets**
        - **Enabled** (bool) - 服务节点是否启用
        - **Port** (int) - 服务节点的端口，限定取值：[1-65535]
        - **Region** (str) - 服务节点所在地域
        - **ResourceIP** (str) - 服务节点的IP。在IP类型时，必传
        - **ResourceId** (str) - 服务节点的资源ID。在非IP类型时，必传
        - **ResourceName** (str) - 服务节点的资源名称
        - **ResourceType** (str) - 服务节点的类型。限定枚举值："UHost" / "UNI"/"UPM"/"IP"，默认值："UHost"；非IP类型，如果该资源有多个IP，将只能添加主IP；非IP类型，展示时，会显示相关资源信息，IP类型只展示IP信息。
        - **SubnetId** (str) - 服务节点的子网资源ID。
        - **VPCId** (str) - 服务节点的VPC资源ID。在IP类型时，必传
        - **Weight** (int) - 服务节点的权重。限定取值：[1-100]，默认值1；仅在加权轮询、加权最小连接数算法时有效


        **Response Model**

        **Target**
        - **Enabled** (bool) - 服务节点是否开启
        - **Id** (str) - 服务节点的标识 ID说明：添加服务节点的时候无需传更新服务节点属性时必传
        - **Port** (int) - 服务节点的端口
        - **Region** (str) - 服务节点所在地域
        - **ResourceIP** (str) - 服务节点的IP。在IP类型时，必传
        - **ResourceId** (str) - 服务节点的资源ID。在非IP类型时，必传
        - **ResourceName** (str) - 服务节点的资源名称
        - **ResourceType** (str) - 服务节点的类型
        - **State** (str) - 服务节点的健康检查状态说明：描述服务节点信息时显示限定枚举值："Healthy"/"Unhealthy"
        - **SubnetId** (str) - 服务节点的子网资源ID。在IP类型时，必传
        - **VPCId** (str) - 服务节点的VPC资源ID。在IP类型时，必传
        - **Weight** (int) - 服务节点的权重。支持更新


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.AddNLBTargetsRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("AddNLBTargets", d, **kwargs)
        return apis.AddNLBTargetsResponseSchema().loads(resp)

    def create_nlb_listener(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CreateNLBListener - 创建监听器

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **NLBId** (str) - (Required) 负载均衡实例的ID
        - **Protocol** (str) - (Required) 监听协议限定取值："TCP"/"UDP"
        - **EndPort** (int) - 端口范围的结束端口限定取值：[1-65535]取值不小于起始端口默认值 65535
        - **ForwardSrcIPMethod** (str) - 传递源 IP 方法。限定取值："" / "None" / "Toa"/"ProxyProto"，空字符串和 None 代表关闭。
        - **HealthCheckConfig** (dict) - 见 **CreateNLBListenerParamHealthCheckConfig** 模型定义
        - **Name** (str) - 监听器的名称限定字符长度：[1-255]限定特殊字符，仅支持：-_.默认值：listener
        - **Remark** (str) - 监听器的备注信息限定字符长度：[0-255]
        - **Scheduler** (str) - 负载均衡算法限定取值："RoundRobin"/"SourceHash"/"LeastConn"/"WeightLeastConn "/"WeightRoundRobin"默认值 "RoundRobin"
        - **StartPort** (int) - 端口范围的起始端口限定取值：[1-65535]默认值 1
        - **StickinessTimeout** (int) - 会话保持超时时间。单位：秒限定取值：[60-900]，0 表示不开启会话保持默认值60

        **Response**

        - **ListenerId** (str) - 监听器的ID

        **Request Model**

        **CreateNLBListenerParamHealthCheckConfig**
        - **Enabled** (bool) - 是否开启健康检查功能。暂时不支持关闭，默认 true
        - **Port** (int) - 健康检查探测端口 说明： 限定取值：[1-65535]，Ping 探测下，端口可填 0
        - **ReqMsg** (str) - UDP" 检查模式的请求字符串
        - **ResMsg** (str) - "UDP" 检查模式的预期响应字符串
        - **Type** (str) - 健康检查方式 限定取值："Port"/"UDP"/"Ping" 默认值：“Port”，Ping 只允许 UDP 协议监听器设置


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.CreateNLBListenerRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateNLBListener", d, **kwargs)
        return apis.CreateNLBListenerResponseSchema().loads(resp)

    def create_network_load_balancer(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CreateNetworkLoadBalancer - 创建网络型负载均衡

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **SubnetId** (str) - (Required) 负载均衡实例所属的子网资源ID
        - **VPCId** (str) - (Required) 载均衡实例所属的VPC资源ID
        - **ApiVersion** (str) - API 版本，限定取值 "v1.0"/"v2.0"，默认值："v2.0"
        - **ChargeType** (str) - 付费模式，限定取值："Dynamic"/"Month"/"Year"
        - **CouponId** (str) - 代金券code
        - **IPVersion** (str) - 负载均衡实例的IP协议，限定取值："IPv4"/"IPv6"/"DualStack"，默认值："IPv4"
        - **Name** (str) - 负载均衡实例的名称限定字符长度：[1-255]限定特殊字符，仅支持：-_.默认值：nlb
        - **Quantity** (int) - 购买的时长，ChargeType = "Month"，Quantity = 0 代表购买到月底，其余情况必须赋值
        - **Remark** (str) - 负载均衡实例的备注信息限定字符长度：[0-255]
        - **Tag** (str) - 负载均衡实例所属的业务组ID

        **Response**

        - **Message** (str) - 返回信息
        - **NLBId** (str) - 返回的NLBId

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.CreateNetworkLoadBalancerRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateNetworkLoadBalancer", d, **kwargs)
        return apis.CreateNetworkLoadBalancerResponseSchema().loads(resp)

    def delete_nlb_listener(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DeleteNLBListener - 删除一个网络型负载均衡监听器

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ListenerId** (str) - (Required) 监听器的ID
        - **NLBId** (str) - (Required) 负载均衡实例的ID

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DeleteNLBListenerRequestSchema().dumps(d)

        resp = self.invoke("DeleteNLBListener", d, **kwargs)
        return apis.DeleteNLBListenerResponseSchema().loads(resp)

    def delete_network_load_balancer(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DeleteNetworkLoadBalancer - 删除负载均衡实例

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **NLBId** (str) - (Required) 负载均衡实例的ID
        - **ReleaseEIP** (bool) - 删除NLB时释放绑定的EIP

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DeleteNetworkLoadBalancerRequestSchema().dumps(d)

        resp = self.invoke("DeleteNetworkLoadBalancer", d, **kwargs)
        return apis.DeleteNetworkLoadBalancerResponseSchema().loads(resp)

    def describe_nlb_listeners(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DescribeNLBListeners - 描述监听器

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **NLBId** (str) - (Required) 负载均衡实例的ID。未指定 ListenerId ，则描述指定的 LoadBalancerId 下的所有监听器
        - **Limit** (int) - 限制返回的监听器数量
        - **ListenerId** (str) - 监听器的ID
        - **Offset** (int) - 设置监听器的偏移量

        **Response**

        - **Listeners** (list) - 见 **Listener** 模型定义
        - **TotalCount** (int) - 全部个数

        **Response Model**

        **Target**
        - **Enabled** (bool) - 服务节点是否开启
        - **Id** (str) - 服务节点的标识 ID说明：添加服务节点的时候无需传更新服务节点属性时必传
        - **Port** (int) - 服务节点的端口
        - **Region** (str) - 服务节点所在地域
        - **ResourceIP** (str) - 服务节点的IP。在IP类型时，必传
        - **ResourceId** (str) - 服务节点的资源ID。在非IP类型时，必传
        - **ResourceName** (str) - 服务节点的资源名称
        - **ResourceType** (str) - 服务节点的类型
        - **State** (str) - 服务节点的健康检查状态说明：描述服务节点信息时显示限定枚举值："Healthy"/"Unhealthy"
        - **SubnetId** (str) - 服务节点的子网资源ID。在IP类型时，必传
        - **VPCId** (str) - 服务节点的VPC资源ID。在IP类型时，必传
        - **Weight** (int) - 服务节点的权重。支持更新


        **HealthCheckConfig**
        - **Enabled** (bool) - 是否开启健康检查功能。
        - **Interval** (int) - 健康检查间隔时间限定取值：[1-60] 单位秒默认 2s
        - **MaxFail** (int) - 健康检查最大失败数限定取值：[1-10] 默认 3
        - **MinSuccess** (int) - 健康检查最小成功数限定取值：[1-10] 默认 3
        - **Port** (int) - 健康检查探测端口说明：限定取值：[1-65535]
        - **ReqMsg** (str) - UDP" 检查模式的请求字符串"HTTP" 检查模式的请求 json 字符串
        - **ResMsg** (str) - "UDP" 检查模式的预期响应字符串"HTTP" 检查模式的响应状态码
        - **Type** (str) - 健康检查方式限定取值："Port"/"UDP"/"Ping" /"HTTP"默认值：“Port”


        **Listener**
        - **DeletionProtection** (bool) - 是否开启删除保护
        - **EndPort** (int) - 端口范围的结束端口
        - **ForwardSrcIPMethod** (str) - 传递源 IP 方法。限定取值："" / "None" / "Toa"/"ProxyProto"，空字符串和 None 代表关闭。
        - **HealthCheckConfig** (dict) - 见 **HealthCheckConfig** 模型定义
        - **ListenerId** (str) - 监听器的ID
        - **Name** (str) - 监听器的名称
        - **Protocol** (str) - 监听协议，限定取值："TCP"/"UDP"
        - **Remark** (str) - 监听器的备注信息
        - **Scheduler** (str) - 负载均衡算法，限定取值："RoundRobin"/"SourceHash"/"LeastConn"/"WeightLeastConn "/"WeightRoundRobin "
        - **StartPort** (int) - 端口范围的起始端口
        - **State** (str) - listener 健康状态，"Healthy"/"Unhealthy"/"PartialHealth"/"None"
        - **StickinessTimeout** (int) - 会话保持超时时间。单位：秒，0表示不开启会话保持
        - **Targets** (list) - 见 **Target** 模型定义


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DescribeNLBListenersRequestSchema().dumps(d)

        resp = self.invoke("DescribeNLBListeners", d, **kwargs)
        return apis.DescribeNLBListenersResponseSchema().loads(resp)

    def describe_network_load_balancers(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DescribeNetworkLoadBalancers - 描述负载均衡实例

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Limit** (int) - 数据分页值，默认为100
        - **NLBIds** (list) - 负载均衡实例的ID，数组
        - **Offset** (int) - 数据偏移量，默认为0
        - **ShowDetail** (bool) - 是否获取监听器和后端服务节点的详细信息
        - **SubnetId** (str) - 限定所在的子网
        - **VPCId** (str) - 限定所在的VPC

        **Response**

        - **NLBs** (list) - 见 **NetworkLoadBalancer** 模型定义
        - **TotalCount** (int) - 满足条件的负载均衡实例总数

        **Response Model**

        **HealthCheckConfig**
        - **Enabled** (bool) - 是否开启健康检查功能。
        - **Interval** (int) - 健康检查间隔时间限定取值：[1-60] 单位秒默认 2s
        - **MaxFail** (int) - 健康检查最大失败数限定取值：[1-10] 默认 3
        - **MinSuccess** (int) - 健康检查最小成功数限定取值：[1-10] 默认 3
        - **Port** (int) - 健康检查探测端口说明：限定取值：[1-65535]
        - **ReqMsg** (str) - UDP" 检查模式的请求字符串"HTTP" 检查模式的请求 json 字符串
        - **ResMsg** (str) - "UDP" 检查模式的预期响应字符串"HTTP" 检查模式的响应状态码
        - **Type** (str) - 健康检查方式限定取值："Port"/"UDP"/"Ping" /"HTTP"默认值：“Port”


        **Target**
        - **Enabled** (bool) - 服务节点是否开启
        - **Id** (str) - 服务节点的标识 ID说明：添加服务节点的时候无需传更新服务节点属性时必传
        - **Port** (int) - 服务节点的端口
        - **Region** (str) - 服务节点所在地域
        - **ResourceIP** (str) - 服务节点的IP。在IP类型时，必传
        - **ResourceId** (str) - 服务节点的资源ID。在非IP类型时，必传
        - **ResourceName** (str) - 服务节点的资源名称
        - **ResourceType** (str) - 服务节点的类型
        - **State** (str) - 服务节点的健康检查状态说明：描述服务节点信息时显示限定枚举值："Healthy"/"Unhealthy"
        - **SubnetId** (str) - 服务节点的子网资源ID。在IP类型时，必传
        - **VPCId** (str) - 服务节点的VPC资源ID。在IP类型时，必传
        - **Weight** (int) - 服务节点的权重。支持更新


        **Listener**
        - **EndPort** (int) - 端口范围的结束端口
        - **ForwardSrcIPMethod** (str) - 传递源 IP 方法。限定取值："" / "None" / "Toa"/"ProxyProto"，空字符串和 None 代表关闭。
        - **HealthCheckConfig** (dict) - 见 **HealthCheckConfig** 模型定义
        - **ListenerId** (str) - 监听器的ID
        - **Name** (str) - 监听器的名称
        - **Protocol** (str) - 监听协议，限定取值："TCP"/"UDP"
        - **Remark** (str) - 监听器的备注信息
        - **Scheduler** (str) - 负载均衡算法，限定取值："RoundRobin"/"SourceHash"/"LeastConn"/"WeightLeastConn "/"WeightRoundRobin "
        - **StartPort** (int) - 端口范围的起始端口
        - **State** (str) - listener 健康状态，"Healthy"/"Unhealthy"/"PartialHealth"/"None"
        - **StickinessTimeout** (int) - 会话保持超时时间。单位：秒，0表示不开启会话保持
        - **Targets** (list) - 见 **Target** 模型定义


        **IPInfo**
        - **AddressType** (str) - 网络模式，限定枚举值："Internet" / "Intranet"
        - **Bandwidth** (int) - 带宽值。单位 M
        - **BandwidthType** (int) - 带宽类型，限定枚举值：0（普通带宽）/ 1（共享带宽）
        - **IP** (str) - IP 地址
        - **IPVersion** (str) - IP协议版本，限定枚举值："IPv4" / "IPv6"
        - **Id** (str) - 唯一标识 ID
        - **OperatorName** (str) - 外网IP的运营商信息，限定枚举值："Telecom" / "Unicom"/"International"/"Bgp"/"Duplet"/"BGPPro"/"China-mobile"/"Anycast"
        - **Type** (int) - IP 类型，1（前向 IP）/ 2（后向 IP）


        **NetworkLoadBalancer**
        - **AutoRenewEnabled** (bool) - 是否开启自动续费
        - **ChargeType** (str) - 付费模式
        - **CreateTime** (int) - 负载均衡实例创建时间。格式为 Unix Timestamp
        - **ForwardingMode** (str) - 负载均衡实例的转发模式
        - **IPInfos** (list) - 见 **IPInfo** 模型定义
        - **IPVersion** (str) - 负载均衡实例支持的IP协议版本
        - **Listeners** (list) - 见 **Listener** 模型定义
        - **NLBId** (str) - 负载均衡实例的ID
        - **Name** (str) - 负载均衡实例的名称
        - **PurchaseValue** (int) - 有效期（计费）
        - **Remark** (str) - 负载均衡实例的备注信息
        - **Status** (str) - NLB 状态：Normal-正常；Closed-欠费停服；Deleted 已删除
        - **SubnetId** (str) - 负载均衡实例所属的子网资源ID
        - **Tag** (str) - 负载均衡实例所属的业务组ID
        - **VPCId** (str) - 负载均衡实例所属的VPC资源ID


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DescribeNetworkLoadBalancersRequestSchema().dumps(d)

        resp = self.invoke("DescribeNetworkLoadBalancers", d, **kwargs)
        return apis.DescribeNetworkLoadBalancersResponseSchema().loads(resp)

    def get_network_load_balancer_price(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetNetworkLoadBalancerPrice - 获取负载均衡价格

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ChargeType** (str) - 付费模式限定枚举值："Year" / "Month"/ "Dynamic"默认获取三种价格
        - **PayMode** (str) - 负载均衡实例计费方式限定枚举值："Instance" / "LCU"默认值："Instance"
        - **Quantity** (int) - 购买时长按小时购买（Dynamic）时无需此参数。月付时，此参数传 0，代表了购买至月末默认 1

        **Response**

        - **Prices** (list) - 见 **PriceDetail** 模型定义

        **Response Model**

        **PriceDetail**
        - **ChargeType** (str) - 负载均衡付费方式
        - **CustomPrice** (float) - 用户折后价，单位“元”。CustomPrice=OriginalPrice*用户折扣
        - **OriginalPrice** (float) - 购买负载均衡的原价，单位“元”
        - **Price** (float) - 购买负载均衡的实际价格，单位“元”
        - **PurchaseValue** (int) - 资源有效期，以Unix Timestamp表示


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GetNetworkLoadBalancerPriceRequestSchema().dumps(d)

        resp = self.invoke("GetNetworkLoadBalancerPrice", d, **kwargs)
        return apis.GetNetworkLoadBalancerPriceResponseSchema().loads(resp)

    def remove_nlb_targets(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """RemoveNLBTargets - 删除后端服务节点

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Ids** (list) - (Required) 服务节点的标识ID。单次请求不能超过 40 个
        - **ListenerId** (str) - (Required) 监听器的ID
        - **NLBId** (str) - (Required) 负载均衡实例的ID

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.RemoveNLBTargetsRequestSchema().dumps(d)

        resp = self.invoke("RemoveNLBTargets", d, **kwargs)
        return apis.RemoveNLBTargetsResponseSchema().loads(resp)

    def update_nlb_listener_attribute(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """UpdateNLBListenerAttribute - 更新监听器属性

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ListenerId** (str) - (Required) 负载均衡实例的ID
        - **NLBId** (str) - (Required) 负载均衡实例的ID
        - **EndPort** (int) - 端口范围的结束端口 限定取值：[1-65535] 取值不小于起始端口 默认值 65535，只有全端口模式支持修改
        - **ForwardSrcIPMethod** (str) - 传递源 IP 方法。限定取值："" / "None" / "Toa"，空字符串和 None 代表关闭。
        - **HealthCheckConfig** (dict) - 见 **UpdateNLBListenerAttributeParamHealthCheckConfig** 模型定义
        - **Name** (str) - 监听器的名称
        - **Remark** (str) - 监听器的备注信息
        - **Scheduler** (str) - 负载均衡算法 限定取值："RoundRobin"/"SourceHash"/"LeastConn"/"WeightLeastConn "/"WeightRoundRobin" 默认值 "RoundRobin"
        - **StartPort** (int) - 端口范围的起始端口 限定取值：[1-65535] 默认值 1，只有全端口模式支持修改
        - **StickinessTimeout** (int) - 会话保持超时时间。单位：秒说明：限定取值：[60-900]，0 表示不开启会话保持默认值60

        **Response**


        **Request Model**

        **UpdateNLBListenerAttributeParamHealthCheckConfig**
        - **Enabled** (bool) - 是否开启健康检查功能。暂时不支持关闭，默认 true
        - **Port** (int) - 健康检查探测端口 说明： 限定取值：[1-65535]
        - **ReqMsg** (str) - UDP" 检查模式的请求字符串
        - **ResMsg** (str) - "UDP" 检查模式的预期响应字符串
        - **Type** (str) - 健康检查方式 限定取值："Port"/"UDP"/"Ping" 默认值：“Port”


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.UpdateNLBListenerAttributeRequestSchema().dumps(d)

        resp = self.invoke("UpdateNLBListenerAttribute", d, **kwargs)
        return apis.UpdateNLBListenerAttributeResponseSchema().loads(resp)

    def update_nlb_targets_attribute(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """UpdateNLBTargetsAttribute - 更新后端服务节点属性

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ListenerId** (str) - (Required) 监听器的ID
        - **NLBId** (str) - (Required) 负载均衡实例的ID
        - **Targets** (list) - 见 **UpdateNLBTargetsAttributeParamTargets** 模型定义

        **Response**


        **Request Model**

        **UpdateNLBTargetsAttributeParamTargets**
        - **Enabled** (bool) - 是否禁用服务节点
        - **Id** (str) - 服务节点的ID
        - **Weight** (int) - 服务节点的权重。限定取值：[1-100]，默认值1；仅在加权轮询、加权最小连接数算法时有效


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.UpdateNLBTargetsAttributeRequestSchema().dumps(d)

        resp = self.invoke("UpdateNLBTargetsAttribute", d, **kwargs)
        return apis.UpdateNLBTargetsAttributeResponseSchema().loads(resp)

    def update_network_load_balancer_attribute(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """UpdateNetworkLoadBalancerAttribute - 更新负载均衡实例属性

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **NLBId** (str) - (Required) 负载均衡实例的ID
        - **Name** (str) - 负载均衡实例的名称
        - **Remark** (str) - 负载均衡实例的备注信息
        - **Tag** (str) - 负载均衡实例所属的业务组ID

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.UpdateNetworkLoadBalancerAttributeRequestSchema().dumps(d)

        resp = self.invoke("UpdateNetworkLoadBalancerAttribute", d, **kwargs)
        return apis.UpdateNetworkLoadBalancerAttributeResponseSchema().loads(
            resp
        )
