""" Code is generated by ucloud-model, DO NOT EDIT IT. """

import typing


from ucloud.core.client import Client
from ucloud.services.cloudwatch.schemas import apis


class CloudWatchClient(Client):
    def __init__(
        self, config: dict, transport=None, middleware=None, logger=None
    ):
        super(CloudWatchClient, self).__init__(
            config, transport, middleware, logger
        )

    def bind_alert_strategy(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """BindAlertStrategy - 绑定告警策略

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **AlertStrategyIDs** (list) - (Required) 告警策略id数组
        - **ProductKey** (str) - (Required) 产品唯一标识，参见  `产品概览 <https://docs.ucloud.cn/cloudwatch/metric/intro>`_
        - **Resources** (list) - (Required) 资源数组

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.BindAlertStrategyRequestSchema().dumps(d)

        resp = self.invoke("BindAlertStrategy", d, **kwargs)
        return apis.BindAlertStrategyResponseSchema().loads(resp)

    def create_alert_strategy(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CreateAlertStrategy - 创建告警策略

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **ConfigMode** (int) - (Required) 条件设置方式1 - 手动配置 2 - 选择模版
        - **Name** (str) - (Required) 告警策略名称。最大长度255个字符
        - **ObjectType** (int) - (Required) 绑定资源类型1 - 资源组 2 - 资源
        - **ProductKey** (str) - (Required) 产品唯一标识，参见  `产品概览 <https://docs.ucloud.cn/cloudwatch/metric/intro>`_
        - **CallbackLanguage** (str) - 当通知渠道=回调webhook时，需要设置回调语言。枚举值：cn - 中文en - 英文
        - **CallbackUrls** (list) - 回调URL地址
        - **NotifyChannelDs** (list) - 通知渠道。枚举值：sms - 短信email - 邮件webhook - 回调
        - **NotifyGroupIDs** (list) - 通知人组id
        - **NotifyTemplateId** (str) - 通知模板id
        - **NotifyType** (str) - 通知类型。枚举值： group - 通知组 user - 通知人
        - **NotifyUserIDs** (list) - 通知人id
        - **ResourceGroupIDs** (list) - 绑定资源组，对应绑定资源类型ObjectType=1
        - **Resources** (list) - 绑定资源，对应绑定资源类型ObjectType=2
        - **RuleSet** (list) - 见 **CreateAlertStrategyParamRuleSet** 模型定义
        - **TemplateId** (int) - 模板id.对应ConfigMode=2时候需要填写

        **Response**

        - **Data** (dict) - 见 **AlertStrategyId** 模型定义

        **Request Model**

        **CreateAlertStrategyParamRuleSet**
        - **Level** (str) - 告警等级。枚举值：P0,P1,P2,P3
        - **MetricID** (int) - 规则指标ID。参考该类型产品下返回的指标列表GetProductMetrics
        - **SendInterval** (int) - 沉默周期(告警周期选择为连续时必填)
        - **SendPeriodType** (str) - 触发周期。枚举值continuous - 连续exponent - 指数single - 不重复
        - **Status** (int) - 告警状态。枚举值0 - 关闭1 - 开启
        - **ThresholdCompare** (int) - 阈值比较方式。 枚举值：1：>= 2：<= 3：> 4：< 5：== 6：!=
        - **ThresholdValue** (int) - 触发阈值
        - **TriggerCount** (int) - 触发次数


        **Response Model**

        **AlertStrategyId**
        - **AlertStrategyID** (int) - 告警策略id


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.CreateAlertStrategyRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateAlertStrategy", d, **kwargs)
        return apis.CreateAlertStrategyResponseSchema().loads(resp)

    def create_alert_strategy_template(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CreateAlertStrategyTemplate - 新建条件模板

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Name** (str) - (Required) 告警模板名称。最大长度64个字符
        - **ProductKey** (str) - (Required) 产品唯一标识，参见  `产品概览 <https://docs.ucloud.cn/cloudwatch/metric/intro>`_
        - **Remark** (str) - 备注
        - **RuleSet** (list) - 见 **CreateAlertStrategyTemplateParamRuleSet** 模型定义

        **Response**

        - **Data** (dict) - 见 **AlertTemplate** 模型定义

        **Request Model**

        **CreateAlertStrategyTemplateParamRuleSet**
        - **Level** (str) - 告警等级。枚举值：P0,P1,P2,P3
        - **MetricID** (int) - 规则指标ID。参考该类型产品下返回的指标列表GetProductMetrics
        - **SendInterval** (int) - 沉默周期(告警周期选择为连续时必填)
        - **SendPeriodType** (str) - 触发周期。枚举值：continuous连续 exponent 指数 single 不重复
        - **Status** (int) - 告警状态。枚举值：0-关闭 1-开启
        - **ThresholdCompare** (int) - 阈值比较方式。枚举值: 1：>= 2：<= 3：> 4：< 5：== 6：!=
        - **ThresholdValue** (int) - 触发阈值
        - **TriggerCount** (int) - 触发次数


        **Response Model**

        **AlertTemplate**
        - **TemplateID** (int) - 告警模板ID


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.CreateAlertStrategyTemplateRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateAlertStrategyTemplate", d, **kwargs)
        return apis.CreateAlertStrategyTemplateResponseSchema().loads(resp)

    def delete_alert_strategy_template(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DeleteAlertStrategyTemplate - 删除告警条件模板

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **TemplateID** (list) - (Required) 告警模板ID(支持批量删除)

        **Response**

        - **Data** (dict) - 见 **DelAlertTemplate** 模型定义

        **Response Model**

        **DelAlertTemplate**
        - **TemplateID** (list) - 模板ID


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.DeleteAlertStrategyTemplateRequestSchema().dumps(d)

        resp = self.invoke("DeleteAlertStrategyTemplate", d, **kwargs)
        return apis.DeleteAlertStrategyTemplateResponseSchema().loads(resp)

    def enable_alert_strategy(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """EnableAlertStrategy - 是否启用告警策略

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **AlertStrategyID** (int) - (Required) 告警策略id
        - **Status** (int) - (Required) 启用状态0 停用 1 启用

        **Response**

        - **Data** (dict) - 见 **AlertStrategyId** 模型定义

        **Response Model**

        **AlertStrategyId**
        - **AlertStrategyID** (int) - 告警策略id


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.EnableAlertStrategyRequestSchema().dumps(d)

        resp = self.invoke("EnableAlertStrategy", d, **kwargs)
        return apis.EnableAlertStrategyResponseSchema().loads(resp)

    def get_metric_data_aggregation_method(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetMetricDataAggregationMethod - 获取指标数据聚合方式

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Zone** (str) - 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Data** (dict) - 见 **GetMetricDataAggregationMethodResp** 模型定义

        **Response Model**

        **MetricDataAggregationMethod**
        - **Description** (str) - 描述
        - **IsDefault** (bool) - 是否是默认方式
        - **Label** (str) - 标签
        - **Value** (str) - 值


        **GetMetricDataAggregationMethodResp**
        - **List** (list) - 见 **MetricDataAggregationMethod** 模型定义


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GetMetricDataAggregationMethodRequestSchema().dumps(d)

        resp = self.invoke("GetMetricDataAggregationMethod", d, **kwargs)
        return apis.GetMetricDataAggregationMethodResponseSchema().loads(resp)

    def get_product_metrics(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetProductMetrics - 获取云产品关联的指标列表

        **Request**

        - **ProductKey** (str) - (Required) 产品唯一标识，参见  `产品概览 <https://docs.ucloud.cn/cloudwatch/metric/intro>`_

        **Response**

        - **Data** (dict) - 见 **GetProductMetricsRespData** 模型定义

        **Response Model**

        **ConversionRule**
        - **ConversionFactor** (int) - 转换因子
        - **From** (str) - 来源
        - **To** (str) - 目标


        **MetricUnitConfig**
        - **ConversionFactor** (int) - 转换因子
        - **ConversionRules** (list) - 见 **ConversionRule** 模型定义
        - **UnitCnNames** (list) - 指标中文名列表
        - **UnitEnNames** (list) - 指标英文名列表


        **MetricUnit**
        - **ConversionFactor** (int) - 转换因子
        - **CreatedAt** (str) - 创建时间
        - **CreatedBy** (str) - 创建人
        - **DeletedAt** (int) - 删除时间
        - **GroupId** (int) - GroupId
        - **UnitChName** (str) - 单位中文名称
        - **UnitDesc** (str) - 单位描述
        - **UnitEnName** (str) - 单位英文名称
        - **UnitID** (int) - 单位id
        - **UpdatedAt** (str) - 修改时间
        - **UpdatedBy** (str) - 修改人


        **Metirc**
        - **CreatedAt** (str) - 创建时间
        - **CreatedBy** (str) - 创建者
        - **FrequencyMs** (int) - 上报频率毫秒
        - **Metric** (str) - 指标唯一标识 (uhost_cpu_usage)
        - **MetricChDesc** (str) - 指标中文描述
        - **MetricChName** (str) - 指标中文名称
        - **MetricEnDesc** (str) - 指标英文描述
        - **MetricEnName** (str) - 指标英文名称
        - **MetricGroup** (str) - 指标分类/指标组
        - **MetricID** (int) - 指标ID
        - **ProductType** (int) - 云产品ID
        - **Unit** (dict) - 见 **MetricUnit** 模型定义
        - **UnitID** (int) - 单位ID
        - **UpdatedAt** (str) - 修改时间
        - **UpdatedBy** (str) - 修改者


        **GetProductMetricsRespData**
        - **List** (list) - 见 **Metirc** 模型定义
        - **Total** (int) - 查询结果总数
        - **UnitConfigs** (list) - 见 **MetricUnitConfig** 模型定义


        """
        # build request
        d = {}
        req and d.update(req)
        d = apis.GetProductMetricsRequestSchema().dumps(d)

        resp = self.invoke("GetProductMetrics", d, **kwargs)
        return apis.GetProductMetricsResponseSchema().loads(resp)

    def list_alert_record(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListAlertRecord - 获取时间段内的告警记录

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **EndAt** (int) - (Required) 结束时间，查询告警记录结束时间(查询开始时间和结束时间不能超过一个月)，值为10位数时间戳
        - **StartAt** (int) - (Required) 开始时间，查询告警记录开始时间，不支持查询距当前时间一年前的数据)，值为10位数时间戳
        - **Filter** (dict) - 见 **ListAlertRecordParamFilter** 模型定义
        - **Fuzzy** (str) - 模糊查询(支持资源id模糊搜索)
        - **Limit** (int) - 查询返回数量，默认值300，最大值：300。
        - **Offset** (int) - 数据偏移量 (默认0)
        - **OrderType** (str) - 排序(默认根据告警发生时间倒序)，枚举值：asc-升序，desc-降序

        **Response**

        - **Data** (list) - 见 **AlertRecord** 模型定义
        - **TotalCount** (int) - 告警记录总数

        **Request Model**

        **ListAlertRecordParamFilter**
        - **Levels** (list) - 告警级别，根据告警级别精确搜索对应的告警记录，枚举值:P0,P1,P2,P3
        - **ProductTypes** (list) - 产品ID，根据产品类型精确搜索对应的告警记录，参见  `产品概览 <https://docs.ucloud.cn/cloudwatch/metric/intro>`_
        - **Status** (list) - 告警状态，根据告警状态精确搜索对应的告警记录，枚举值：firing-告警中，resolved-已恢复


        **Response Model**

        **AlertRecord**
        - **ContentAttrList** (list) - 产品相关的额外属性列表
        - **EndAt** (int) - 告警结束时间
        - **Level** (str) - 告警等级
        - **MetricID** (int) - 指标id
        - **MetricName** (str) - 指标名称
        - **ProductName** (str) - 产品类型名称
        - **ProductType** (int) - 产品类型
        - **ProjectID** (int) - 项目ProjectID
        - **RecordID** (int) - 告警记录RecordID
        - **Region** (str) - 可用区
        - **ResourceID** (str) - 资源id
        - **RuleID** (int) - 告警记录触发告警规则Id
        - **ShieldRuleID** (int) - 告警屏蔽规则id(如果配置了屏蔽规则，并且满足条件)
        - **StartAt** (int) - 告警触发时间
        - **Status** (str) - 告警状态
        - **StrategyID** (int) - 告警记录触发告警策略Id
        - **StrategyName** (str) - 告警记录触发告警策略名称
        - **Tag** (list) - 告警点tag信息
        - **ThresholdCompare** (int) - 比较符
        - **ThresholdValue** (int) - 告警阈值
        - **UnitName** (str) - 指标单位名称
        - **Value** (int) - 告警当前值


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.ListAlertRecordRequestSchema().dumps(d)

        resp = self.invoke("ListAlertRecord", d, **kwargs)
        return apis.ListAlertRecordResponseSchema().loads(resp)

    def list_alert_strategy(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListAlertStrategy - 获取告警策略列表

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Filter** (dict) - 见 **ListAlertStrategyParamFilter** 模型定义
        - **Fuzzy** (str) - 模糊查询(支持告警策略名称模糊搜索)
        - **Limit** (int) - 查询返回数量，默认值300，最大值：300。
        - **Offset** (int) - 数据偏移量 (默认0)
        - **Resources** (list) - 资源id集合,根据资源id返回绑定的告警策略列表

        **Response**

        - **Data** (list) - 见 **AlertStrategy** 模型定义
        - **Message** (str) - 错误信息
        - **TotalCount** (int) - 告警策略总数
        - **TraceId** (str) - 链路ID

        **Request Model**

        **ListAlertStrategyParamFilter**
        - **AlertStrategyIDs** (list) - 告警策略id，根据策略id获取告警策略列表
        - **ProductTypes** (list) - 产品ID，参见  `产品概览 <https://docs.ucloud.cn/cloudwatch/metric/intro>`_
        - **Status** (list) - 告警策略状态，根据告警策略状态精确搜索对应的告警策略，枚举值：0-停用，1-启用


        **Response Model**

        **AlertRule**
        - **Level** (str) - 告警等级。枚举值：P0,P1,P2,P3
        - **MetricID** (int) - 规则指标ID。参考该类型产品下返回的指标列表GetProductMetrics
        - **MetricName** (str) - 指标名称
        - **RuleID** (int) - 规则ID
        - **SendInterval** (int) - 发送间隔
        - **SendPeriodType** (str) - 触发周期。枚举值：continuous连续 exponent 指数 single 不重复
        - **Status** (int) - 告警状态。枚举值：0-关闭 1-开启
        - **ThresholdCompare** (int) - 阈值比较方式枚举值比较方式:1->=2-<=3->4-<5-==6-!=
        - **ThresholdValue** (float) - 触发阈值
        - **TriggerCount** (int) - 触发次数
        - **UnitID** (int) - 单位id
        - **UnitName** (str) - 单位名称


        **AlertStrategy**
        - **AlertStrategyID** (int) - 告警策略Id
        - **CallbackLanguage** (str) - 回调语言(cn,en)
        - **CallbackUrls** (list) - 回调地址
        - **ConfigMode** (int) - 告警规则配置类型(基于模板配置、基于手工配置)
        - **CreatedAt** (int) - 创建时间
        - **CreatedBy** (str) - 创建人
        - **Name** (str) - 告警策略名称
        - **NotifyChannelDs** (list) - 通知渠道('email', 'sms', 'webhook')
        - **NotifyGroupIDs** (list) - 通知组id集合
        - **NotifyType** (str) - 通知方式(通知组：group，通知人：user)
        - **NotifyUserIDs** (list) - 通知人用户id集合
        - **ProductKey** (str) - 资源类型名称
        - **ProductType** (int) - 资源类型
        - **Remark** (str) - 告警策略备注
        - **RuleSet** (list) - 见 **AlertRule** 模型定义
        - **Status** (int) - 告警策略状态
        - **TemplateId** (int) - 模板Id
        - **UpdatedAt** (int) - 更新时间
        - **UpdatedBy** (str) - 更新人


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.ListAlertStrategyRequestSchema().dumps(d)

        resp = self.invoke("ListAlertStrategy", d, **kwargs)
        return apis.ListAlertStrategyResponseSchema().loads(resp)

    def list_alert_strategy_template(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListAlertStrategyTemplate - 条件模板列表

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Filter** (dict) - 见 **ListAlertStrategyTemplateParamFilter** 模型定义
        - **Limit** (int) - 查询返回数量，默认值300，最大值：300。
        - **Offset** (int) - 数据偏移量 (默认0)

        **Response**

        - **Data** (list) - 见 **ListAlertTemplate** 模型定义
        - **Message** (str) - 错误信息
        - **TotalCount** (int) - 条件模板总条数

        **Request Model**

        **ListAlertStrategyTemplateParamFilter**
        - **TemplateIDs** (list) - 模板id集合，根据模板id获取告警条件模板列表


        **Response Model**

        **AlertRule**
        - **Level** (str) - 告警等级。枚举值：P0,P1,P2,P3
        - **MetricID** (int) - 规则指标ID。参考该类型产品下返回的指标列表GetProductMetrics
        - **MetricName** (str) - 指标名称
        - **RuleID** (int) - 规则ID
        - **SendInterval** (int) - 发送间隔
        - **SendPeriodType** (str) - 触发周期。枚举值：continuous连续 exponent 指数 single 不重复
        - **Status** (int) - 告警状态。枚举值：0-关闭 1-开启
        - **ThresholdCompare** (int) - 阈值比较方式枚举值比较方式:1->=2-<=3->4-<5-==6-!=
        - **ThresholdValue** (float) - 触发阈值
        - **TriggerCount** (int) - 触发次数
        - **UnitID** (int) - 单位id
        - **UnitName** (str) - 单位名称


        **ListAlertTemplate**
        - **CompanyID** (int) - 公司id
        - **Name** (str) - 告警模板名称
        - **ProductKey** (str) - 产品类型(字符型)。参考ListMonitorProduct获取监控对象类型列表
        - **ProductType** (int) - 产品类型(数值型)。参考ListMonitorProduct获取监控对象类型列表
        - **Remark** (str) - 条件模板备注
        - **RuleSet** (list) - 见 **AlertRule** 模型定义
        - **TemplateID** (int) - 模板Id


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.ListAlertStrategyTemplateRequestSchema().dumps(d)

        resp = self.invoke("ListAlertStrategyTemplate", d, **kwargs)
        return apis.ListAlertStrategyTemplateResponseSchema().loads(resp)

    def list_monitor_product(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListMonitorProduct - 获取监控对象类型列表

        **Request**


        **Response**

        - **Data** (dict) - 见 **ListMonitorProduct** 模型定义
        - **Message** (str) - 错误信息
        - **TraceId** (str) - 链路ID

        **Response Model**

        **Product**
        - **Id** (int) - ID
        - **Metas** (str) - {Type: 1|2, Key:string, Name: string}[] -> JSON字符串
        - **ProductChName** (str) - 产品中文名称
        - **ProductEnName** (str) - 产品英文名称
        - **ProductGroup** (str) - 产品分组
        - **ProductKey** (str) - 资源类型唯一key
        - **ProductName** (str) - 产品名称
        - **ProductName1** (str) - 产品子名称
        - **ProductType** (int) - 资源类型ID


        **ListMonitorProduct**
        - **List** (list) - 见 **Product** 模型定义
        - **Total** (int) - 查询结果总数


        """
        # build request
        d = {}
        req and d.update(req)
        d = apis.ListMonitorProductRequestSchema().dumps(d)

        resp = self.invoke("ListMonitorProduct", d, **kwargs)
        return apis.ListMonitorProductResponseSchema().loads(resp)

    def modify_alert_strategy_remark(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ModifyAlertStrategyRemark - 修改告警策略备注

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **AlertStrategyID** (int) - (Required) 策略id
        - **Name** (str) - (Required) 策略名称
        - **Remark** (str) - 策略备注

        **Response**

        - **Data** (dict) - 见 **AlertStrategyId** 模型定义

        **Response Model**

        **AlertStrategyId**
        - **AlertStrategyID** (int) - 告警策略id


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.ModifyAlertStrategyRemarkRequestSchema().dumps(d)

        resp = self.invoke("ModifyAlertStrategyRemark", d, **kwargs)
        return apis.ModifyAlertStrategyRemarkResponseSchema().loads(resp)

    def query_metric_data_set(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """QueryMetricDataSet - 查询监控指标数据集

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **CalcMethod** (str) - (Required) 计算方式，枚举值如下：raw:原始值,max:最大值,min:最小值,avg:平均值,sum:求和
        - **EndTime** (int) - (Required) 截止时间，值为10位数时间戳
        - **Period** (int) - (Required) 周期，单位为秒，即：数据查询时，返回数据点的时间间隔。不同的查询时间范围，对应的周期不同：0<时间范围<=1h——周期：1分钟/5分钟，对应的枚举值为60、300；1h<时间范围<=12h——周期：1分钟/5分钟/1小时，对应的枚举值为60、300、3600；12h<时间范围<=24h——周期：5分钟/1小时，对应的枚举值为300、3600；1天<时间范围<=30天——周期：1小时/6小时/24小时，对应的枚举值为3600、21600、86400
        - **ProductKey** (str) - (Required) 产品唯一标识，参见  `产品概览 <https://docs.ucloud.cn/cloudwatch/metric/intro>`_
        - **StartTime** (int) - (Required) 开始时间，值为10位数时间戳
        - **MetricInfos** (list) - 见 **QueryMetricDataSetParamMetricInfos** 模型定义

        **Response**

        - **Data** (dict) - 见 **QueryMetricDataResp** 模型定义
        - **Message** (str) - 错误信息
        - **TraceId** (str) - 日志链路id

        **Request Model**

        **QueryMetricDataSetParamMetricInfosTagList**
        - **TagKey** (str) - 要查询指标的Tag的key，参数中N的取值范围为 0～9
        - **TagValues** (list) - 要查询指标的Tag的Value，参数中N的取值范围为 0～9


        **QueryMetricDataSetParamMetricInfos**
        - **Metric** (str) - 指标名，参数中N的取值范围为 0～9
        - **ResourceId** (str) - 指标所属资源id，参数中N的取值范围为 0～9
        - **TagList** (list) - 见 **QueryMetricDataSetParamMetricInfosTagList** 模型定义
        - **TagOperation** (str) - 多个 Tag 条件的匹配方式；可选 and（默认）或 or。TagList 中使用 or 时最多支持 3 个 Tag。


        **Response Model**

        **ObjectType**
        - **Id** (int) - ID
        - **Metas** (str) - {type: spec|basic, key:string, name: string}[] -> JSON字符串
        - **ObjectType** (str) - 资源类型ID
        - **ObjectTypeKey** (str) - 资源类型
        - **ProductCNName** (str) - 产品中文名称
        - **ProductENName** (str) - 产品英文名称
        - **ProductName** (str) - 产品名称
        - **ProductName1** (str) - 产品子名称


        **MetricSample**
        - **Timestamp** (int) - 时间戳
        - **Value** (float) - 样本值


        **MetricResult**
        - **ResourceId** (str) - 资源的短id
        - **ResourceName** (str) - 资源名称
        - **TagList** (list) - 资源标签列表。每项为 TagListItem：Tag（标签名）和 TagValue（标签值）。
        - **Values** (list) - 见 **MetricSample** 模型定义


        **QueryMetricDataRespItem**
        - **ErrCode** (int) - 该指标查询的处理状态码
        - **ErrMsg** (str) - 该指标查询的状态说明
        - **Metric** (str) - 指标名
        - **Results** (list) - 见 **MetricResult** 模型定义
        - **TagEntries** (list) - 见 **ObjectType** 模型定义


        **QueryMetricDataResp**
        - **InvalidResourceIds** (list) - 无效或无权限资源的 ID 列表
        - **List** (list) - 见 **QueryMetricDataRespItem** 模型定义


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.QueryMetricDataSetRequestSchema().dumps(d)

        resp = self.invoke("QueryMetricDataSet", d, **kwargs)
        return apis.QueryMetricDataSetResponseSchema().loads(resp)

    def query_metric_data_summary(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """QueryMetricDataSummary - 获取资源看图属性列表

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Limit** (int) - (Required) 当前页数据尺寸
        - **Offset** (int) - (Required) 跳过的数量
        - **ProductKey** (str) - (Required) 产品唯一标识，参见  `产品概览 <https://docs.ucloud.cn/cloudwatch/metric/intro>`_
        - **Metrics** (list) - 指定要查询的指标列表，不指定则使用默认的指标集合
        - **ResourceIds** (list) - 指定要查询的资源ID列表

        **Response**

        - **Data** (dict) - 见 **QueryMetricDataSummaryRespData** 模型定义
        - **Message** (str) - 错误信息
        - **TotalCount** (int) - 总数
        - **TraceId** (str) - 链路id

        **Response Model**

        **MetricSample**
        - **Timestamp** (int) - 时间戳
        - **Value** (float) - 样本值


        **Product**
        - **Id** (int) - ID
        - **Metas** (str) - {Type: 1|2, Key:string, Name: string}[] -> JSON字符串
        - **ProductChName** (str) - 产品中文名称
        - **ProductEnName** (str) - 产品英文名称
        - **ProductGroup** (str) - 产品分组
        - **ProductKey** (str) - 资源类型唯一key
        - **ProductName** (str) - 产品名称
        - **ProductName1** (str) - 产品子名称
        - **ProductType** (int) - 资源类型ID


        **MetricSingleSample**
        - **Metric** (str) - 指标名
        - **TagsList** (list) - 见 **Product** 模型定义
        - **Value** (dict) - 见 **MetricSample** 模型定义


        **ResourceMonitorItem**
        - **Metric** (str) - 指标名
        - **MetricValues** (list) - 见 **MetricSingleSample** 模型定义


        **ResourceSummary**
        - **CompanyId** (int) - 公司id
        - **LabelAttrList** (list) - 见 **ResourceMonitorItem** 模型定义
        - **MonitorAttr** (list) - 见 **ResourceMonitorItem** 模型定义
        - **Name** (str) - 资源名称
        - **OrganizationId** (int) - 项目id
        - **ProductKey** (str) - 产品类型
        - **ProjectId** (int) - 项目id
        - **Region** (str) - 地域
        - **RegionCN** (str) - 地域中文名
        - **ResourceExtendAttrList** (list) - 见 **ResourceMonitorItem** 模型定义
        - **ResourceId** (str) - 资源id
        - **Status** (int) - 资源状态
        - **Zone** (str) - 可用区
        - **ZoneCN** (str) - 可用区中文


        **QueryMetricDataSummaryRespData**
        - **List** (list) - 见 **ResourceSummary** 模型定义
        - **Total** (int) - 总数


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.QueryMetricDataSummaryRequestSchema().dumps(d)

        resp = self.invoke("QueryMetricDataSummary", d, **kwargs)
        return apis.QueryMetricDataSummaryResponseSchema().loads(resp)

    def un_bind_alert_strategy(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """UnBindAlertStrategy - 解绑告警策略

        **Request**

        - **ProjectId** (str) - (Config) 项目ID
        - **AlertStrategyIDs** (list) - (Required) 告警策略数组
        - **ProductKey** (str) - (Required) 产品唯一标识，参见  `产品概览 <https://docs.ucloud.cn/cloudwatch/metric/intro>`_
        - **Resources** (list) - (Required) 资源数组

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.UnBindAlertStrategyRequestSchema().dumps(d)

        resp = self.invoke("UnBindAlertStrategy", d, **kwargs)
        return apis.UnBindAlertStrategyResponseSchema().loads(resp)

    def update_alert_strategy(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """UpdateAlertStrategy - 更新告警策略

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **AlertStrategyID** (str) - (Required) 告警策略id
        - **ConfigMode** (int) - (Required) 条件设置方式 1. 手动配置 2.选择模版
        - **Name** (str) - (Required) 告警策略名称。最大长度255个字符
        - **ObjectType** (int) - (Required) 绑定资源类型 1 资源组 2 资源
        - **ProductKey** (str) - (Required) 产品唯一标识，参见  `产品概览 <https://docs.ucloud.cn/cloudwatch/metric/intro>`_
        - **CallbackLanguage** (str) - 通知渠道回调webhook 回调语言 中文cn 英文en
        - **CallbackUrls** (list) - 回调url
        - **NotifyChannelDs** (list) - 通知渠道 短信sms 邮件email 回调webhook
        - **NotifyGroupIDs** (list) - 通知人组id
        - **NotifyType** (str) - 通知类型 通知组 group 通知人 user
        - **NotifyUserIDs** (list) - 通知人id
        - **ResourceGroupIDs** (list) - 绑定资源组，对应绑定资源类型ObjectType=1
        - **Resources** (list) - 绑定资源，对应绑定资源类型ObjectType=2
        - **RuleSet** (list) - 见 **UpdateAlertStrategyParamRuleSet** 模型定义
        - **TemplateId** (int) - 模板id.对应ConfigMode=2

        **Response**

        - **Data** (dict) - 见 **AlertStrategyId** 模型定义

        **Request Model**

        **UpdateAlertStrategyParamRuleSet**
        - **Level** (str) - 告警等级。枚举值：P0,P1,P2,P3
        - **MetricID** (int) - 规则指标ID。参考该类型产品下返回的指标列表GetProductMetrics
        - **SendInterval** (int) - 沉默周期(告警周期选择为连续时必填)
        - **SendPeriodType** (str) - 触发周期。枚举值：continuous连续 exponent 指数 single 不重复
        - **Status** (int) - 告警状态。枚举值：0-关闭 1-开启
        - **ThresholdCompare** (int) - 阈值比较方式。 枚举值：1：>= 2：<= 3：> 4：< 5：== 6：!=
        - **ThresholdValue** (int) - 触发阈值
        - **TriggerCount** (int) - 触发次数


        **Response Model**

        **AlertStrategyId**
        - **AlertStrategyID** (int) - 告警策略id


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.UpdateAlertStrategyRequestSchema().dumps(d)

        resp = self.invoke("UpdateAlertStrategy", d, **kwargs)
        return apis.UpdateAlertStrategyResponseSchema().loads(resp)

    def update_alert_strategy_template(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """UpdateAlertStrategyTemplate - 编辑条件模板

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Name** (str) - (Required) 告警模板名称。最大长度64个字符
        - **ProductKey** (str) - (Required) 产品唯一标识，参见  `产品概览 <https://docs.ucloud.cn/cloudwatch/metric/intro>`_
        - **TemplateID** (int) - (Required) 条件模板ID
        - **Remark** (str) - 备注
        - **RuleSet** (list) - 见 **UpdateAlertStrategyTemplateParamRuleSet** 模型定义

        **Response**

        - **Data** (dict) - 见 **AlertTemplate** 模型定义

        **Request Model**

        **UpdateAlertStrategyTemplateParamRuleSet**
        - **Level** (str) - 告警等级。枚举值：P0,P1,P2,P3
        - **MetricID** (int) - 规则指标ID。参考该类型产品下返回的指标列表GetProductMetrics
        - **SendInterval** (int) - 沉默周期(告警周期选择为连续时必填)
        - **SendPeriodType** (str) - 触发周期。枚举值：continuous连续 exponent 指数 single 不重复
        - **Status** (int) - 告警状态。枚举值：0-关闭 1-开启
        - **ThresholdCompare** (int) - 阈值比较方式。枚举值比较方式: 1->=2-<= 3-> 4-< 5-== 6-!=
        - **ThresholdValue** (int) - 触发阈值
        - **TriggerCount** (int) - 触发次数


        **Response Model**

        **AlertTemplate**
        - **TemplateID** (int) - 告警模板ID


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.UpdateAlertStrategyTemplateRequestSchema().dumps(d)

        resp = self.invoke("UpdateAlertStrategyTemplate", d, **kwargs)
        return apis.UpdateAlertStrategyTemplateResponseSchema().loads(resp)
