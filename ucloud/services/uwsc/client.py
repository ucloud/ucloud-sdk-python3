""" Code is generated by ucloud-model, DO NOT EDIT IT. """

import typing


from ucloud.core.client import Client
from ucloud.services.uwsc.schemas import apis


class UWSCClient(Client):
    def __init__(
        self, config: dict, transport=None, middleware=None, logger=None
    ):
        super(UWSCClient, self).__init__(config, transport, middleware, logger)

    def add_export_line_rules(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """AddExportLineRules - 添加加速规则

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **ResourceId** (str) - (Required) UReach资源ID
        - **RuleTypes** (list) - (Required) 白名单类型：Developer、CrossBorder

        **Response**

        - **Message** (str) - 返回信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.AddExportLineRulesRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("AddExportLineRules", d, **kwargs)
        return apis.AddExportLineRulesResponseSchema().loads(resp)

    def bind_cpe(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """BindCPE - 绑定 CPE

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **CPEId** (str) - (Required) cpe id
        - **Sn** (str) - (Required) sn id

        **Response**

        - **Message** (str) - 返回信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.BindCPERequestSchema().dumps(d)

        resp = self.invoke("BindCPE", d, **kwargs)
        return apis.BindCPEResponseSchema().loads(resp)

    def create_cpe(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """CreateCPE - 创建 CPE

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **DeviceType** (str) - (Required) 设备型号，枚举值: UCPE3600、UCPE3601
        - **Name** (str) - (Required) cpe 名称
        - **Remark** (str) - (Required) cpe 备注
        - **Count** (int) - 数量，默认为1
        - **Label** (str) - 标签：UReach智能网关：Access
        - **UserInfo** (str) - 结构体，详见UserInfo模型

        **Response**

        - **CPEId** (str) - cpe id
        - **Message** (str) - 返回信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.CreateCPERequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateCPE", d, **kwargs)
        return apis.CreateCPEResponseSchema().loads(resp)

    def create_export_line(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CreateExportLine - 创建加速线路

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **CPEId** (str) - (Required) CPE Id
        - **FromRegion** (str) - (Required) 本端地域
        - **IpType** (str) - (Required) IP类型：International、BGP、Native、Resident
        - **ToRegion** (str) - (Required) 目标地域
        - **Bandwidth** (int) - 带宽大小
        - **ChargeType** (str) - 计费模式。枚举值为： Year，按年付费； Month，按月付费； Dynamic，按小时付费（需开启权限）。默认为月付
        - **CouponId** (int) - 代金券ID。请登录用户中心查看
        - **Name** (str) - 资源名称
        - **PayMode** (str) - 计费类型：fixed-bw，固定带宽；traffic，流量计费
        - **PkgType** (str) - 套餐类型：2M-Entry、5M-Basic、10M-Enterprise
        - **Quantity** (int) - 购买时长。默认: 1。按小时购买(Dynamic)时无需此参数。 月付时，此参数传0，代表了购买至月末。
        - **Remark** (str) - 备注
        - **Tag** (str) - 业务组名称, 默认为 "Default"

        **Response**

        - **Message** (str) - 返回信息
        - **ResourceId** (str) - 资源ID

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.CreateExportLineRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateExportLine", d, **kwargs)
        return apis.CreateExportLineResponseSchema().loads(resp)

    def delete_export_line(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DeleteExportLine - 删除加速线路

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **ResourceId** (str) - (Required) UReach资源ID

        **Response**

        - **Message** (str) - 返回信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.DeleteExportLineRequestSchema().dumps(d)

        resp = self.invoke("DeleteExportLine", d, **kwargs)
        return apis.DeleteExportLineResponseSchema().loads(resp)

    def delete_export_line_rules(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DeleteExportLineRules - 删除加速规则

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **ResourceId** (str) - (Required) UReach资源ID
        - **RuleTypes** (list) - (Required) 白名单类型：Developer、CrossBorder

        **Response**

        - **Message** (str) - 返回信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.DeleteExportLineRulesRequestSchema().dumps(d)

        resp = self.invoke("DeleteExportLineRules", d, **kwargs)
        return apis.DeleteExportLineRulesResponseSchema().loads(resp)

    def describe_cpe(self, req: typing.Optional[dict] = None, **kwargs) -> dict:
        """DescribeCPE - 查询 CPE 信息

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **CPEId** (str) - cpe id
        - **Label** (str) - 标签：Access、Network

        **Response**

        - **CPEInfos** (list) - 见 **CPEInfo** 模型定义
        - **Message** (str) - 返回信息

        **Response Model**

        **CPEInfo**
        - **CPEId** (str) - 资源ID
        - **CPEName** (str) - 资源名称
        - **ConfUpdateTime** (int) - 配置更新时间
        - **CreateTime** (int) - 创建时间
        - **DeviceTypeName** (str) - 设备型号
        - **LineStatus** (str) - 是否与线路绑定
        - **LinkNum** (int) - 链路数量
        - **PopGwId** (list) - 绑定的UWAN资源ID
        - **Ports** (list) - 端口
        - **Remark** (str) - 备注
        - **ResourceIds** (list) - 绑定的UReach线路资源ID
        - **Sn** (str) - 设备SN
        - **Status** (int) - 状态
        - **UseTime** (int) - 有效使用时间(天)
        - **Vendor** (str) - 供应商


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.DescribeCPERequestSchema().dumps(d)

        resp = self.invoke("DescribeCPE", d, **kwargs)
        return apis.DescribeCPEResponseSchema().loads(resp)

    def describe_export_line(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DescribeExportLine - 查询CPE绑定的加速线路信息

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **CPEId** (str) - CPE资源ID
        - **ResourceId** (str) - UReach资源ID

        **Response**

        - **LineInfos** (list) - 见 **LineInfo** 模型定义
        - **Message** (str) - 返回信息

        **Response Model**

        **SocksInfo**
        - **Account** (str) -
        - **Addr** (str) -
        - **Port** (int) -


        **LineInfo**
        - **Bandwidth** (int) - 带宽大小(Mbps)
        - **CPEIds** (list) - 线路绑定的CPE资源ID
        - **ChargeType** (str) - 付费方式(按月、按年等)
        - **CreateTime** (int) - 创建时间
        - **ExpireTime** (int) - 到期时间
        - **FromRegion** (str) - 入口地域
        - **FromRegionName** (str) - 入口地域名称
        - **IP** (list) -
        - **InstanceId** (str) - 线路资源ID对应的加速线路ID
        - **InstanceStatus** (str) - 是否过期(normal/expire)
        - **IpType** (str) - IP类型
        - **Name** (str) - 名称
        - **PayMode** (str) - 计费方式(固定带宽: fixed-bw；流量计费：traffic)
        - **PkgType** (str) - 套餐类型
        - **Remark** (str) - 备注
        - **ResourceId** (str) - 线路资源ID
        - **Socks** (dict) - 见 **SocksInfo** 模型定义
        - **Source** (list) - 源IP地址
        - **Status** (str) - 线路是否绑定CPE；"1"：已绑定；"0"未绑定
        - **ToRegion** (str) - 出口地域
        - **ToRegionName** (str) - 出口地域名称


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.DescribeExportLineRequestSchema().dumps(d)

        resp = self.invoke("DescribeExportLine", d, **kwargs)
        return apis.DescribeExportLineResponseSchema().loads(resp)

    def describe_export_line_rules(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DescribeExportLineRules - 查询白名单

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **ResourceId** (str) - UReach资源ID
        - **RuleType** (str) - 白名单类型

        **Response**

        - **Message** (str) - 返回信息
        - **WhiteListInfos** (list) - 见 **WhiteListInfo** 模型定义

        **Response Model**

        **WhiteListInfo**
        - **Name** (str) -
        - **Remark** (str) -
        - **RuleType** (str) -


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.DescribeExportLineRulesRequestSchema().dumps(d)

        resp = self.invoke("DescribeExportLineRules", d, **kwargs)
        return apis.DescribeExportLineRulesResponseSchema().loads(resp)

    def update_export_line(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """UpdateExportLine - 修改加速线路

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Bandwidth** (str) - (Required) 带宽大小（仅自定义套餐支持修改）
        - **ResourceId** (str) - (Required) UReach资源ID

        **Response**

        - **Message** (str) - 返回信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.UpdateExportLineRequestSchema().dumps(d)

        resp = self.invoke("UpdateExportLine", d, **kwargs)
        return apis.UpdateExportLineResponseSchema().loads(resp)

    def upgrade_export_line(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """UpgradeExportLine - 升级加速线路

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **IpType** (str) - (Required) 原IP类型：International、BGP、Native、Resident
        - **PkgType** (str) - (Required) 目标套餐类型：2M-Entry、5M-Basic、10M-Enterprise
        - **ResourceId** (str) - (Required) UReach资源ID
        - **Bandwidth** (int) - 带宽大小
        - **ChargeType** (str) - 计费模式。枚举值为： Year，按年付费； Month，按月付费； Dynamic，按小时付费（需开启权限）。默认为月付
        - **CouponId** (int) - 代金券ID。请登录用户中心查看
        - **Quantity** (int) - 购买时长。默认: 1。按小时购买(Dynamic)时无需此参数。 月付时，此参数传0，代表了购买至月末。

        **Response**

        - **Message** (str) - 返回信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
        }
        req and d.update(req)
        d = apis.UpgradeExportLineRequestSchema().dumps(d)

        resp = self.invoke("UpgradeExportLine", d, **kwargs)
        return apis.UpgradeExportLineResponseSchema().loads(resp)
