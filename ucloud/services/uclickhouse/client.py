""" Code is generated by ucloud-model, DO NOT EDIT IT. """

import typing


from ucloud.core.client import Client
from ucloud.services.uclickhouse.schemas import apis


class UClickhouseClient(Client):
    def __init__(
        self, config: dict, transport=None, middleware=None, logger=None
    ):
        super(UClickhouseClient, self).__init__(
            config, transport, middleware, logger
        )

    def create_u_clickhouse_cluster(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CreateUClickhouseCluster - 创建UClickhouse集群

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **AdminPassword** (str) - (Required) 集群管理员密码，密码规则：1.密码长度限8-32个字符2.不能包含[A-Z],[a-z],[0-9]和[@#%^*+=_;:,?!&()-]之外的字符3.需要同时包含两项或以上（大写字母/小写字母/数字/特殊符号）
        - **ClickhouseMachineTypeId** (str) - (Required) 集群机型，可通过GetUClickhouseClusterCreateOption接口获取具体值
        - **ClickhouseVersion** (str) - (Required) Clickhouse版本，可通过GetUClickhouseClusterCreateOption接口获取具体版本
        - **DataDiskType** (str) - (Required) 数据盘类型，可通过GetUClickhouseClusterCreateOption接口获取具体值
        - **SubnetId** (str) - (Required) 子网ID
        - **VPCId** (str) - (Required) VPC ID
        - **BackupId** (str) - 备份任务ID，从备份恢复时，该字段必传，此值为备份任务ID，可以从原集群备份任务列表（ListUClickhouseBackups）获取
        - **ChargeType** (str) - 付费类型，枚举值：Year（年付），Month（月付），Dynamic（时付），默认值为Month,月付
        - **ClusterName** (str) - 实例名称名称规则：1.长度为1-50位的字符2.不能包含_,中文,[A-Z],[a-z],[0-9]之外的非法字符，集群名称默认为clickhouse
        - **DataDiskSize** (int) - 数据盘大小，最小100，步长为50，默认值为100，单位GB
        - **IsMultiZone** (str) - 是否多可用区，默认为false
        - **IsSecGroup** (str) - 是否开启安全组，true为开启，false为不开启，默认为false，不开启安全组
        - **IsZookeeperHA** (bool) - 是否Zookeeper高可用，true为zookeeper高可用，false为非高可用，默认为true，高可用
        - **Labels** (list) - 见 **CreateUClickhouseClusterParamLabels** 模型定义
        - **MultiZones** (list) - 【数组】可用区名称，IsMultiZone为true时，必传，可通过ListUClickhouseAvailableZone获取支持的可用区
        - **Quantity** (int) - 购买时长，默认值为1。月付时，此参数传0，代表购买至月末
        - **ReplicateCount** (int) - 副本数量，取值为1或2，1为单副本（非高可用），2为双副本（高可用），默认值为高可用（即为2）
        - **SecGroupIds** (str) - 安全组ID，IsSecGroup为true时，必传
        - **ShardCount** (int) - 分片数量，若传递，则至少1个分片,默认值为1
        - **ZookeeperDataDiskSize** (str) - Zookeeper数据盘大小，IsZookeeperHA为true时，必传，最小100，步长为50
        - **ZookeeperDataDiskType** (str) - Zookeeper数据盘类型，IsZookeeperHA为true时，必传，可通过GetUClickhouseClusterCreateOption接口获取具体值
        - **ZookeeperMachineTypeId** (str) - Zookeeper机型ID，IsZookeeperHA为true时，必传，可通过GetUClickhouseClusterCreateOption接口获取具体值

        **Response**

        - **Data** (dict) - 见 **CreateUClickhouseClusterResponseData** 模型定义
        - **Message** (str) - 返回信息

        **Request Model**

        **CreateUClickhouseClusterParamLabels**
        - **Key** (str) - 标签的key
        - **Value** (str) - 标签的value


        **Response Model**

        **CreateUClickhouseClusterResponseData**
        - **ClusterId** (str) - 集群ID


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.CreateUClickhouseClusterRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateUClickhouseCluster", d, **kwargs)
        return apis.CreateUClickhouseClusterResponseSchema().loads(resp)

    def describe_u_clickhouse_cluster(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DescribeUClickhouseCluster - 获取集群信息

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterId** (str) - (Required) 集群ID

        **Response**

        - **Data** (dict) - 见 **DescribeUClickhouseClusterResponseData** 模型定义
        - **Message** (str) - 返回信息

        **Response Model**

        **ClickhouseCluster**
        - **ClickhouseDataDiskSize** (int) - Clickhouse数据盘大小
        - **ClickhouseDataDiskType** (str) - Clickhouse数据盘类型
        - **ClickhouseMachineTypeId** (str) - Clickhouse机型ID
        - **ClickhouseMachineTypeName** (str) - Clickhouse机型名称
        - **ClickhouseNodeCPU** (int) - Clickhouse节点CPU
        - **ClickhouseNodeMemory** (int) - Clickhouse内存
        - **ClickhouseVersion** (str) - 集群版本
        - **ClusterId** (str) - 集群ID
        - **ClusterName** (str) - 集群名称
        - **CreateTimestamp** (int) - 集群创建时间
        - **ExpireTimestamp** (float) - 实例过期时间
        - **IsBackup** (str) - 实例是否开启备份
        - **IsSecgroup** (str) - 实例是否开启安全组
        - **IsTieredStorage** (str) - 实例是否开启冷热分层
        - **IsZookeeperHA** (str) - Zookeeper是否高可用
        - **MachineType** (str) - 机型
        - **MultiZones** (list) - 实例所在可用区
        - **OrganizationId** (int) - 项目ID
        - **RegionId** (int) - 地域ID
        - **ReplicateCount** (int) - 副本数
        - **ShardCount** (int) - 分片数
        - **Status** (str) - 集群状态：CREATING（创建中）、RUNNING（运行中）、RESIZING（变配中）、RESTARTING（重启中）、UPGRADING（升级中）、DESTROYING（销毁中）、DESTROYED（已删除）、CREATE_FAILED（创建失败）、RESTART_FAILED（重启失败）、DESTROY_FAILED（删除失败）、RESIZE_FAILED（变配失败）、BACKUP_RESTORING（备份恢复中）、BACKUP_RESTORE_FAILED（备份恢复失败）、EXPANDING（扩容中）、EXPAND_FAILED（扩容失败）
        - **SubnetId** (str) - 子网ID
        - **TopOrganizationId** (int) - 公司ID
        - **VPCId** (str) - VPCID
        - **ZookeeperDataDiskSize** (int) - Zookeeper数据盘大小
        - **ZookeeperDataDiskType** (str) - Zookeeper数据盘类型
        - **ZookeeperMachineTypeId** (str) - Zookeeper机型ID
        - **ZookeeperMachineTypeName** (str) - Zookeeper机型名称
        - **ZookeeperNodeCPU** (int) - Zookeeper节点CPU
        - **ZookeeperNodeMemory** (int) - Zookeeper节点内存
        - **ZookeeperVersion** (str) - Zookeeper版本


        **Payment**
        - **ChargeType** (str) - 支付类型
        - **CreateTimestamp** (int) - 创建时间
        - **ExpireTimestamp** (int) - 过期时间
        - **OriginalPrice** (int) - 原始价格
        - **Price** (int) - 价格
        - **ResourceId** (str) - 资源ID


        **ZookeeperNode**
        - **CPU** (int) - CPU
        - **ClusterId** (str) - 集群ID
        - **CreateTimestamp** (int) - 创建时间
        - **DataDiskSize** (int) - 数据盘大小
        - **DataDiskType** (str) - 数据盘类型
        - **MachineType** (str) - 机型
        - **Memory** (int) - 内存
        - **NodeId** (str) - 节点ID
        - **NodeName** (str) - 节点名称
        - **ResourceId** (str) - 资源ID
        - **ServiceStatus** (str) - 服务状态：RUNNING（运行中）、STARTING（启动中）、STOPPED（已停止）、RESTARTING（重启中）
        - **SysDiskSize** (int) - 系统盘大小
        - **SysDiskType** (str) - 系统盘类型
        - **Zone** (str) - 可用区名称
        - **ZookeeperMyId** (str) - zookeeper的myid


        **ClickhouseNode**
        - **CPU** (int) - CPU大小
        - **ClusterId** (str) - 集群ID
        - **CreateTimestamp** (int) - 创建时间
        - **DataDiskSize** (int) - 数据盘大小,GB
        - **DataDiskType** (str) - 数据盘类型
        - **IPv4** (str) - ipv4
        - **MachineType** (str) - 机型
        - **Memory** (int) - 内存，GB
        - **NodeId** (str) - 节点ID
        - **NodeName** (str) - 节点名称
        - **ResourceId** (str) - 资源ID
        - **ServiceStatus** (str) - 服务状态：RUNNING（运行中）、STARTING（启动中）、STOPPED（已停止）、RESTARTING（重启中）
        - **ShardGroup** (str) - 分片组
        - **SysDiskSize** (int) - 系统盘大小,GB
        - **SysDiskType** (str) - 系统盘类型
        - **Zone** (str) - 可用区名称


        **DescribeUClickhouseClusterResponseData**
        - **ClickhouseNodes** (list) - 见 **ClickhouseNode** 模型定义
        - **Cluster** (dict) - 见 **ClickhouseCluster** 模型定义
        - **Payment** (dict) - 见 **Payment** 模型定义
        - **ZookeeperNodes** (list) - 见 **ZookeeperNode** 模型定义


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DescribeUClickhouseClusterRequestSchema().dumps(d)

        resp = self.invoke("DescribeUClickhouseCluster", d, **kwargs)
        return apis.DescribeUClickhouseClusterResponseSchema().loads(resp)

    def destroy_u_clickhouse_cluster(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DestroyUClickhouseCluster - 删除CK集群

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterId** (str) - (Required) 集群ID

        **Response**

        - **Message** (str) - 返回信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DestroyUClickhouseClusterRequestSchema().dumps(d)

        resp = self.invoke("DestroyUClickhouseCluster", d, **kwargs)
        return apis.DestroyUClickhouseClusterResponseSchema().loads(resp)

    def expand_u_clickhouse_cluster(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ExpandUClickhouseCluster - 集群水平扩容

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterId** (str) - (Required) 集群ID
        - **TotalNodeCount** (int) - (Required) 扩容后集群的节点数量，集群为高可用时，需要传入偶数个节点
        - **SyncNodeId** (str) - 水平扩容时，选择某一个原节点的ID，用于同步表结构/用户信息。不传递时，表示不同步表结构/用户信息，仅水平扩容节点数量

        **Response**

        - **Message** (str) - 返回信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ExpandUClickhouseClusterRequestSchema().dumps(d)

        resp = self.invoke("ExpandUClickhouseCluster", d, **kwargs)
        return apis.ExpandUClickhouseClusterResponseSchema().loads(resp)

    def get_u_clickhouse_cluster_create_option(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetUClickhouseClusterCreateOption - 获取Clickhouse的创建配置项

        **Request**

        - **ProjectId** (str) - (Config) 项目Id
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Data** (dict) - 见 **GetCreateUClickhouseClusterOptionResponseData** 模型定义
        - **Message** (str) - 返回信息

        **Response Model**

        **ClickhouseDataDisk**
        - **DefaultDataDiskSize** (int) - 默认大小，GB
        - **DiskType** (str) - 磁盘类型
        - **MaxDiskSize** (int) - 最大值，GB
        - **MinDiskSize** (int) - 最小值，GB
        - **Step** (int) - 步长，GB


        **ClickhouseMachineTypeOption**
        - **CPU** (int) - CPU大小
        - **ClickhouseMachineTypeId** (str) - Clickhouse机型ID
        - **DataDisks** (list) - 见 **ClickhouseDataDisk** 模型定义
        - **MachineType** (str) - 机型
        - **Memory** (int) - 内存大小，GB
        - **NodeCounts** (list) - 允许创建的节点个数


        **ClickhouseMachineType**
        - **ClickhouseMachineTypeName** (str) - CK机型名称
        - **ClickhouseMachineTypeOptions** (list) - 见 **ClickhouseMachineTypeOption** 模型定义
        - **IsSecgroupMachineType** (str) - 机型是否支持安全组


        **ClickhouseVersion**
        - **Version** (str) - 版本号
        - **VersionName** (str) - 版本名称


        **GetCreateUClickhouseClusterOptionResponseData**
        - **ClickhouseMachineTypes** (list) - 见 **ClickhouseMachineType** 模型定义
        - **ClickhouseVersions** (list) - 见 **ClickhouseVersion** 模型定义
        - **MaxNodeCount** (int) - 实例可创建的最大节点数量
        - **ZookeeperMachineTypes** (list) - 见 **ClickhouseMachineType** 模型定义


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GetUClickhouseClusterCreateOptionRequestSchema().dumps(d)

        resp = self.invoke("GetUClickhouseClusterCreateOption", d, **kwargs)
        return apis.GetUClickhouseClusterCreateOptionResponseSchema().loads(
            resp
        )

    def list_u_clickhouse_cluster(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListUClickhouseCluster - 获取UClickhouse集群列表

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Data** (dict) - 见 **ListUClickhouseClusterResponseData** 模型定义
        - **Message** (str) - 返回信息

        **Response Model**

        **ClickhouseCluster**
        - **ClickhouseDataDiskSize** (int) - Clickhouse数据盘大小
        - **ClickhouseDataDiskType** (str) - Clickhouse数据盘类型
        - **ClickhouseMachineTypeId** (str) - Clickhouse机型ID
        - **ClickhouseMachineTypeName** (str) - Clickhouse机型名称
        - **ClickhouseNodeCPU** (int) - Clickhouse节点CPU
        - **ClickhouseNodeMemory** (int) - Clickhouse内存
        - **ClickhouseVersion** (str) - 集群版本
        - **ClusterId** (str) - 集群ID
        - **ClusterName** (str) - 集群名称
        - **CreateTimestamp** (int) - 集群创建时间
        - **ExpireTimestamp** (float) - 实例过期时间
        - **IsBackup** (str) - 实例是否开启备份
        - **IsSecgroup** (str) - 实例是否开启安全组
        - **IsTieredStorage** (str) - 实例是否开启冷热分层
        - **IsZookeeperHA** (str) - Zookeeper是否高可用
        - **MachineType** (str) - 机型
        - **MultiZones** (list) - 实例所在可用区
        - **OrganizationId** (int) - 项目ID
        - **RegionId** (int) - 地域ID
        - **ReplicateCount** (int) - 副本数
        - **ShardCount** (int) - 分片数
        - **Status** (str) - 集群状态：CREATING（创建中）、RUNNING（运行中）、RESIZING（变配中）、RESTARTING（重启中）、UPGRADING（升级中）、DESTROYING（销毁中）、DESTROYED（已删除）、CREATE_FAILED（创建失败）、RESTART_FAILED（重启失败）、DESTROY_FAILED（删除失败）、RESIZE_FAILED（变配失败）、BACKUP_RESTORING（备份恢复中）、BACKUP_RESTORE_FAILED（备份恢复失败）、EXPANDING（扩容中）、EXPAND_FAILED（扩容失败）
        - **SubnetId** (str) - 子网ID
        - **TopOrganizationId** (int) - 公司ID
        - **VPCId** (str) - VPCID
        - **ZookeeperDataDiskSize** (int) - Zookeeper数据盘大小
        - **ZookeeperDataDiskType** (str) - Zookeeper数据盘类型
        - **ZookeeperMachineTypeId** (str) - Zookeeper机型ID
        - **ZookeeperMachineTypeName** (str) - Zookeeper机型名称
        - **ZookeeperNodeCPU** (int) - Zookeeper节点CPU
        - **ZookeeperNodeMemory** (int) - Zookeeper节点内存
        - **ZookeeperVersion** (str) - Zookeeper版本


        **ListUClickhouseClusterResponseData**
        - **Clusters** (dict) - 见 **ClickhouseCluster** 模型定义
        - **TotalCount** (int) - 集群总数


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListUClickhouseClusterRequestSchema().dumps(d)

        resp = self.invoke("ListUClickhouseCluster", d, **kwargs)
        return apis.ListUClickhouseClusterResponseSchema().loads(resp)

    def resize_u_clickhouse_cluster(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ResizeUClickhouseCluster - 集群改配

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterId** (str) - (Required) 集群ID
        - **IsZooKeeperNode** (bool) - 是否为zookeeper节点，为true时表示升级zookeeper节点规格，为false时表示升级clickhouse节点规格，默认为false
        - **TargetDataDiskSize** (int) - 目标磁盘大小，单位GB，只能扩容，与TargetMachineTypeId不能同时为空
        - **TargetMachineTypeId** (str) - 目标机型ID，可通过GetUClickhouseClusterCreateOption接口获取具体值，与TargetDataDiskSize不能同时为空

        **Response**

        - **Message** (str) - 返回信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ResizeUClickhouseClusterRequestSchema().dumps(d)

        resp = self.invoke("ResizeUClickhouseCluster", d, **kwargs)
        return apis.ResizeUClickhouseClusterResponseSchema().loads(resp)

    def restart_u_clickhouse_cluster_service(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """RestartUClickhouseClusterService - 重启集群的UClickhouse服务

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **ClusterId** (str) - (Required) 集群ID

        **Response**

        - **Message** (str) - 返回信息

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.RestartUClickhouseClusterServiceRequestSchema().dumps(d)

        resp = self.invoke("RestartUClickhouseClusterService", d, **kwargs)
        return apis.RestartUClickhouseClusterServiceResponseSchema().loads(resp)
