""" Code is generated by ucloud-model, DO NOT EDIT IT. """

import typing


from ucloud.core.client import Client
from ucloud.services.sandbox.schemas import apis


class SandboxClient(Client):
    def __init__(
        self, config: dict, transport=None, middleware=None, logger=None
    ):
        super(SandboxClient, self).__init__(
            config, transport, middleware, logger
        )

    def check_sandbox_custom_domain(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CheckSandboxCustomDomain - 检查自定义域名

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **CustomDomain** (str) - (Required) 自定义域名
        - **TargetDomain** (str) - (Required) 目标域名
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Get** (bool) - 如果为true，则表示获取检查结果

        **Response**

        - **Status** (str) - 检查结果Unknown:未知Checking：检查中Success：成功Timeout：超时Error：检查失败

        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.CheckSandboxCustomDomainRequestSchema().dumps(d)

        resp = self.invoke("CheckSandboxCustomDomain", d, **kwargs)
        return apis.CheckSandboxCustomDomainResponseSchema().loads(resp)

    def create_sandbox_site(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """CreateSandboxSite - 创建站点空间

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **APIKey** (str) - (Required) 密钥
        - **AccessCode** (str) - (Required) 访问码
        - **KeyID** (str) - (Required) 密钥ID
        - **Name** (str) - (Required) 站点空间名称
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Envs** (str) - 环境变量，格式：["key=value"]

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.CreateSandboxSiteRequestSchema().dumps(d)

        # build options
        kwargs["max_retries"] = 0  # ignore retry when api is not idempotent

        resp = self.invoke("CreateSandboxSite", d, **kwargs)
        return apis.CreateSandboxSiteResponseSchema().loads(resp)

    def delete_sandbox_site(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """DeleteSandboxSite - 删除站点空间

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **SandboxID** (str) - (Required) 站点沙箱ID
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.DeleteSandboxSiteRequestSchema().dumps(d)

        resp = self.invoke("DeleteSandboxSite", d, **kwargs)
        return apis.DeleteSandboxSiteResponseSchema().loads(resp)

    def get_sandbox_site(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """GetSandboxSite - 获取沙箱站点

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **SandboxID** (str) - (Required) 沙箱ID
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **Site** (dict) - 见 **Site** 模型定义

        **Response Model**

        **SiteIPAccess**
        - **IPList** (list) - IP 列表
        - **Mode** (str) - IP限制模式"":不限制IP“allow”:自定义白名单“deny”:自定义黑名单


        **Site**
        - **AccessCode** (str) - 访问码
        - **ConnectKey** (str) - 连接密钥
        - **CustomDomain** (str) - 自定义域名
        - **CustomDomainError** (str) - 自定义域名签发错误信息
        - **CustomDomainStatus** (str) - 自定义域名签发状态
        - **Envs** (list) - 站点环境变量。格式：key=value
        - **Host** (str) - 站点访问地址
        - **ID** (str) - 站点ID
        - **IPAccess** (dict) - 见 **SiteIPAccess** 模型定义
        - **KeyID** (str) - 站点Key ID
        - **Name** (str) - 名称
        - **Ready** (bool) - 是否就绪
        - **SandboxID** (str) - 沙箱ID
        - **UpdateTime** (int) - 更新时间


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.GetSandboxSiteRequestSchema().dumps(d)

        resp = self.invoke("GetSandboxSite", d, **kwargs)
        return apis.GetSandboxSiteResponseSchema().loads(resp)

    def list_sandbox_sites(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """ListSandboxSites - 列出站点空间

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**

        - **CustomTargetDomain** (str) - 目标域名地址
        - **Sites** (list) - 见 **Site** 模型定义

        **Response Model**

        **SiteIPAccess**
        - **IPList** (list) - IP 列表
        - **Mode** (str) - IP限制模式"":不限制IP“allow”:自定义白名单“deny”:自定义黑名单


        **Site**
        - **AccessCode** (str) - 访问码
        - **ConnectKey** (str) - 连接密钥
        - **CustomDomain** (str) - 自定义域名
        - **CustomDomainError** (str) - 自定义域名签发错误信息
        - **CustomDomainStatus** (str) - 自定义域名签发状态
        - **Envs** (list) - 站点环境变量。格式：["key=value"]
        - **Host** (str) - 站点访问地址
        - **ID** (str) - 站点ID
        - **IPAccess** (dict) - 见 **SiteIPAccess** 模型定义
        - **KeyID** (str) - 站点Key ID
        - **Name** (str) - 名称
        - **Ready** (bool) - 是否就绪
        - **SandboxID** (str) - 沙箱ID
        - **UpdateTime** (int) - 更新时间


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.ListSandboxSitesRequestSchema().dumps(d)

        resp = self.invoke("ListSandboxSites", d, **kwargs)
        return apis.ListSandboxSitesResponseSchema().loads(resp)

    def update_sandbox_site(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """UpdateSandboxSite - 更新站点空间

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **SandboxID** (str) - (Required) 站点沙箱ID
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **APIKey** (str) - 更新站点空间API Key
        - **AccessCode** (str) - 访问码
        - **Envs** (str) - 更新站点空间环境变量，格式为["Key=Value"]
        - **IPAccess** (dict) - 见 **UpdateSandboxSiteParamIPAccess** 模型定义
        - **KeyID** (str) - 更新站点空间Key ID（需要和APIKey一起传）
        - **Name** (str) - 更新站点空间名称

        **Response**


        **Request Model**

        **UpdateSandboxSiteParamIPAccess**
        - **IPList** (str) - 访问限制IP，格式["192.168.1.2","192.168.1.3"]
        - **Mode** (str) - 访问模式，- ""：无IP限制- "allow":白名单模式- "deny":黑名单


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.UpdateSandboxSiteRequestSchema().dumps(d)

        resp = self.invoke("UpdateSandboxSite", d, **kwargs)
        return apis.UpdateSandboxSiteResponseSchema().loads(resp)

    def update_sandbox_site_custom_domain(
        self, req: typing.Optional[dict] = None, **kwargs
    ) -> dict:
        """UpdateSandboxSiteCustomDomain - 更新沙箱站点自定义域名

        **Request**

        - **ProjectId** (str) - (Config) 项目ID。不填写为默认项目，子帐号必须填写。 请参考 `GetProjectList接口 <https://docs.ucloud.cn/api/summary/get_project_list>`_
        - **Region** (str) - (Config) 地域。 参见  `地域和可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_
        - **CustomDomain** (str) - (Required) 自定义域名
        - **SandboxID** (str) - (Required) 沙箱ID
        - **Zone** (str) - (Required) 可用区。参见  `可用区列表 <https://docs.ucloud.cn/api/summary/regionlist>`_

        **Response**


        """
        # build request
        d = {
            "ProjectId": self.config.project_id,
            "Region": self.config.region,
        }
        req and d.update(req)
        d = apis.UpdateSandboxSiteCustomDomainRequestSchema().dumps(d)

        resp = self.invoke("UpdateSandboxSiteCustomDomain", d, **kwargs)
        return apis.UpdateSandboxSiteCustomDomainResponseSchema().loads(resp)
